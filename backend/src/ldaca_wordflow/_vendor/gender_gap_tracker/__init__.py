"""Pinned quotation-extractor subset from SFU GenderGapTracker.

Only the local quote extractor and its verb list are shipped. The upstream
project's database, dashboard, training, and command-line code are deliberately
excluded from the backend runtime package. See ``LICENSE`` for upstream terms.
"""
