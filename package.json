{
  "name": "ldaca-wordflow-root",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "description": "Root workspace for LDaCA Wordflow",
  "scripts": {
    "dev": "pnpm -C frontend dev",
    "build": "pnpm -C frontend build",
    "bump-version": "node scripts/bump-version.mjs",
    "check-versions": "node scripts/check-versions.mjs",
    "deploy_frontend_to_backend": "pnpm -C frontend build && node scripts/deploy-frontend-to-backend.mjs",
    "desktop:dev": "pnpm -C frontend desktop:dev",
    "desktop:build:mac": "pnpm -C frontend desktop:build:mac",
    "desktop:build:windows": "pnpm -C frontend desktop:build:windows",
    "prepare:backend-runtime": "pnpm -C frontend prepare:backend-runtime"
  }
}