# Plan

1. Replace legacy preview and detach contracts with root Preview Sessions and
   typed Run All children.
2. Persist Annotation selectors, settings, and correction-column choices.
3. Render Review from current durable Data Blocks, including the Concordance
   source-to-result left join.
4. Cut schemas to Workspace 8 and archive 7, regenerate the SDK, update docs,
   and verify the clean contract.
