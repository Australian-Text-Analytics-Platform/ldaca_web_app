# Tasks

- [x] Add failing strict-domain and existing publication contract tests.
- [x] Replace every `output_node_id` contract with `output_node_ids`.
- [x] Add failing Topic Modeling child request, worker, publication, rollback,
      persistence, and API tests.
- [x] Implement typed atomic Topic Modeling detachment.
- [x] Regenerate the OpenAPI client.
- [x] Add frontend request wiring, result action, dialog, cache invalidation,
      and component tests.
- [x] Update durable docs and tutorials.
- [x] Run backend, frontend, docs, diff, and browser verification. The live
      browser reached the frontend startup gate, but the pre-existing process
      holding port 8001 did not answer health checks; focused UI tests and the
      production build verified the new surface instead.
- [x] Archive the completed spec and close issue #12 with verification evidence.
