# Plan

1. Add the version-2-compatible account preference and regenerate OpenAPI.
2. Add the pinned manifest, deterministic generator, pre-React bootstrap, and
   account-authoritative runtime.
3. Replace shared primitives and migrate every live surface and integration.
4. Preserve stable data colors and theme-independent white exports.
5. Add source/runtime regressions, update durable documentation, run package
   checks, complete visual acceptance, and archive this specification.
