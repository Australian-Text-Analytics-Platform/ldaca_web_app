/**
 * Workspace store has been fully replaced by WorkspaceProvider + React context slices.
 * This module remains as an empty stub so legacy imports fail loudly at build time.
 */

export const useWorkspaceStore = () => {
  throw new Error('useWorkspaceStore has been removed. Use WorkspaceProvider hooks instead.');
};
