// This file is auto-generated by @hey-api/openapi-ts

import { type Client, formDataBodySerializer, type Options as Options2, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { AddNodeToWorkspaceData, AddNodeToWorkspaceErrors, AddNodeToWorkspaceResponses, AdminCleanupData, AdminCleanupErrors, AdminCleanupResponses, AiAnnotationCurrentTasksData, AiAnnotationCurrentTasksErrors, AiAnnotationCurrentTasksResponses, AiAnnotationTaskRequestData, AiAnnotationTaskRequestErrors, AiAnnotationTaskRequestResponses, AiAnnotationTaskResultData, AiAnnotationTaskResultErrors, AiAnnotationTaskResultPostData, AiAnnotationTaskResultPostErrors, AiAnnotationTaskResultPostResponses, AiAnnotationTaskResultResponses, AuthHealthData, AuthHealthResponses, AuthStatusData, AuthStatusErrors, AuthStatusResponses, CalculateTokenFrequenciesData, CalculateTokenFrequenciesErrors, CalculateTokenFrequenciesResponses, CancelTaskData, CancelTaskErrors, CancelTaskResponses, CastNodeData, CastNodeErrors, CastNodeResponses, CilogonCallbackData, CilogonCallbackErrors, CilogonCallbackResponses, CilogonLoginData, CilogonLoginResponses, ClearAiAnnotationData, ClearAiAnnotationErrors, ClearAiAnnotationResponses, ClearFilesTasksData, ClearFilesTasksErrors, ClearFilesTasksResponses, ClearTasksData, ClearTasksErrors, ClearTasksResponses, ClearTokenFrequenciesData, ClearTokenFrequenciesErrors, ClearTokenFrequenciesResponses, ClearTopicModelingEmbeddingCacheData, ClearTopicModelingEmbeddingCacheErrors, ClearTopicModelingEmbeddingCacheResponses, ClearTopicModelingResultsData, ClearTopicModelingResultsErrors, ClearTopicModelingResultsResponses, CloneNodeData, CloneNodeErrors, CloneNodeResponses, ColumnOperationsData, ColumnOperationsErrors, ColumnOperationsResponses, ConcatNodesData, ConcatNodesErrors, ConcatNodesPreviewData, ConcatNodesPreviewErrors, ConcatNodesPreviewResponses, ConcatNodesResponses, ConcordanceCurrentTasksData, ConcordanceCurrentTasksErrors, ConcordanceCurrentTasksResponses, ConcordanceDetachOptionsData, ConcordanceDetachOptionsErrors, ConcordanceDetachOptionsResponses, ConcordanceTaskDispersionBinsData, ConcordanceTaskDispersionBinsErrors, ConcordanceTaskDispersionBinsResponses, ConcordanceTaskRequestData, ConcordanceTaskRequestErrors, ConcordanceTaskRequestResponses, ConcordanceTaskResultData, ConcordanceTaskResultErrors, ConcordanceTaskResultPostData, ConcordanceTaskResultPostErrors, ConcordanceTaskResultPostResponses, ConcordanceTaskResultResponses, CreateFolderData, CreateFolderErrors, CreateFolderResponses, CreateWorkspaceData, CreateWorkspaceErrors, CreateWorkspaceResponses, DeleteFileData, DeleteFileErrors, DeleteFileResponses, DeleteNodeColumnData, DeleteNodeColumnErrors, DeleteNodeColumnResponses, DeleteNodeData, DeleteNodeErrors, DeleteNodeResponses, DeleteWorkspaceData, DeleteWorkspaceErrors, DeleteWorkspaceResponses, DescribeColumnData, DescribeColumnErrors, DescribeColumnResponses, DetachAiAnnotationData, DetachAiAnnotationErrors, DetachAiAnnotationResponses, DetachConcordanceData, DetachConcordanceDispersionData, DetachConcordanceDispersionErrors, DetachConcordanceDispersionResponses, DetachConcordanceErrors, DetachConcordanceResponses, DetachQuotationData, DetachQuotationErrors, DetachQuotationResponses, DetachSequentialAnalysisTaskData, DetachSequentialAnalysisTaskErrors, DetachSequentialAnalysisTaskResponses, DetachTopicModelingData, DetachTopicModelingErrors, DetachTopicModelingResponses, DownloadFileData, DownloadFileErrors, DownloadFileResponses, DownloadWorkspaceArtifactData, DownloadWorkspaceArtifactErrors, DownloadWorkspaceArtifactResponses, ExportNodesData, ExportNodesErrors, ExportNodesResponses, FilterNodeData, FilterNodeErrors, FilterNodeResponses, FilterPreviewData, FilterPreviewErrors, FilterPreviewResponses, GetAiAnnotationCategoriesData, GetAiAnnotationCategoriesErrors, GetAiAnnotationCategoriesResponses, GetAiAnnotationModelsData, GetAiAnnotationModelsErrors, GetAiAnnotationModelsResponses, GetAiAnnotationProvidersData, GetAiAnnotationProvidersErrors, GetAiAnnotationProvidersResponses, GetAuthInfoData, GetAuthInfoErrors, GetAuthInfoResponses, GetColumnUniqueValuesData, GetColumnUniqueValuesErrors, GetColumnUniqueValuesResponses, GetConfigData, GetConfigResponses, GetCurrentUserInfoData, GetCurrentUserInfoErrors, GetCurrentUserInfoResponses, GetCurrentWorkspaceData, GetCurrentWorkspaceErrors, GetCurrentWorkspaceResponses, GetFileInfoData, GetFileInfoErrors, GetFileInfoResponses, GetNodeDataData, GetNodeDataErrors, GetNodeDataResponses, GetNodeInfoData, GetNodeInfoErrors, GetNodeInfoResponses, GetNodeQueryPlanData, GetNodeQueryPlanErrors, GetNodeQueryPlanResponses, GetNodeShapeData, GetNodeShapeErrors, GetNodeShapeResponses, GetPreferencesData, GetPreferencesErrors, GetPreferencesResponses, GetQuotationData, GetQuotationErrors, GetQuotationResponses, GetRawFileData, GetRawFileErrors, GetRawFileResponses, GetSampleDataCatalogueData, GetSampleDataCatalogueErrors, GetSampleDataCatalogueResponses, GetSampleDataReadmeData, GetSampleDataReadmeErrors, GetSampleDataReadmeResponses, GetTokenizerModelsData, GetTokenizerModelsErrors, GetTokenizerModelsResponses, GetTopicModelingEmbeddingCacheSizeData, GetTopicModelingEmbeddingCacheSizeErrors, GetTopicModelingEmbeddingCacheSizeResponses, GetUserFilesData, GetUserFilesErrors, GetUserFilesResponses, GetWorkspaceGraphData, GetWorkspaceGraphErrors, GetWorkspaceGraphResponses, GetWorkspaceInfoData, GetWorkspaceInfoErrors, GetWorkspaceInfoResponses, GetWorkspaceNodesData, GetWorkspaceNodesErrors, GetWorkspaceNodesResponses, GetWorkspaceTabsData, GetWorkspaceTabsErrors, GetWorkspaceTabsResponses, GetWorkspaceUiStateData, GetWorkspaceUiStateErrors, GetWorkspaceUiStateResponses, GoogleAuthCallbackData, GoogleAuthCallbackErrors, GoogleAuthCallbackResponses, GoogleAuthData, GoogleAuthErrors, GoogleAuthResponses, HealthCheckData, HealthCheckResponses, ImportLdacaDatasetData, ImportLdacaDatasetErrors, ImportLdacaDatasetResponses, ImportSampleDataData, ImportSampleDataErrors, ImportSampleDataResponses, JoinNodesData, JoinNodesErrors, JoinNodesPreviewData, JoinNodesPreviewErrors, JoinNodesPreviewResponses, JoinNodesResponses, ListFilesTasksData, ListFilesTasksErrors, ListFilesTasksResponses, ListLdacaFeaturedCollectionsData, ListLdacaFeaturedCollectionsErrors, ListLdacaFeaturedCollectionsResponses, ListTasksData, ListTasksErrors, ListTasksResponses, ListUsersData, ListUsersErrors, ListUsersResponses, ListWorkspacesData, ListWorkspacesErrors, ListWorkspacesResponses, LogoutData, LogoutErrors, LogoutResponses, MaterializeConcordanceData, MaterializeConcordanceErrors, MaterializeConcordanceResponses, MaterializeQuotationData, MaterializeQuotationErrors, MaterializeQuotationResponses, MoveFileData, MoveFileErrors, MoveFileResponses, PolarsExpressionApplyData, PolarsExpressionApplyErrors, PolarsExpressionApplyResponses, PolarsExpressionPreviewData, PolarsExpressionPreviewErrors, PolarsExpressionPreviewResponses, PreviewSequentialAnalysisData, PreviewSequentialAnalysisErrors, PreviewSequentialAnalysisResponses, PutWorkspaceTabsData, PutWorkspaceTabsErrors, PutWorkspaceTabsResponses, PutWorkspaceUiStateData, PutWorkspaceUiStateErrors, PutWorkspaceUiStateResponses, QuotationCurrentTasksData, QuotationCurrentTasksErrors, QuotationCurrentTasksResponses, QuotationDetachOptionsData, QuotationDetachOptionsErrors, QuotationDetachOptionsResponses, QuotationTaskRequestData, QuotationTaskRequestErrors, QuotationTaskRequestResponses, QuotationTaskResultData, QuotationTaskResultErrors, QuotationTaskResultResponses, RedoNodeOperationData, RedoNodeOperationErrors, RedoNodeOperationResponses, RenameNodeColumnData, RenameNodeColumnErrors, RenameNodeColumnResponses, RenameWorkspaceData, RenameWorkspaceErrors, RenameWorkspaceResponses, ReplaceApplyData, ReplaceApplyErrors, ReplaceApplyResponses, ReplacePreviewData, ReplacePreviewErrors, ReplacePreviewResponses, RootData, RootResponses, RunAiAnnotationData, RunAiAnnotationErrors, RunAiAnnotationResponses, RunConcordanceData, RunConcordanceErrors, RunConcordanceResponses, RunSequentialAnalysisData, RunSequentialAnalysisErrors, RunSequentialAnalysisResponses, RunTopicModelingData, RunTopicModelingErrors, RunTopicModelingResponses, SaveAiAnnotationData, SaveAiAnnotationErrors, SaveAiAnnotationResponses, SaveWorkspaceData, SaveWorkspaceErrors, SaveWorkspaceResponses, SearchLdacaCollectionsData, SearchLdacaCollectionsErrors, SearchLdacaCollectionsResponses, SequentialAnalysisCurrentTasksData, SequentialAnalysisCurrentTasksErrors, SequentialAnalysisCurrentTasksResponses, SequentialAnalysisTaskRequestData, SequentialAnalysisTaskRequestErrors, SequentialAnalysisTaskRequestResponses, SequentialAnalysisTaskResultData, SequentialAnalysisTaskResultErrors, SequentialAnalysisTaskResultResponses, SetCurrentWorkspaceData, SetCurrentWorkspaceErrors, SetCurrentWorkspaceResponses, SetNodeDocumentColumnData, SetNodeDocumentColumnErrors, SetNodeDocumentColumnResponses, SetNodeTokenizationPreferenceData, SetNodeTokenizationPreferenceErrors, SetNodeTokenizationPreferenceResponses, SliceNodeData, SliceNodeErrors, SliceNodeResponses, SlicePreviewData, SlicePreviewErrors, SlicePreviewResponses, StartWorkspaceDownloadData, StartWorkspaceDownloadErrors, StartWorkspaceDownloadResponses, StatusData, StatusResponses, StreamTasksData, StreamTasksErrors, StreamTasksResponses, TokenFrequenciesCurrentTasksData, TokenFrequenciesCurrentTasksErrors, TokenFrequenciesCurrentTasksResponses, TokenFrequenciesTaskRequestData, TokenFrequenciesTaskRequestErrors, TokenFrequenciesTaskRequestResponses, TokenFrequenciesTaskResultData, TokenFrequenciesTaskResultErrors, TokenFrequenciesTaskResultResponses, TopicModelingCurrentTasksData, TopicModelingCurrentTasksErrors, TopicModelingCurrentTasksResponses, TopicModelingDetachOptionsData, TopicModelingDetachOptionsErrors, TopicModelingDetachOptionsResponses, TopicModelingTaskRequestData, TopicModelingTaskRequestErrors, TopicModelingTaskRequestResponses, TopicModelingTaskResultData, TopicModelingTaskResultErrors, TopicModelingTaskResultResponses, UndoNodeOperationData, UndoNodeOperationErrors, UndoNodeOperationResponses, UnifiedFilePreviewData, UnifiedFilePreviewErrors, UnifiedFilePreviewResponses, UnloadWorkspaceData, UnloadWorkspaceErrors, UnloadWorkspaceResponses, UpdateConfigData, UpdateConfigErrors, UpdateConfigResponses, UpdateNodeNameData, UpdateNodeNameErrors, UpdateNodeNameResponses, UpdatePreferencesData, UpdatePreferencesErrors, UpdatePreferencesResponses, UpdateQuotationTaskResultData, UpdateQuotationTaskResultErrors, UpdateQuotationTaskResultResponses, UpdateSequentialAnalysisTaskResultData, UpdateSequentialAnalysisTaskResultErrors, UpdateSequentialAnalysisTaskResultResponses, UpdateTokenFrequenciesTaskResultData, UpdateTokenFrequenciesTaskResultErrors, UpdateTokenFrequenciesTaskResultResponses, UpdateWorkspaceDescriptionData, UpdateWorkspaceDescriptionErrors, UpdateWorkspaceDescriptionResponses, UploadFileData, UploadFileErrors, UploadFileResponses, UploadWorkspaceZipData, UploadWorkspaceZipErrors, UploadWorkspaceZipResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Root
 *
 * Return API feature/index metadata.
 *
 * Used by:
 * - browser/manual root checks and basic service discovery because callers need the shared
 * shared backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Provides a human-readable entrypoint summary for backend capabilities.
 */
export const root = <ThrowOnError extends boolean = false>(options?: Options<RootData, ThrowOnError>) => (options?.client ?? client).get<RootResponses, unknown, ThrowOnError>({ url: '/', ...options });

/**
 * Admin Cleanup
 *
 * Trigger cleanup of expired session rows.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - admin maintenance actions because they need this unit's "Trigger cleanup of expired session rows" behavior.
 *
 * Why:
 * - Allows manual session-store maintenance in addition to automatic cleanup.
 *
 * Refactor note:
 * - Add `require_admin` dependency before wider deployment.
 */
export const adminCleanup = <ThrowOnError extends boolean = false>(options?: Options<AdminCleanupData, ThrowOnError>) => (options?.client ?? client).get<AdminCleanupResponses, AdminCleanupErrors, ThrowOnError>({ url: '/api/admin/cleanup', ...options });

/**
 * List Users
 *
 * List users with active-session counts.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - admin dashboard/user management views because they need this unit's "List users with active-session counts" behavior.
 *
 * Why:
 * - Provides operational visibility into user and session activity.
 *
 * Refactor note:
 * - Add `require_admin` dependency before wider deployment to avoid role drift.
 */
export const listUsers = <ThrowOnError extends boolean = false>(options?: Options<ListUsersData, ThrowOnError>) => (options?.client ?? client).get<ListUsersResponses, ListUsersErrors, ThrowOnError>({ url: '/api/admin/users', ...options });

/**
 * Get Auth Info
 *
 * Main auth endpoint - tells frontend everything it needs to know.
 *
 * Returns:
 * - In single-user mode: authenticated=True with root user info
 * - In multi-user mode with valid token: authenticated=True with user info
 * - In multi-user mode without token: authenticated=False with available auth methods
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend app bootstrap auth probe because they need this unit's "Main auth endpoint - tells frontend everything it needs to know" behavior.
 *
 * Why:
 * - Provides one canonical auth capability + identity payload per startup.
 */
export const getAuthInfo = <ThrowOnError extends boolean = false>(options?: Options<GetAuthInfoData, ThrowOnError>) => (options?.client ?? client).get<GetAuthInfoResponses, GetAuthInfoErrors, ThrowOnError>({ url: '/api/auth/', ...options });

/**
 * Cilogon Callback
 *
 * Handle the CILogon authorization code callback.
 *
 * CILogon redirects here after the user authenticates. We verify the CSRF
 * ``state``, exchange the authorization code for tokens, fetch the user's
 * profile from the userinfo endpoint, provision a local session, and
 * redirect back to the SPA with the access token in the URL query string.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /cilogon/callback route because they need this unit's "Handle the CILogon authorization code callback" behavior.
 */
export const cilogonCallback = <ThrowOnError extends boolean = false>(options?: Options<CilogonCallbackData, ThrowOnError>) => (options?.client ?? client).get<CilogonCallbackResponses, CilogonCallbackErrors, ThrowOnError>({ url: '/api/auth/cilogon/callback', ...options });

/**
 * Cilogon Login
 *
 * Redirect the browser to the CILogon authorization endpoint.
 *
 * Generates a random ``state`` for CSRF protection (stored as a short-lived
 * cookie), builds the OIDC authorization URL from the discovery document,
 * and redirects the user to CILogon to authenticate.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /cilogon/login route because they need this unit's "Redirect the browser to the CILogon authorization endpoint" behavior.
 */
export const cilogonLogin = <ThrowOnError extends boolean = false>(options?: Options<CilogonLoginData, ThrowOnError>) => (options?.client ?? client).get<CilogonLoginResponses, unknown, ThrowOnError>({ url: '/api/auth/cilogon/login', ...options });

/**
 * Google Auth
 *
 * Authenticate user via Google OAuth and create app session tokens.
 *
 * Used by the frontend Google sign-in flow (JSON body variant). Delegates
 * the verification + provisioning logic to :func:`_verify_and_create_session`
 * and shapes the response into ``GoogleOut``.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /google route because they need this unit's "Authenticate user via Google OAuth and create app session tokens" behavior.
 */
export const googleAuth = <ThrowOnError extends boolean = false>(options: Options<GoogleAuthData, ThrowOnError>) => (options.client ?? client).post<GoogleAuthResponses, GoogleAuthErrors, ThrowOnError>({
    url: '/api/auth/google',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Google Auth Callback
 *
 * Handle the Google Identity Services redirect callback.
 *
 * Google POSTs the ID-token ``credential`` and a CSRF token here after the
 * user authenticates on Google's consent page (``ux_mode: 'redirect'``).
 * We verify the token, create/find the local user, issue a session, and
 * redirect back to the SPA with the access token in the URL fragment.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /google/callback route because they need this unit's "Handle the Google Identity Services redirect callback" behavior.
 */
export const googleAuthCallback = <ThrowOnError extends boolean = false>(options: Options<GoogleAuthCallbackData, ThrowOnError>) => (options.client ?? client).post<GoogleAuthCallbackResponses, GoogleAuthCallbackErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/auth/google/callback',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Auth Health
 *
 * Return authentication subsystem readiness metadata.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - health/status probes and diagnostics pages because they need this unit's "Return authentication subsystem readiness metadata" behavior.
 *
 * Why:
 * - Exposes auth mode and endpoint availability without authentication.
 */
export const authHealth = <ThrowOnError extends boolean = false>(options?: Options<AuthHealthData, ThrowOnError>) => (options?.client ?? client).get<AuthHealthResponses, unknown, ThrowOnError>({ url: '/api/auth/health', ...options });

/**
 * Logout
 *
 * Logout current user session (multi-user) or no-op (single-user).
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend sign-out action because they need this unit's "Logout current user session (multi-user) or no-op (single-user)" behavior.
 *
 * Why:
 * - Keeps logout behavior mode-aware while preserving shared endpoint contract.
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>) => (options?.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({ url: '/api/auth/logout', ...options });

/**
 * Get Current User Info
 *
 * Return normalized current user profile fields.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend profile/session widgets because they need this unit's "Return normalized current user profile fields" behavior.
 *
 * Why:
 * - Provides stable user response shape independent of DB row types.
 */
export const getCurrentUserInfo = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserInfoData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentUserInfoResponses, GetCurrentUserInfoErrors, ThrowOnError>({ url: '/api/auth/me', ...options });

/**
 * Auth Status
 *
 * Return minimal authenticated status payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - lightweight frontend auth status checks because they need this unit's "Return minimal authenticated status payload" behavior.
 *
 * Why:
 * - Allows cheap auth verification without full `get_auth_info` metadata.
 */
export const authStatus = <ThrowOnError extends boolean = false>(options?: Options<AuthStatusData, ThrowOnError>) => (options?.client ?? client).get<AuthStatusResponses, AuthStatusErrors, ThrowOnError>({ url: '/api/auth/status', ...options });

/**
 * Get Config
 *
 * Return currently effective runtime configuration values.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend settings/config panels and OAuth client bootstrap because they need this unit's "Return currently effective runtime configuration values" behavior.
 *
 * Why:
 * - Exposes backend mode, storage root, and Google OAuth client ID so the
 * frontend can initialize the login provider at runtime.
 */
export const getConfig = <ThrowOnError extends boolean = false>(options?: Options<GetConfigData, ThrowOnError>) => (options?.client ?? client).get<GetConfigResponses, unknown, ThrowOnError>({ url: '/api/config/', ...options });

/**
 * Update Config
 *
 * Update in-memory runtime configuration values.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend config edit flow because they need this unit's "Update in-memory runtime configuration values" behavior.
 *
 * Why:
 * - Allows runtime overrides without process restart.
 *
 * Refactor note:
 * - Current update is in-memory only; persist-or-reload strategy may be needed
 * for multi-process or restart-stable configuration behavior.
 */
export const updateConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateConfigData, ThrowOnError>) => (options.client ?? client).post<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError>({
    url: '/api/config/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get User Files
 *
 * List user-visible files as a nested tree.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file browser panel.
 *
 * Why:
 * - The frontend consumes the directory structure directly instead of
 * reconstructing it from a flat file listing.
 */
export const getUserFiles = <ThrowOnError extends boolean = false>(options?: Options<GetUserFilesData, ThrowOnError>) => (options?.client ?? client).get<GetUserFilesResponses, GetUserFilesErrors, ThrowOnError>({ url: '/api/files/', ...options });

/**
 * Create Folder
 *
 * Create a folder inside the user's data directory.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /folders route.
 */
export const createFolder = <ThrowOnError extends boolean = false>(options: Options<CreateFolderData, ThrowOnError>) => (options.client ?? client).post<CreateFolderResponses, CreateFolderErrors, ThrowOnError>({
    url: '/api/files/folders',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import Ldaca Dataset
 *
 * Submit background task to import LDaCA dataset from URL.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend dataset import action.
 *
 * Why:
 * - Runs network/download/import pipeline outside request-response lifecycle.
 */
export const importLdacaDataset = <ThrowOnError extends boolean = false>(options: Options<ImportLdacaDatasetData, ThrowOnError>) => (options.client ?? client).post<ImportLdacaDatasetResponses, ImportLdacaDatasetErrors, ThrowOnError>({
    url: '/api/files/import-ldaca',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import Sample Data
 *
 * Import (or re-import) sample data for the current user on demand.
 *
 * Copies bundled datasets (SCL, ADO/twitter) immediately, then downloads any
 * missing or updated remote datasets if SAMPLE_DATA_REMOTE_URL is configured.
 *
 * If request.collection_ids is non-empty, only those collections are
 * downloaded remotely. An empty list means "download all non-bundled
 * collections" (default).
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /import-sample-data route.
 */
export const importSampleData = <ThrowOnError extends boolean = false>(options?: Options<ImportSampleDataData, ThrowOnError>) => (options?.client ?? client).post<ImportSampleDataResponses, ImportSampleDataErrors, ThrowOnError>({
    url: '/api/files/import-sample-data',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * List Ldaca Featured Collections
 *
 * Return staff-picked LDaCA collections for the import dialog.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /ldaca/featured route.
 */
export const listLdacaFeaturedCollections = <ThrowOnError extends boolean = false>(options?: Options<ListLdacaFeaturedCollectionsData, ThrowOnError>) => (options?.client ?? client).get<ListLdacaFeaturedCollectionsResponses, ListLdacaFeaturedCollectionsErrors, ThrowOnError>({ url: '/api/files/ldaca/featured', ...options });

/**
 * Search Ldaca Collections
 *
 * Search LDaCA records through the backend ONI proxy.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /ldaca/search route.
 */
export const searchLdacaCollections = <ThrowOnError extends boolean = false>(options: Options<SearchLdacaCollectionsData, ThrowOnError>) => (options.client ?? client).post<SearchLdacaCollectionsResponses, SearchLdacaCollectionsErrors, ThrowOnError>({
    url: '/api/files/ldaca/search',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Move File
 *
 * Move a file into another directory inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /move route.
 */
export const moveFile = <ThrowOnError extends boolean = false>(options: Options<MoveFileData, ThrowOnError>) => (options.client ?? client).post<MoveFileResponses, MoveFileErrors, ThrowOnError>({
    url: '/api/files/move',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unified File Preview
 *
 * Unified file preview endpoint.
 *
 * - Returns supported types based on extension.
 * - Provides preview data (first few rows or page slice).
 * - For Excel files, returns sheet_names and supports selecting sheet via
 * payload.sheet_name.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file preview modal/table.
 *
 * Why:
 * - Provides one format-aware preview API for heterogeneous file types.
 */
export const unifiedFilePreview = <ThrowOnError extends boolean = false>(options: Options<UnifiedFilePreviewData, ThrowOnError>) => (options.client ?? client).post<UnifiedFilePreviewResponses, UnifiedFilePreviewErrors, ThrowOnError>({
    url: '/api/files/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Raw File
 *
 * Return raw UTF-8 text content for a file inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /raw route.
 */
export const getRawFile = <ThrowOnError extends boolean = false>(options: Options<GetRawFileData, ThrowOnError>) => (options.client ?? client).get<GetRawFileResponses, GetRawFileErrors, ThrowOnError>({ url: '/api/files/raw', ...options });

/**
 * Get Sample Data Catalogue
 *
 * Return the sample data catalogue augmented with per-collection status.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sample-data/catalogue route.
 */
export const getSampleDataCatalogue = <ThrowOnError extends boolean = false>(options?: Options<GetSampleDataCatalogueData, ThrowOnError>) => (options?.client ?? client).get<GetSampleDataCatalogueResponses, GetSampleDataCatalogueErrors, ThrowOnError>({ url: '/api/files/sample-data/catalogue', ...options });

/**
 * Get Sample Data Readme
 *
 * Proxy a README.md from the remote sample data repository.
 *
 * Only .md files are permitted; path traversal is rejected.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sample-data/readme route.
 */
export const getSampleDataReadme = <ThrowOnError extends boolean = false>(options: Options<GetSampleDataReadmeData, ThrowOnError>) => (options.client ?? client).get<GetSampleDataReadmeResponses, GetSampleDataReadmeErrors, ThrowOnError>({ url: '/api/files/sample-data/readme', ...options });

/**
 * List Files Tasks
 *
 * List file-import worker tasks exposed via the files API.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend import-task status polling.
 *
 * Why:
 * - Exposes file-import task status via explicit task types.
 */
export const listFilesTasks = <ThrowOnError extends boolean = false>(options?: Options<ListFilesTasksData, ThrowOnError>) => (options?.client ?? client).get<ListFilesTasksResponses, ListFilesTasksErrors, ThrowOnError>({ url: '/api/files/tasks', ...options });

/**
 * Clear Files Tasks
 *
 * Clear persisted file-import task records.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend task-list cleanup actions.
 *
 * Why:
 * - Removes completed/failed import task clutter while keeping artifacts.
 */
export const clearFilesTasks = <ThrowOnError extends boolean = false>(options?: Options<ClearFilesTasksData, ThrowOnError>) => (options?.client ?? client).post<ClearFilesTasksResponses, ClearFilesTasksErrors, ThrowOnError>({ url: '/api/files/tasks/clear', ...options });

/**
 * Upload File
 *
 * Upload file to user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /upload route.
 */
export const uploadFile = <ThrowOnError extends boolean = false>(options: Options<UploadFileData, ThrowOnError>) => (options.client ?? client).post<UploadFileResponses, UploadFileErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/api/files/upload',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Delete File
 *
 * Delete user's file.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /{filename:path} route.
 */
export const deleteFile = <ThrowOnError extends boolean = false>(options: Options<DeleteFileData, ThrowOnError>) => (options.client ?? client).delete<DeleteFileResponses, DeleteFileErrors, ThrowOnError>({ url: '/api/files/{filename}', ...options });

/**
 * Download File
 *
 * Download user's file.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /{filename:path} route.
 */
export const downloadFile = <ThrowOnError extends boolean = false>(options: Options<DownloadFileData, ThrowOnError>) => (options.client ?? client).get<DownloadFileResponses, DownloadFileErrors, ThrowOnError>({ url: '/api/files/{filename}', ...options });

/**
 * Get File Info
 *
 * Get detailed file information.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /{filename:path}/info route.
 */
export const getFileInfo = <ThrowOnError extends boolean = false>(options: Options<GetFileInfoData, ThrowOnError>) => (options.client ?? client).get<GetFileInfoResponses, GetFileInfoErrors, ThrowOnError>({ url: '/api/files/{filename}/info', ...options });

/**
 * Get Preferences
 *
 * Return get preferences API requests for preference routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET / route because they need this unit's "Return get preferences API requests for preference routes" behavior.
 */
export const getPreferences = <ThrowOnError extends boolean = false>(options?: Options<GetPreferencesData, ThrowOnError>) => (options?.client ?? client).get<GetPreferencesResponses, GetPreferencesErrors, ThrowOnError>({ url: '/api/preferences/', ...options });

/**
 * Update Preferences
 *
 * Update update preferences API requests for preference routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT / route because they need this unit's "Update update preferences API requests for preference routes" behavior.
 */
export const updatePreferences = <ThrowOnError extends boolean = false>(options: Options<UpdatePreferencesData, ThrowOnError>) => (options.client ?? client).put<UpdatePreferencesResponses, UpdatePreferencesErrors, ThrowOnError>({
    url: '/api/preferences/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Tasks
 *
 * List tasks for current user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET / route because they need this unit's "List tasks for current user" behavior.
 */
export const listTasks = <ThrowOnError extends boolean = false>(options?: Options<ListTasksData, ThrowOnError>) => (options?.client ?? client).get<ListTasksResponses, ListTasksErrors, ThrowOnError>({ url: '/api/tasks', ...options });

/**
 * Cancel Task
 *
 * Stop a running task and mark it as cancelled.
 *
 * Unlike ``/tasks/clear``, the task record is kept so the user can see the
 * cancelled state in the task list and explicitly clear it afterwards.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /cancel route because they need this unit's "Stop a running task and mark it as cancelled" behavior.
 */
export const cancelTask = <ThrowOnError extends boolean = false>(options: Options<CancelTaskData, ThrowOnError>) => (options.client ?? client).post<CancelTaskResponses, CancelTaskErrors, ThrowOnError>({ url: '/api/tasks/cancel', ...options });

/**
 * Clear Tasks
 *
 * Clear a task and all associated caches by task id.
 *
 * If the task is still running it is cancelled first.  Both the worker
 * task manager record and the analysis task manager record (including
 * the current-task-id mapping) are removed.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /clear route because they need this unit's "Clear a task and all associated caches by task id" behavior.
 */
export const clearTasks = <ThrowOnError extends boolean = false>(options: Options<ClearTasksData, ThrowOnError>) => (options.client ?? client).post<ClearTasksResponses, ClearTasksErrors, ThrowOnError>({ url: '/api/tasks/clear', ...options });

/**
 * Stream Tasks
 *
 * Unified SSE stream for task center.
 *
 * Includes all user tasks from a single per-user task manager channel.
 *
 * - frontend Task Center SSE subscriber
 *
 * Why:
 * - Streams all per-user task updates through one connection.
 *
 * Refactor note:
 * - Nested helper closures inside endpoint are sizeable; extraction to a small
 * streaming service object could improve testability.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /stream route because they need this unit's "Unified SSE stream for task center" behavior.
 */
export const streamTasks = <ThrowOnError extends boolean = false>(options?: Options<StreamTasksData, ThrowOnError>) => (options?.client ?? client).get<StreamTasksResponses, StreamTasksErrors, ThrowOnError>({ url: '/api/tasks/stream', ...options });

/**
 * List Workspaces
 *
 * List all persisted workspaces visible to the current user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend workspace switcher/landing views because they need this unit's "List all persisted workspaces visible to the current user" behavior.
 *
 * Why:
 * - Provides fast summary metadata without loading full workspace graphs.
 */
export const listWorkspaces = <ThrowOnError extends boolean = false>(options?: Options<ListWorkspacesData, ThrowOnError>) => (options?.client ?? client).get<ListWorkspacesResponses, ListWorkspacesErrors, ThrowOnError>({ url: '/api/workspaces/', ...options });

/**
 * Create Workspace
 *
 * Create a workspace and return normalized workspace metadata.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend new-workspace dialog because they need this unit's "Create a workspace and return normalized workspace metadata" behavior.
 *
 * Why:
 * - Centralizes workspace-name validation and initialization metadata.
 */
export const createWorkspace = <ThrowOnError extends boolean = false>(options: Options<CreateWorkspaceData, ThrowOnError>) => (options.client ?? client).post<CreateWorkspaceResponses, CreateWorkspaceErrors, ThrowOnError>({
    url: '/api/workspaces/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clear Ai Annotation
 *
 * Clear stored AI annotation task state.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /ai-annotation route because they need this unit's "Clear stored AI annotation task state" behavior.
 */
export const clearAiAnnotation = <ThrowOnError extends boolean = false>(options?: Options<ClearAiAnnotationData, ThrowOnError>) => (options?.client ?? client).delete<ClearAiAnnotationResponses, ClearAiAnnotationErrors, ThrowOnError>({ url: '/api/workspaces/ai-annotation', ...options });

/**
 * Run Ai Annotation
 *
 * Run AI annotation classification on selected nodes.
 *
 * Calls the OpenAI-compatible endpoint, stores results in
 * the analysis task manager, and returns paginated output.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /ai-annotation route because they need this unit's "Run AI annotation classification on selected nodes" behavior.
 */
export const runAiAnnotation = <ThrowOnError extends boolean = false>(options: Options<RunAiAnnotationData, ThrowOnError>) => (options.client ?? client).post<RunAiAnnotationResponses, RunAiAnnotationErrors, ThrowOnError>({
    url: '/api/workspaces/ai-annotation',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Ai Annotation Models
 *
 * List available models from an OpenAI-compatible endpoint.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /ai-annotation/models route because they need this unit's "List available models from an OpenAI-compatible endpoint" behavior.
 */
export const getAiAnnotationModels = <ThrowOnError extends boolean = false>(options: Options<GetAiAnnotationModelsData, ThrowOnError>) => (options.client ?? client).post<GetAiAnnotationModelsResponses, GetAiAnnotationModelsErrors, ThrowOnError>({
    url: '/api/workspaces/ai-annotation/models',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ai Annotation Current Tasks
 *
 * Return current task IDs for AI annotation analysis.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /ai-annotation/tasks/current route because they need this unit's "Return current task IDs for AI annotation analysis" behavior.
 */
export const aiAnnotationCurrentTasks = <ThrowOnError extends boolean = false>(options?: Options<AiAnnotationCurrentTasksData, ThrowOnError>) => (options?.client ?? client).get<AiAnnotationCurrentTasksResponses, AiAnnotationCurrentTasksErrors, ThrowOnError>({ url: '/api/workspaces/ai-annotation/tasks/current', ...options });

/**
 * Ai Annotation Task Request
 *
 * Return stored request payload for an AI annotation task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /ai-annotation/tasks/{task_id}/request route because they need this unit's "Return stored request payload for an AI annotation task" behavior.
 */
export const aiAnnotationTaskRequest = <ThrowOnError extends boolean = false>(options: Options<AiAnnotationTaskRequestData, ThrowOnError>) => (options.client ?? client).get<AiAnnotationTaskRequestResponses, AiAnnotationTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/ai-annotation/tasks/{task_id}/request', ...options });

/**
 * Ai Annotation Task Result
 *
 * Read AI annotation result with optional pagination/sort overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /ai-annotation/tasks/{task_id}/result route because they need this unit's "Read AI annotation result with optional pagination/sort overrides" behavior.
 */
export const aiAnnotationTaskResult = <ThrowOnError extends boolean = false>(options: Options<AiAnnotationTaskResultData, ThrowOnError>) => (options.client ?? client).get<AiAnnotationTaskResultResponses, AiAnnotationTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/ai-annotation/tasks/{task_id}/result', ...options });

/**
 * Ai Annotation Task Result Post
 *
 * Read AI annotation result using POST body overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /ai-annotation/tasks/{task_id}/result route because they need this unit's "Read AI annotation result using POST body overrides" behavior.
 */
export const aiAnnotationTaskResultPost = <ThrowOnError extends boolean = false>(options: Options<AiAnnotationTaskResultPostData, ThrowOnError>) => (options.client ?? client).post<AiAnnotationTaskResultPostResponses, AiAnnotationTaskResultPostErrors, ThrowOnError>({
    url: '/api/workspaces/ai-annotation/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Run Concordance
 *
 * Run concordance immediately and store task metadata for retrieval.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend run route: `POST /workspaces/{id}/concordance` because they need this unit's "Run concordance immediately and store task metadata for retrieval" behavior.
 *
 * Why:
 * - Keeps API behavior aligned with other analyses by returning task-linked
 * responses while using shared concordance response builders.
 */
export const runConcordance = <ThrowOnError extends boolean = false>(options: Options<RunConcordanceData, ThrowOnError>) => (options.client ?? client).post<RunConcordanceResponses, RunConcordanceErrors, ThrowOnError>({
    url: '/api/workspaces/concordance',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Concordance Current Tasks
 *
 * Return current task IDs for concordance analysis.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /concordance/tasks/current route because they need this unit's "Return current task IDs for concordance analysis" behavior.
 */
export const concordanceCurrentTasks = <ThrowOnError extends boolean = false>(options?: Options<ConcordanceCurrentTasksData, ThrowOnError>) => (options?.client ?? client).get<ConcordanceCurrentTasksResponses, ConcordanceCurrentTasksErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/current', ...options });

/**
 * Concordance Task Dispersion Bins
 *
 * Return 100-bucket dispersion histogram for one materialised concordance node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend dispersion summary plot, when a node has been materialised by because they need this unit's "Return 100-bucket dispersion histogram for one materialised concordance node" behavior.
 * "Process All". The frontend re-aggregates these 100 buckets into a
 * smaller number of display bins (4, 5, 10, 20, 25, 50, 100) without
 * another network round-trip.
 *
 * Why:
 * - Server-side pre-binning collapses the response from one row per hit
 * (potentially tens of MB on large blocks) to ~100 rows per matched-text
 * term, regardless of corpus size.
 */
export const concordanceTaskDispersionBins = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskDispersionBinsData, ThrowOnError>) => (options.client ?? client).get<ConcordanceTaskDispersionBinsResponses, ConcordanceTaskDispersionBinsErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/{task_id}/bins', ...options });

/**
 * Concordance Task Request
 *
 * Return stored request payload for a concordance task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /concordance/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a concordance task" behavior.
 */
export const concordanceTaskRequest = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskRequestData, ThrowOnError>) => (options.client ?? client).get<ConcordanceTaskRequestResponses, ConcordanceTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/{task_id}/request', ...options });

/**
 * Concordance Task Result
 *
 * Read concordance result with optional pagination/sort overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend polling route: `GET /workspaces/{id}/concordance/tasks/{id}/result` because they need this unit's "Read concordance result with optional pagination/sort overrides" behavior.
 *
 * Why:
 * - Hydrates saved concordance state while allowing query-time view changes.
 */
export const concordanceTaskResult = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskResultData, ThrowOnError>) => (options.client ?? client).get<ConcordanceTaskResultResponses, ConcordanceTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/{task_id}/result', ...options });

/**
 * Concordance Task Result Post
 *
 * Read concordance result using POST body overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend state-sync route: because they need this unit's "Read concordance result using POST body overrides" behavior.
 * `POST /workspaces/{id}/concordance/tasks/{id}/result`
 *
 * Why:
 * - Preserves compatibility with clients that send result preferences in body
 * payloads instead of query parameters.
 */
export const concordanceTaskResultPost = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskResultPostData, ThrowOnError>) => (options.client ?? client).post<ConcordanceTaskResultPostResponses, ConcordanceTaskResultPostErrors, ThrowOnError>({
    url: '/api/workspaces/concordance/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Current Workspace
 *
 * Return get current workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /current route because they need this unit's "Return get current workspace API requests for workspace lifecycle routes" behavior.
 */
export const getCurrentWorkspace = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentWorkspaceData, ThrowOnError>) => (options?.client ?? client).get<GetCurrentWorkspaceResponses, GetCurrentWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/current', ...options });

/**
 * Set Current Workspace
 *
 * Set or clear the current in-memory workspace for the user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend workspace selection flow because they need this unit's "Set or clear the current in-memory workspace for the user" behavior.
 *
 * Why:
 * - Ensures subsequent node/analysis operations target the intended workspace.
 */
export const setCurrentWorkspace = <ThrowOnError extends boolean = false>(options?: Options<SetCurrentWorkspaceData, ThrowOnError>) => (options?.client ?? client).post<SetCurrentWorkspaceResponses, SetCurrentWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/current', ...options });

/**
 * Delete Workspace
 *
 * Delete delete workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /delete route because they need this unit's "Delete delete workspace API requests for workspace lifecycle routes" behavior.
 */
export const deleteWorkspace = <ThrowOnError extends boolean = false>(options: Options<DeleteWorkspaceData, ThrowOnError>) => (options.client ?? client).delete<DeleteWorkspaceResponses, DeleteWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/delete', ...options });

/**
 * Update Workspace Description
 *
 * Update update workspace description API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /description route because they need this unit's "Update update workspace description API requests for workspace lifecycle routes" behavior.
 */
export const updateWorkspaceDescription = <ThrowOnError extends boolean = false>(options?: Options<UpdateWorkspaceDescriptionData, ThrowOnError>) => (options?.client ?? client).put<UpdateWorkspaceDescriptionResponses, UpdateWorkspaceDescriptionErrors, ThrowOnError>({ url: '/api/workspaces/description', ...options });

/**
 * Start Workspace Download
 *
 * Start a background task to package the workspace as a ZIP archive.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend Download button in Workspace Manager because they need this unit's "Start a background task to package the workspace as a ZIP archive" behavior.
 *
 * Why:
 * - Moves potentially slow ZIP compression into the Task Center so users can
 * track progress and the UI stays responsive.
 */
export const startWorkspaceDownload = <ThrowOnError extends boolean = false>(options?: Options<StartWorkspaceDownloadData, ThrowOnError>) => (options?.client ?? client).post<StartWorkspaceDownloadResponses, StartWorkspaceDownloadErrors, ThrowOnError>({ url: '/api/workspaces/download', ...options });

/**
 * Download Workspace Artifact
 *
 * Stream a completed workspace ZIP artifact and delete it after download.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend auto-download on task completion because they need this unit's "Stream a completed workspace ZIP artifact and delete it after download" behavior.
 *
 * Why:
 * - One-time artifact policy: the ZIP is deleted after the first successful
 * download to avoid unbounded disk usage.
 */
export const downloadWorkspaceArtifact = <ThrowOnError extends boolean = false>(options: Options<DownloadWorkspaceArtifactData, ThrowOnError>) => (options.client ?? client).get<DownloadWorkspaceArtifactResponses, DownloadWorkspaceArtifactErrors, ThrowOnError>({ url: '/api/workspaces/download/tasks/{task_id}/artifact', ...options });

/**
 * Export Nodes
 *
 * Export one or more workspace nodes as downloadable file(s).
 *
 * If multiple node_ids are provided, a ZIP archive is returned.
 * Supported formats: csv, json, parquet, ipc, ndjson.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /export route because they need this unit's "Export one or more workspace nodes as downloadable file(s)" behavior.
 */
export const exportNodes = <ThrowOnError extends boolean = false>(options: Options<ExportNodesData, ThrowOnError>) => (options.client ?? client).get<ExportNodesResponses, ExportNodesErrors, ThrowOnError>({ url: '/api/workspaces/export', ...options });

/**
 * Get Workspace Graph
 *
 * Return workspace graph payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend graph canvas initialization and refresh because they need this unit's "Return workspace graph payload" behavior.
 */
export const getWorkspaceGraph = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspaceGraphData, ThrowOnError>) => (options?.client ?? client).get<GetWorkspaceGraphResponses, GetWorkspaceGraphErrors, ThrowOnError>({ url: '/api/workspaces/graph', ...options });

/**
 * Get Workspace Info
 *
 * Return get workspace info API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /info route because they need this unit's "Return get workspace info API requests for workspace lifecycle routes" behavior.
 */
export const getWorkspaceInfo = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspaceInfoData, ThrowOnError>) => (options?.client ?? client).get<GetWorkspaceInfoResponses, GetWorkspaceInfoErrors, ThrowOnError>({ url: '/api/workspaces/info', ...options });

/**
 * Rename Workspace
 *
 * Update rename workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /name route because they need this unit's "Update rename workspace API requests for workspace lifecycle routes" behavior.
 */
export const renameWorkspace = <ThrowOnError extends boolean = false>(options: Options<RenameWorkspaceData, ThrowOnError>) => (options.client ?? client).put<RenameWorkspaceResponses, RenameWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/name', ...options });

/**
 * Get Workspace Nodes
 *
 * Return get workspace nodes API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /nodes route because they need this unit's "Return get workspace nodes API requests for workspace lifecycle routes" behavior.
 */
export const getWorkspaceNodes = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspaceNodesData, ThrowOnError>) => (options?.client ?? client).get<GetWorkspaceNodesResponses, GetWorkspaceNodesErrors, ThrowOnError>({ url: '/api/workspaces/nodes', ...options });

/**
 * Add Node To Workspace
 *
 * Add a data file as a new node to workspace.
 *
 * Files are eagerly loaded into a Polars DataFrame, persisted as parquet under the workspace's `data/` folder,
 * and then reloaded as a LazyFrame. This separates bulk data from `metadata.json` while keeping
 * lazy processing semantics.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes route because they need this unit's "Add a data file as a new node to workspace" behavior.
 */
export const addNodeToWorkspace = <ThrowOnError extends boolean = false>(options: Options<AddNodeToWorkspaceData, ThrowOnError>) => (options.client ?? client).post<AddNodeToWorkspaceResponses, AddNodeToWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/nodes', ...options });

/**
 * Concat Nodes
 *
 * Create a new child node by concatenating multiple nodes vertically.
 */
export const concatNodes = <ThrowOnError extends boolean = false>(options: Options<ConcatNodesData, ThrowOnError>) => (options.client ?? client).post<ConcatNodesResponses, ConcatNodesErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/concat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Concat Nodes Preview
 *
 * Preview the result of concatenating multiple nodes vertically.
 */
export const concatNodesPreview = <ThrowOnError extends boolean = false>(options: Options<ConcatNodesPreviewData, ThrowOnError>) => (options.client ?? client).post<ConcatNodesPreviewResponses, ConcatNodesPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/concat/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Join Nodes
 *
 * Join two workspace nodes and persist the result as a new child node.
 */
export const joinNodes = <ThrowOnError extends boolean = false>(options: Options<JoinNodesData, ThrowOnError>) => (options.client ?? client).post<JoinNodesResponses, JoinNodesErrors, ThrowOnError>({ url: '/api/workspaces/nodes/join', ...options });

/**
 * Join Nodes Preview
 *
 * Preview a join between two workspace nodes.
 */
export const joinNodesPreview = <ThrowOnError extends boolean = false>(options: Options<JoinNodesPreviewData, ThrowOnError>) => (options.client ?? client).post<JoinNodesPreviewResponses, JoinNodesPreviewErrors, ThrowOnError>({ url: '/api/workspaces/nodes/join/preview', ...options });

/**
 * Delete Node
 *
 * Delete a node from the workspace.
 */
export const deleteNode = <ThrowOnError extends boolean = false>(options: Options<DeleteNodeData, ThrowOnError>) => (options.client ?? client).delete<DeleteNodeResponses, DeleteNodeErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}', ...options });

/**
 * Get Node Info
 *
 * Return workspace node metadata.
 */
export const getNodeInfo = <ThrowOnError extends boolean = false>(options: Options<GetNodeInfoData, ThrowOnError>) => (options.client ?? client).get<GetNodeInfoResponses, GetNodeInfoErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}', ...options });

/**
 * Get Ai Annotation Categories
 *
 * Return distinct category values from the annotation column of a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /nodes/{node_id}/ai-annotation/categories route because they need this unit's "Return distinct category values from the annotation column of a node" behavior.
 */
export const getAiAnnotationCategories = <ThrowOnError extends boolean = false>(options: Options<GetAiAnnotationCategoriesData, ThrowOnError>) => (options.client ?? client).get<GetAiAnnotationCategoriesResponses, GetAiAnnotationCategoriesErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/ai-annotation/categories', ...options });

/**
 * Detach Ai Annotation
 *
 * Run AI annotation on a node and detach the result as a new child node.
 *
 * Classifies texts using the OpenAI-compatible endpoint, writes the result
 * as a parquet file, and adds it as a new node in the workspace.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/ai-annotation/detach route because they need this unit's "Run AI annotation on a node and detach the result as a new child node" behavior.
 */
export const detachAiAnnotation = <ThrowOnError extends boolean = false>(options: Options<DetachAiAnnotationData, ThrowOnError>) => (options.client ?? client).post<DetachAiAnnotationResponses, DetachAiAnnotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/ai-annotation/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Ai Annotation Providers
 *
 * Return distinct provider values from the annotation column of a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /nodes/{node_id}/ai-annotation/providers route because they need this unit's "Return distinct provider values from the annotation column of a node" behavior.
 */
export const getAiAnnotationProviders = <ThrowOnError extends boolean = false>(options: Options<GetAiAnnotationProvidersData, ThrowOnError>) => (options.client ?? client).get<GetAiAnnotationProvidersResponses, GetAiAnnotationProvidersErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/ai-annotation/providers', ...options });

/**
 * Save Ai Annotation
 *
 * Save AI annotation edits back to the source node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/ai-annotation/save route because they need this unit's "Save AI annotation edits back to the source node" behavior.
 */
export const saveAiAnnotation = <ThrowOnError extends boolean = false>(options: Options<SaveAiAnnotationData, ThrowOnError>) => (options.client ?? client).post<SaveAiAnnotationResponses, SaveAiAnnotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/ai-annotation/save',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cast Node
 *
 * Cast a single column data type in a node using Polars casting methods (in-place operation).
 *
 * Args:
 * workspace_id: The workspace identifier
 * node_id: The node identifier to cast
 * cast_data: Dictionary with casting specifications:
 * - column: str - name of the column to cast
 * - target_type: str - target data type (e.g., "integer", "float", "string", "datetime", "boolean", "categorical")
 * - format: str (optional) - datetime format string for string to datetime conversion
 * Example: {"column": "date_col", "target_type": "datetime", "format": "%Y-%m-%d"}
 *
 * Returns:
 * Dictionary with the updated node information after casting
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/cast route because they need this unit's "Cast a single column data type in a node using Polars casting methods (in-place operation)" behavior.
 */
export const castNode = <ThrowOnError extends boolean = false>(options: Options<CastNodeData, ThrowOnError>) => (options.client ?? client).post<CastNodeResponses, CastNodeErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/cast',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clone Node
 *
 * Clone a workspace node (deep-copy its LazyFrame).
 */
export const cloneNode = <ThrowOnError extends boolean = false>(options: Options<CloneNodeData, ThrowOnError>) => (options.client ?? client).post<CloneNodeResponses, CloneNodeErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/clone', ...options });

/**
 * Delete Node Column
 *
 * Delete a column from a node by delegating to DocWorkspace Node.drop.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /nodes/{node_id}/columns/{column_name} route because they need this unit's "Delete a column from a node by delegating to DocWorkspace Node.drop" behavior.
 */
export const deleteNodeColumn = <ThrowOnError extends boolean = false>(options: Options<DeleteNodeColumnData, ThrowOnError>) => (options.client ?? client).delete<DeleteNodeColumnResponses, DeleteNodeColumnErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}', ...options });

/**
 * Rename Node Column
 *
 * Rename a column by delegating to DocWorkspace Node.rename.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /nodes/{node_id}/columns/{column_name} route because they need this unit's "Rename a column by delegating to DocWorkspace Node.rename" behavior.
 */
export const renameNodeColumn = <ThrowOnError extends boolean = false>(options: Options<RenameNodeColumnData, ThrowOnError>) => (options.client ?? client).put<RenameNodeColumnResponses, RenameNodeColumnErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/columns/{column_name}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Describe Column
 *
 * Get descriptive statistics for a column using Polars describe.
 */
export const describeColumn = <ThrowOnError extends boolean = false>(options: Options<DescribeColumnData, ThrowOnError>) => (options.client ?? client).get<DescribeColumnResponses, DescribeColumnErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}/describe', ...options });

/**
 * Column Operations
 *
 * Return available no-arg Polars operations for a column, filtered by dtype.
 */
export const columnOperations = <ThrowOnError extends boolean = false>(options: Options<ColumnOperationsData, ThrowOnError>) => (options.client ?? client).get<ColumnOperationsResponses, ColumnOperationsErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}/operations', ...options });

/**
 * Get Column Unique Values
 *
 * Return unique values for a column.
 */
export const getColumnUniqueValues = <ThrowOnError extends boolean = false>(options: Options<GetColumnUniqueValuesData, ThrowOnError>) => (options.client ?? client).get<GetColumnUniqueValuesResponses, GetColumnUniqueValuesErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}/unique', ...options });

/**
 * Detach Concordance
 *
 * Submit a background task to create a concordance-detached node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend detach action: because they need this unit's "Submit a background task to create a concordance-detached node" behavior.
 * `POST /workspaces/{id}/nodes/{node_id}/concordance/detach`
 *
 * Why:
 * - Runs potentially expensive row extraction out-of-band and returns task id
 * for progress tracking.
 */
export const detachConcordance = <ThrowOnError extends boolean = false>(options: Options<DetachConcordanceData, ThrowOnError>) => (options.client ?? client).post<DetachConcordanceResponses, DetachConcordanceErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/concordance/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Concordance Detach Options
 *
 * Return detachable concordance columns for one node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend concordance detach dialog because they need this unit's "Return detachable concordance columns for one node" behavior.
 *
 * Why:
 * - Keeps mandatory generated concordance columns and optional metadata
 * columns aligned with backend detach behavior.
 */
export const concordanceDetachOptions = <ThrowOnError extends boolean = false>(options: Options<ConcordanceDetachOptionsData, ThrowOnError>) => (options.client ?? client).get<ConcordanceDetachOptionsResponses, ConcordanceDetachOptionsErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/concordance/detach-options', ...options });

/**
 * Detach Concordance Dispersion
 *
 * Submit a per-document aggregated detach (dispersion view).
 *
 * Output shape differs from the per-hit detach: one row per source document,
 * matched-text/L1/R1/freq columns become `List<T>`, plus a `CONC_extraction`
 * string column that joins each hit's character slice with newline + asterisk
 * bullets in document-flow order. Used by the dispersion summary chart so the
 * user can pull a per-document view (optionally limited to bin-filtered hits)
 * into the workspace as a new data block.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/concordance/dispersion-detach route because they need this unit's "Submit a per-document aggregated detach (dispersion view)" behavior.
 */
export const detachConcordanceDispersion = <ThrowOnError extends boolean = false>(options: Options<DetachConcordanceDispersionData, ThrowOnError>) => (options.client ?? client).post<DetachConcordanceDispersionResponses, DetachConcordanceDispersionErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/concordance/dispersion-detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Materialize Concordance
 *
 * Submit a background task that writes the full flattened occurrence parquet.
 *
 * Unlike detach, this does not add a node to the workspace. On completion the
 * parent concordance analysis task's `materialized_paths` is updated so
 * subsequent pagination and detach reuse the cached parquet.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/concordance/materialize route because they need this unit's "Submit a background task that writes the full flattened occurrence parquet" behavior.
 */
export const materializeConcordance = <ThrowOnError extends boolean = false>(options: Options<MaterializeConcordanceData, ThrowOnError>) => (options.client ?? client).post<MaterializeConcordanceResponses, MaterializeConcordanceErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/concordance/materialize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Node Data
 *
 * Return paginated node data rows.
 */
export const getNodeData = <ThrowOnError extends boolean = false>(options: Options<GetNodeDataData, ThrowOnError>) => (options.client ?? client).get<GetNodeDataResponses, GetNodeDataErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/data', ...options });

/**
 * Set Node Document Column
 *
 * Set or clear the document column on a workspace node.
 */
export const setNodeDocumentColumn = <ThrowOnError extends boolean = false>(options: Options<SetNodeDocumentColumnData, ThrowOnError>) => (options.client ?? client).put<SetNodeDocumentColumnResponses, SetNodeDocumentColumnErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/document-column',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Polars Expression Apply
 *
 * Apply a Polars expression and persist the result.
 */
export const polarsExpressionApply = <ThrowOnError extends boolean = false>(options: Options<PolarsExpressionApplyData, ThrowOnError>) => (options.client ?? client).post<PolarsExpressionApplyResponses, PolarsExpressionApplyErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/expression/apply',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Polars Expression Preview
 *
 * Preview a Polars expression applied to the source node.
 */
export const polarsExpressionPreview = <ThrowOnError extends boolean = false>(options: Options<PolarsExpressionPreviewData, ThrowOnError>) => (options.client ?? client).post<PolarsExpressionPreviewResponses, PolarsExpressionPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/expression/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Filter Node
 *
 * Create a new child node by filtering the source node's data.
 */
export const filterNode = <ThrowOnError extends boolean = false>(options: Options<FilterNodeData, ThrowOnError>) => (options.client ?? client).post<FilterNodeResponses, FilterNodeErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/filter',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Filter Preview
 *
 * Preview the result of a filter operation on the source node.
 */
export const filterPreview = <ThrowOnError extends boolean = false>(options: Options<FilterPreviewData, ThrowOnError>) => (options.client ?? client).post<FilterPreviewResponses, FilterPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/filter/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Node Name
 *
 * Rename a workspace node.
 */
export const updateNodeName = <ThrowOnError extends boolean = false>(options: Options<UpdateNodeNameData, ThrowOnError>) => (options.client ?? client).put<UpdateNodeNameResponses, UpdateNodeNameErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/name', ...options });

/**
 * Get Node Query Plan
 *
 * Return the Polars query plan for a workspace node.
 */
export const getNodeQueryPlan = <ThrowOnError extends boolean = false>(options: Options<GetNodeQueryPlanData, ThrowOnError>) => (options.client ?? client).get<GetNodeQueryPlanResponses, GetNodeQueryPlanErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/query-plan', ...options });

/**
 * Get Quotation
 *
 * Run quotation extraction on selected node and store latest task payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend quotation run/search action because they need this unit's "Run quotation extraction on selected node and store latest task payload" behavior.
 *
 * Why:
 * - Produces immediate result payload and persists it as current quotation task.
 */
export const getQuotation = <ThrowOnError extends boolean = false>(options: Options<GetQuotationData, ThrowOnError>) => (options.client ?? client).post<GetQuotationResponses, GetQuotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/quotation',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Detach Quotation
 *
 * Submit background task to detach quotations into a new workspace node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend quotation detach action because they need this unit's "Submit background task to detach quotations into a new workspace node" behavior.
 *
 * Why:
 * - Offloads potentially expensive extraction/materialization to worker tasks.
 */
export const detachQuotation = <ThrowOnError extends boolean = false>(options: Options<DetachQuotationData, ThrowOnError>) => (options.client ?? client).post<DetachQuotationResponses, DetachQuotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/quotation/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Quotation Detach Options
 *
 * Return detachable quotation columns for one node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend quotation detach dialog because they need this unit's "Return detachable quotation columns for one node" behavior.
 *
 * Why:
 * - Keeps mandatory generated quotation columns and optional source columns
 * aligned with backend detach behavior.
 */
export const quotationDetachOptions = <ThrowOnError extends boolean = false>(options: Options<QuotationDetachOptionsData, ThrowOnError>) => (options.client ?? client).get<QuotationDetachOptionsResponses, QuotationDetachOptionsErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/quotation/detach-options', ...options });

/**
 * Materialize Quotation
 *
 * Submit a background task that writes the full flattened quotation parquet.
 *
 * Unlike detach, this does not add a node to the workspace. On completion the
 * parent quotation analysis task's `materialized_path` is updated so subsequent
 * pagination and detach reuse the cached parquet.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/quotation/materialize route because they need this unit's "Submit a background task that writes the full flattened quotation parquet" behavior.
 */
export const materializeQuotation = <ThrowOnError extends boolean = false>(options: Options<MaterializeQuotationData, ThrowOnError>) => (options.client ?? client).post<MaterializeQuotationResponses, MaterializeQuotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/quotation/materialize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redo Node Operation
 *
 * Redo the latest undone in-memory execution plan change for a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/redo route because they need this unit's "Redo the latest undone in-memory execution plan change for a node" behavior.
 */
export const redoNodeOperation = <ThrowOnError extends boolean = false>(options: Options<RedoNodeOperationData, ThrowOnError>) => (options.client ?? client).post<RedoNodeOperationResponses, RedoNodeOperationErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/redo', ...options });

/**
 * Replace Apply
 *
 * Apply a regex replace operation and persist the updated node.
 */
export const replaceApply = <ThrowOnError extends boolean = false>(options: Options<ReplaceApplyData, ThrowOnError>) => (options.client ?? client).post<ReplaceApplyResponses, ReplaceApplyErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/replace',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Replace Preview
 *
 * Preview a regex replace operation on the source node.
 */
export const replacePreview = <ThrowOnError extends boolean = false>(options: Options<ReplacePreviewData, ThrowOnError>) => (options.client ?? client).post<ReplacePreviewResponses, ReplacePreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/replace/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Run Sequential Analysis
 *
 * Run sequential analysis for one node and persist/update task payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend sequential-analysis run action because they need this unit's "Run sequential analysis for one node and persist/update task payload" behavior.
 *
 * Why:
 * - Produces aggregated time-series counts and stores them as current task data.
 */
export const runSequentialAnalysis = <ThrowOnError extends boolean = false>(options: Options<RunSequentialAnalysisData, ThrowOnError>) => (options.client ?? client).post<RunSequentialAnalysisResponses, RunSequentialAnalysisErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/sequential-analysis',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Preview Sequential Analysis
 *
 * Run the aggregation server-side but skip task registration and
 * conflict checks.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend preview/count checks because they need row-count feedback without registering
 * or replacing the live analysis task.
 * - callers that pass ``include_data=true`` because they need the same aggregated rows the
 * regular endpoint produces without mutating task state.
 *
 * Why a separate endpoint rather than a flag on the existing one:
 * the existing endpoint is task-aware (rejects conflicting requests,
 * inherits chart_type, writes the result into the task store). All
 * of that machinery is wrong for a pure preview query.
 */
export const previewSequentialAnalysis = <ThrowOnError extends boolean = false>(options: Options<PreviewSequentialAnalysisData, ThrowOnError>) => (options.client ?? client).post<PreviewSequentialAnalysisResponses, PreviewSequentialAnalysisErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/sequential-analysis/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Node Shape
 *
 * Return the row x column shape of a workspace node.
 */
export const getNodeShape = <ThrowOnError extends boolean = false>(options: Options<GetNodeShapeData, ThrowOnError>) => (options.client ?? client).get<GetNodeShapeResponses, GetNodeShapeErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/shape', ...options });

/**
 * Slice Node
 *
 * Create a new child node by slicing/sampling/shuffling the source.
 */
export const sliceNode = <ThrowOnError extends boolean = false>(options: Options<SliceNodeData, ThrowOnError>) => (options.client ?? client).post<SliceNodeResponses, SliceNodeErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/slice',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Slice Preview
 *
 * Preview the result of a slice/sample/shuffle on the source node.
 */
export const slicePreview = <ThrowOnError extends boolean = false>(options: Options<SlicePreviewData, ThrowOnError>) => (options.client ?? client).post<SlicePreviewResponses, SlicePreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/slice/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set Node Tokenization Preference
 *
 * Set or clear a tokenization preference on a workspace node.
 */
export const setNodeTokenizationPreference = <ThrowOnError extends boolean = false>(options: Options<SetNodeTokenizationPreferenceData, ThrowOnError>) => (options.client ?? client).put<SetNodeTokenizationPreferenceResponses, SetNodeTokenizationPreferenceErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/tokenization-preference',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Undo Node Operation
 *
 * Undo the latest in-memory execution plan change for a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/undo route because they need this unit's "Undo the latest in-memory execution plan change for a node" behavior.
 */
export const undoNodeOperation = <ThrowOnError extends boolean = false>(options: Options<UndoNodeOperationData, ThrowOnError>) => (options.client ?? client).post<UndoNodeOperationResponses, UndoNodeOperationErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/undo', ...options });

/**
 * Quotation Current Tasks
 *
 * Return current task IDs for quotation analysis.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /quotation/tasks/current route because they need this unit's "Return current task IDs for quotation analysis" behavior.
 */
export const quotationCurrentTasks = <ThrowOnError extends boolean = false>(options?: Options<QuotationCurrentTasksData, ThrowOnError>) => (options?.client ?? client).get<QuotationCurrentTasksResponses, QuotationCurrentTasksErrors, ThrowOnError>({ url: '/api/workspaces/quotation/tasks/current', ...options });

/**
 * Quotation Task Request
 *
 * Return stored request payload for a quotation task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /quotation/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a quotation task" behavior.
 */
export const quotationTaskRequest = <ThrowOnError extends boolean = false>(options: Options<QuotationTaskRequestData, ThrowOnError>) => (options.client ?? client).get<QuotationTaskRequestResponses, QuotationTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/quotation/tasks/{task_id}/request', ...options });

/**
 * Quotation Task Result
 *
 * Return stored quotation result, optionally recomputed for new page params.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend polling route for quotation result panels because they need this unit's "Return stored quotation result, optionally recomputed for new page params" behavior.
 *
 * Why:
 * - Supports cheap preference-only reads and on-demand page recomputation.
 */
export const quotationTaskResult = <ThrowOnError extends boolean = false>(options: Options<QuotationTaskResultData, ThrowOnError>) => (options.client ?? client).get<QuotationTaskResultResponses, QuotationTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/quotation/tasks/{task_id}/result', ...options });

/**
 * Update Quotation Task Result
 *
 * Persist quotation display preferences and optional page overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend preference updates for context length/sort/page controls because they need this unit's "Persist quotation display preferences and optional page overrides" behavior.
 *
 * Why:
 * - Lets UI tune quotation presentation without rerunning analysis creation.
 *
 * Refactor note:
 * - Shares substantial logic with `quotation_task_result`; both could delegate
 * to a single internal read/update orchestrator.
 */
export const updateQuotationTaskResult = <ThrowOnError extends boolean = false>(options: Options<UpdateQuotationTaskResultData, ThrowOnError>) => (options.client ?? client).post<UpdateQuotationTaskResultResponses, UpdateQuotationTaskResultErrors, ThrowOnError>({
    url: '/api/workspaces/quotation/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Save Workspace
 *
 * Handle save workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /save route because they need this unit's "Handle save workspace API requests for workspace lifecycle routes" behavior.
 */
export const saveWorkspace = <ThrowOnError extends boolean = false>(options?: Options<SaveWorkspaceData, ThrowOnError>) => (options?.client ?? client).post<SaveWorkspaceResponses, SaveWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/save', ...options });

/**
 * Sequential Analysis Current Tasks
 *
 * Return current task IDs for sequential-analysis.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sequential-analysis/tasks/current route because they need this unit's "Return current task IDs for sequential-analysis" behavior.
 */
export const sequentialAnalysisCurrentTasks = <ThrowOnError extends boolean = false>(options?: Options<SequentialAnalysisCurrentTasksData, ThrowOnError>) => (options?.client ?? client).get<SequentialAnalysisCurrentTasksResponses, SequentialAnalysisCurrentTasksErrors, ThrowOnError>({ url: '/api/workspaces/sequential-analysis/tasks/current', ...options });

/**
 * Detach Sequential Analysis Task
 *
 * Create a filtered child node from selected sequential-analysis periods.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /sequential-analysis/tasks/{task_id}/detach route because they need this unit's "Create a filtered child node from selected sequential-analysis periods" behavior.
 */
export const detachSequentialAnalysisTask = <ThrowOnError extends boolean = false>(options: Options<DetachSequentialAnalysisTaskData, ThrowOnError>) => (options.client ?? client).post<DetachSequentialAnalysisTaskResponses, DetachSequentialAnalysisTaskErrors, ThrowOnError>({
    url: '/api/workspaces/sequential-analysis/tasks/{task_id}/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Sequential Analysis Task Request
 *
 * Return stored request payload for a sequential-analysis task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sequential-analysis/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a sequential-analysis task" behavior.
 */
export const sequentialAnalysisTaskRequest = <ThrowOnError extends boolean = false>(options: Options<SequentialAnalysisTaskRequestData, ThrowOnError>) => (options.client ?? client).get<SequentialAnalysisTaskRequestResponses, SequentialAnalysisTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/sequential-analysis/tasks/{task_id}/request', ...options });

/**
 * Sequential Analysis Task Result
 *
 * Return stored result payload for a sequential-analysis task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sequential-analysis/tasks/{task_id}/result route because they need this unit's "Return stored result payload for a sequential-analysis task" behavior.
 */
export const sequentialAnalysisTaskResult = <ThrowOnError extends boolean = false>(options: Options<SequentialAnalysisTaskResultData, ThrowOnError>) => (options.client ?? client).get<SequentialAnalysisTaskResultResponses, SequentialAnalysisTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/sequential-analysis/tasks/{task_id}/result', ...options });

/**
 * Update Sequential Analysis Task Result
 *
 * Persist display-only sequential analysis options on a saved task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend chart-type preference updates because they need this unit's "Persist display-only sequential analysis options on a saved task" behavior.
 *
 * Why:
 * - Avoids recomputation when only chart presentation changes.
 *
 * Refactor note:
 * - Mirrors preference update behavior in other analyses; a shared
 * task-preferences helper could reduce duplication.
 */
export const updateSequentialAnalysisTaskResult = <ThrowOnError extends boolean = false>(options: Options<UpdateSequentialAnalysisTaskResultData, ThrowOnError>) => (options.client ?? client).post<UpdateSequentialAnalysisTaskResultResponses, UpdateSequentialAnalysisTaskResultErrors, ThrowOnError>({
    url: '/api/workspaces/sequential-analysis/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clear Token Frequencies
 *
 * Clear Token Frequency analysis state for a workspace.
 *
 * Mirrors topic-modeling clear behavior by removing the currently tracked
 * analysis task link for the token frequency tab.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /token-frequencies route because they need this unit's "Clear Token Frequency analysis state for a workspace" behavior.
 */
export const clearTokenFrequencies = <ThrowOnError extends boolean = false>(options?: Options<ClearTokenFrequenciesData, ThrowOnError>) => (options?.client ?? client).delete<ClearTokenFrequenciesResponses, ClearTokenFrequenciesErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies', ...options });

/**
 * Calculate token frequencies for selected nodes
 *
 * Calculate and compare token frequencies across one or two nodes using polars-text
 */
export const calculateTokenFrequencies = <ThrowOnError extends boolean = false>(options: Options<CalculateTokenFrequenciesData, ThrowOnError>) => (options.client ?? client).post<CalculateTokenFrequenciesResponses, CalculateTokenFrequenciesErrors, ThrowOnError>({
    url: '/api/workspaces/token-frequencies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Token Frequencies Current Tasks
 *
 * Return current task IDs for token-frequencies analysis.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /token-frequencies/tasks/current route because they need this unit's "Return current task IDs for token-frequencies analysis" behavior.
 */
export const tokenFrequenciesCurrentTasks = <ThrowOnError extends boolean = false>(options?: Options<TokenFrequenciesCurrentTasksData, ThrowOnError>) => (options?.client ?? client).get<TokenFrequenciesCurrentTasksResponses, TokenFrequenciesCurrentTasksErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies/tasks/current', ...options });

/**
 * Token Frequencies Task Request
 *
 * Return stored request payload for a token-frequencies task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /token-frequencies/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a token-frequencies task" behavior.
 */
export const tokenFrequenciesTaskRequest = <ThrowOnError extends boolean = false>(options: Options<TokenFrequenciesTaskRequestData, ThrowOnError>) => (options.client ?? client).get<TokenFrequenciesTaskRequestResponses, TokenFrequenciesTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies/tasks/{task_id}/request', ...options });

/**
 * Token Frequencies Task Result
 *
 * Return normalized token-frequency result payload for one task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /token-frequencies/tasks/{task_id}/result route because they need this unit's "Return normalized token-frequency result payload for one task" behavior.
 */
export const tokenFrequenciesTaskResult = <ThrowOnError extends boolean = false>(options: Options<TokenFrequenciesTaskResultData, ThrowOnError>) => (options.client ?? client).get<TokenFrequenciesTaskResultResponses, TokenFrequenciesTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies/tasks/{task_id}/result', ...options });

/**
 * Update Token Frequencies Task Result
 *
 * Persist token-frequency preference overrides on an existing task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /token-frequencies/tasks/{task_id}/result route because they need this unit's "Persist token-frequency preference overrides on an existing task" behavior.
 */
export const updateTokenFrequenciesTaskResult = <ThrowOnError extends boolean = false>(options: Options<UpdateTokenFrequenciesTaskResultData, ThrowOnError>) => (options.client ?? client).post<UpdateTokenFrequenciesTaskResultResponses, UpdateTokenFrequenciesTaskResultErrors, ThrowOnError>({
    url: '/api/workspaces/token-frequencies/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Tokenizer Models
 *
 * Return available tokenizer model records.
 */
export const getTokenizerModels = <ThrowOnError extends boolean = false>(options?: Options<GetTokenizerModelsData, ThrowOnError>) => (options?.client ?? client).get<GetTokenizerModelsResponses, GetTokenizerModelsErrors, ThrowOnError>({ url: '/api/workspaces/tokenizer-models', ...options });

/**
 * Clear Topic Modeling Results
 *
 * Clear stored topic-modeling task state for a workspace.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend clear action: `DELETE /workspaces/{id}/topic-modeling` because they need this unit's "Clear stored topic-modeling task state for a workspace" behavior.
 *
 * Why:
 * - Removes stale result/task pointers before reruns and keeps UI state
 * aligned with backend task registries.
 */
export const clearTopicModelingResults = <ThrowOnError extends boolean = false>(options?: Options<ClearTopicModelingResultsData, ThrowOnError>) => (options?.client ?? client).delete<ClearTopicModelingResultsResponses, ClearTopicModelingResultsErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling', ...options });

/**
 * Run Topic Modeling
 *
 * Submit topic-modeling analysis as a worker-backed background task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend run route: `POST /workspaces/{id}/topic-modeling` because they need this unit's "Submit topic-modeling analysis as a worker-backed background task" behavior.
 *
 * Why:
 * - Offloads heavy modeling work to worker processes and returns `task_id`
 * for progress/result polling.
 */
export const runTopicModeling = <ThrowOnError extends boolean = false>(options: Options<RunTopicModelingData, ThrowOnError>) => (options.client ?? client).post<RunTopicModelingResponses, RunTopicModelingErrors, ThrowOnError>({
    url: '/api/workspaces/topic-modeling',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clear Topic Modeling Embedding Cache
 *
 * Delete every DuckDB entry in the user's embedding cache.
 *
 * Sweeps the canonical `user_cache/embeddings/` directory.
 * Returns total bytes and file count freed so the UI can confirm the
 * reclaim. Clearing forces the next topic-modelling run to re-encode all
 * documents from scratch.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /topic-modeling/embedding-cache route because they need this unit's "Delete every DuckDB entry in the user's embedding cache" behavior.
 */
export const clearTopicModelingEmbeddingCache = <ThrowOnError extends boolean = false>(options?: Options<ClearTopicModelingEmbeddingCacheData, ThrowOnError>) => (options?.client ?? client).delete<ClearTopicModelingEmbeddingCacheResponses, ClearTopicModelingEmbeddingCacheErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/embedding-cache', ...options });

/**
 * Get Topic Modeling Embedding Cache Size
 *
 * Report current embedding-cache size and file count for the user.
 *
 * Used by the frontend Clear-cache confirmation dialog so the user can see
 * "X MB will be freed" before they confirm.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /topic-modeling/embedding-cache/size route because they need this unit's "Report current embedding-cache size and file count for the user" behavior.
 */
export const getTopicModelingEmbeddingCacheSize = <ThrowOnError extends boolean = false>(options?: Options<GetTopicModelingEmbeddingCacheSizeData, ThrowOnError>) => (options?.client ?? client).get<GetTopicModelingEmbeddingCacheSizeResponses, GetTopicModelingEmbeddingCacheSizeErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/embedding-cache/size', ...options });

/**
 * Topic Modeling Current Tasks
 *
 * Return current task IDs for topic-modeling analysis.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /topic-modeling/tasks/current route because they need this unit's "Return current task IDs for topic-modeling analysis" behavior.
 */
export const topicModelingCurrentTasks = <ThrowOnError extends boolean = false>(options?: Options<TopicModelingCurrentTasksData, ThrowOnError>) => (options?.client ?? client).get<TopicModelingCurrentTasksResponses, TopicModelingCurrentTasksErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/current', ...options });

/**
 * Detach Topic Modeling
 *
 * Create detached nodes from artifact-backed topic-modeling outputs.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend detach route: because they need this unit's "Create detached nodes from artifact-backed topic-modeling outputs" behavior.
 * `POST /workspaces/{id}/topic-modeling/tasks/{task_id}/detach`
 *
 * Why:
 * - Materializes user-selected columns and topic labels as reusable workspace
 * nodes without rerunning the model.
 */
export const detachTopicModeling = <ThrowOnError extends boolean = false>(options: Options<DetachTopicModelingData, ThrowOnError>) => (options.client ?? client).post<DetachTopicModelingResponses, DetachTopicModelingErrors, ThrowOnError>({
    url: '/api/workspaces/topic-modeling/tasks/{task_id}/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Topic Modeling Detach Options
 *
 * List detachable node/column options for a completed topic task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend detach-options route: because they need this unit's "List detachable node/column options for a completed topic task" behavior.
 * `GET /workspaces/{id}/topic-modeling/tasks/{task_id}/detach-options`
 *
 * Why:
 * - Exposes artifact-backed node metadata so users can choose output columns safely.
 */
export const topicModelingDetachOptions = <ThrowOnError extends boolean = false>(options: Options<TopicModelingDetachOptionsData, ThrowOnError>) => (options.client ?? client).get<TopicModelingDetachOptionsResponses, TopicModelingDetachOptionsErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/{task_id}/detach-options', ...options });

/**
 * Topic Modeling Task Request
 *
 * Return stored request payload for a topic-modeling task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /topic-modeling/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a topic-modeling task" behavior.
 */
export const topicModelingTaskRequest = <ThrowOnError extends boolean = false>(options: Options<TopicModelingTaskRequestData, ThrowOnError>) => (options.client ?? client).get<TopicModelingTaskRequestResponses, TopicModelingTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/{task_id}/request', ...options });

/**
 * Topic Modeling Task Result
 *
 * Return current status or final payload for a topic-modeling task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend polling route: because they need this unit's "Return current status or final payload for a topic-modeling task" behavior.
 * `GET /workspaces/{id}/topic-modeling/tasks/{task_id}/result`
 *
 * Why:
 * - Normalizes task lifecycle states into one response contract for UI polling.
 */
export const topicModelingTaskResult = <ThrowOnError extends boolean = false>(options: Options<TopicModelingTaskResultData, ThrowOnError>) => (options.client ?? client).get<TopicModelingTaskResultResponses, TopicModelingTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/{task_id}/result', ...options });

/**
 * Unload Workspace
 *
 * Handle unload workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /unload route because they need this unit's "Handle unload workspace API requests for workspace lifecycle routes" behavior.
 */
export const unloadWorkspace = <ThrowOnError extends boolean = false>(options?: Options<UnloadWorkspaceData, ThrowOnError>) => (options?.client ?? client).post<UnloadWorkspaceResponses, UnloadWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/unload', ...options });

/**
 * Upload Workspace Zip
 *
 * Upload a workspace ZIP archive and import it into user workspace storage.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /upload route because they need this unit's "Upload a workspace ZIP archive and import it into user workspace storage" behavior.
 */
export const uploadWorkspaceZip = <ThrowOnError extends boolean = false>(options: Options<UploadWorkspaceZipData, ThrowOnError>) => (options.client ?? client).post<UploadWorkspaceZipResponses, UploadWorkspaceZipErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/api/workspaces/upload',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Get Workspace Tabs
 *
 * Return the persisted analysis-tab state for a workspace.
 *
 * Used by the frontend tab store on view entry to restore tabs (and their
 * task ids) after a reload. Returns the empty default when no sidecar exists.
 */
export const getWorkspaceTabs = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceTabsData, ThrowOnError>) => (options.client ?? client).get<GetWorkspaceTabsResponses, GetWorkspaceTabsErrors, ThrowOnError>({ url: '/api/workspaces/{workspace_id}/tabs', ...options });

/**
 * Put Workspace Tabs
 *
 * Replace the persisted analysis-tab state for a workspace.
 *
 * Used by the frontend tab store whenever tabs are created, closed, renamed,
 * reordered, activated, or wired to a new task id. Full-replacement semantics
 * mirror the ``ui_state.json`` sidecar.
 */
export const putWorkspaceTabs = <ThrowOnError extends boolean = false>(options: Options<PutWorkspaceTabsData, ThrowOnError>) => (options.client ?? client).put<PutWorkspaceTabsResponses, PutWorkspaceTabsErrors, ThrowOnError>({
    url: '/api/workspaces/{workspace_id}/tabs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Workspace Ui State
 *
 * Return get workspace ui state API requests for workspace UI-state routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /{workspace_id}/ui-state route because they need this unit's "Return get workspace ui state API requests for workspace UI-state routes" behavior.
 */
export const getWorkspaceUiState = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceUiStateData, ThrowOnError>) => (options.client ?? client).get<GetWorkspaceUiStateResponses, GetWorkspaceUiStateErrors, ThrowOnError>({ url: '/api/workspaces/{workspace_id}/ui-state', ...options });

/**
 * Put Workspace Ui State
 *
 * Update put workspace ui state API requests for workspace UI-state routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /{workspace_id}/ui-state route because they need this unit's "Update put workspace ui state API requests for workspace UI-state routes" behavior.
 */
export const putWorkspaceUiState = <ThrowOnError extends boolean = false>(options: Options<PutWorkspaceUiStateData, ThrowOnError>) => (options.client ?? client).put<PutWorkspaceUiStateResponses, PutWorkspaceUiStateErrors, ThrowOnError>({
    url: '/api/workspaces/{workspace_id}/ui-state',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Health Check
 *
 * Return lightweight health status for liveness probes.
 *
 * Used by:
 * - deployment health checks and uptime monitors because callers need the shared shared
 * backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Gives a fast no-auth probe endpoint for runtime readiness checks.
 */
export const healthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthCheckData, ThrowOnError>) => (options?.client ?? client).get<HealthCheckResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * Status
 *
 * Return detailed component/module status information.
 *
 * Used by:
 * - diagnostics pages and manual troubleshooting because callers need the shared shared
 * backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Exposes richer operational metadata than ``/health``.
 */
export const status = <ThrowOnError extends boolean = false>(options?: Options<StatusData, ThrowOnError>) => (options?.client ?? client).get<StatusResponses, unknown, ThrowOnError>({ url: '/status', ...options });
