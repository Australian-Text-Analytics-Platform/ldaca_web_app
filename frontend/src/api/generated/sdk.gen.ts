// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type ClientMeta, formDataBodySerializer, type Options as Options2, type RequestResult, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { AddNodeToWorkspaceData, AddNodeToWorkspaceErrors, AddNodeToWorkspaceResponses, AdminCleanupData, AdminCleanupErrors, AdminCleanupResponses, AnnotateAiAllData, AnnotateAiAllErrors, AnnotateAiAllResponses, AnnotateAiPreviewClearData, AnnotateAiPreviewClearErrors, AnnotateAiPreviewClearResponses, AnnotateAiPreviewData, AnnotateAiPreviewErrors, AnnotateAiPreviewOverrideData, AnnotateAiPreviewOverrideErrors, AnnotateAiPreviewOverrideResponses, AnnotateAiPreviewResponses, AnnotateAiPreviewStateData, AnnotateAiPreviewStateErrors, AnnotateAiPreviewStateResponses, AuthHealthData, AuthHealthResponses, AuthStatusData, AuthStatusErrors, AuthStatusResponses, CalculateTokenFrequenciesData, CalculateTokenFrequenciesErrors, CalculateTokenFrequenciesResponses, CancelTaskData, CancelTaskErrors, CancelTaskResponses, CastNodeData, CastNodeErrors, CastNodeResponses, CilogonCallbackData, CilogonCallbackErrors, CilogonCallbackResponses, CilogonLoginData, CilogonLoginResponses, ClearFilesTasksData, ClearFilesTasksErrors, ClearFilesTasksResponses, ClearTasksData, ClearTasksErrors, ClearTasksResponses, ClearTokenFrequenciesData, ClearTokenFrequenciesErrors, ClearTokenFrequenciesResponses, ClearTopicModelingResultsData, ClearTopicModelingResultsErrors, ClearTopicModelingResultsResponses, CloneNodeData, CloneNodeErrors, CloneNodeResponses, ColumnOperationsData, ColumnOperationsErrors, ColumnOperationsResponses, ConcatNodesData, ConcatNodesErrors, ConcatNodesPreviewData, ConcatNodesPreviewErrors, ConcatNodesPreviewResponses, ConcatNodesResponses, ConcordanceDetachOptionsData, ConcordanceDetachOptionsErrors, ConcordanceDetachOptionsResponses, ConcordanceTaskDispersionBinsData, ConcordanceTaskDispersionBinsErrors, ConcordanceTaskDispersionBinsResponses, ConcordanceTaskRequestData, ConcordanceTaskRequestErrors, ConcordanceTaskRequestResponses, ConcordanceTaskResultData, ConcordanceTaskResultErrors, ConcordanceTaskResultPostData, ConcordanceTaskResultPostErrors, ConcordanceTaskResultPostResponses, ConcordanceTaskResultResponses, CreateAnnotationClassDescriptionsData, CreateAnnotationClassDescriptionsErrors, CreateAnnotationClassDescriptionsResponses, CreateAnnotationColumnData, CreateAnnotationColumnErrors, CreateAnnotationColumnResponses, CreateFolderData, CreateFolderErrors, CreateFolderResponses, CreateWorkspaceData, CreateWorkspaceErrors, CreateWorkspaceResponses, DeleteFileData, DeleteFileErrors, DeleteFileResponses, DeleteNodeColumnData, DeleteNodeColumnErrors, DeleteNodeColumnResponses, DeleteNodeData, DeleteNodeErrors, DeleteNodeResponses, DeleteWorkspaceData, DeleteWorkspaceErrors, DeleteWorkspaceResponses, DescribeColumnData, DescribeColumnErrors, DescribeColumnResponses, DetachAiPreviewedRowsData, DetachAiPreviewedRowsErrors, DetachAiPreviewedRowsResponses, DetachConcordanceData, DetachConcordanceDispersionData, DetachConcordanceDispersionErrors, DetachConcordanceDispersionResponses, DetachConcordanceErrors, DetachConcordanceResponses, DetachQuotationData, DetachQuotationErrors, DetachQuotationResponses, DetachSequentialAnalysisTaskData, DetachSequentialAnalysisTaskErrors, DetachSequentialAnalysisTaskResponses, DetachTopicModelingData, DetachTopicModelingErrors, DetachTopicModelingResponses, DownloadFileData, DownloadFileErrors, DownloadFileResponses, DownloadWorkspaceArtifactData, DownloadWorkspaceArtifactErrors, DownloadWorkspaceArtifactResponses, ExportNodesData, ExportNodesErrors, ExportNodesResponses, FilterNodeData, FilterNodeErrors, FilterNodeResponses, FilterPreviewData, FilterPreviewErrors, FilterPreviewResponses, GetAnnotationClassDescriptionsData, GetAnnotationClassDescriptionsErrors, GetAnnotationClassDescriptionsResponses, GetAuthInfoData, GetAuthInfoErrors, GetAuthInfoResponses, GetColumnUniqueValuesData, GetColumnUniqueValuesErrors, GetColumnUniqueValuesResponses, GetConfigData, GetConfigResponses, GetCurrentUserInfoData, GetCurrentUserInfoErrors, GetCurrentUserInfoResponses, GetCurrentWorkspaceData, GetCurrentWorkspaceErrors, GetCurrentWorkspaceResponses, GetFileInfoData, GetFileInfoErrors, GetFileInfoResponses, GetNodeDataData, GetNodeDataErrors, GetNodeDataResponses, GetNodeInfoData, GetNodeInfoErrors, GetNodeInfoResponses, GetNodeQueryPlanData, GetNodeQueryPlanErrors, GetNodeQueryPlanResponses, GetNodeShapeData, GetNodeShapeErrors, GetNodeShapeResponses, GetPreferencesData, GetPreferencesErrors, GetPreferencesResponses, GetQuotationData, GetQuotationErrors, GetQuotationResponses, GetRawFileData, GetRawFileErrors, GetRawFileResponses, GetSampleDataCatalogueData, GetSampleDataCatalogueErrors, GetSampleDataCatalogueResponses, GetSampleDataReadmeData, GetSampleDataReadmeErrors, GetSampleDataReadmeResponses, GetTokenizerModelsData, GetTokenizerModelsErrors, GetTokenizerModelsResponses, GetUserFilesData, GetUserFilesErrors, GetUserFilesResponses, GetWorkspaceGraphData, GetWorkspaceGraphErrors, GetWorkspaceGraphResponses, GetWorkspaceInfoData, GetWorkspaceInfoErrors, GetWorkspaceInfoResponses, GetWorkspaceNodesData, GetWorkspaceNodesErrors, GetWorkspaceNodesResponses, GetWorkspaceTabsData, GetWorkspaceTabsErrors, GetWorkspaceTabsResponses, GoogleAuthCallbackData, GoogleAuthCallbackErrors, GoogleAuthCallbackResponses, GoogleAuthData, GoogleAuthErrors, GoogleAuthResponses, HealthCheckData, HealthCheckResponses, ImportLdacaDatasetData, ImportLdacaDatasetErrors, ImportLdacaDatasetResponses, ImportSampleDataData, ImportSampleDataErrors, ImportSampleDataResponses, JoinNodesData, JoinNodesErrors, JoinNodesPreviewData, JoinNodesPreviewErrors, JoinNodesPreviewResponses, JoinNodesResponses, ListAnnotationAiModelsData, ListAnnotationAiModelsErrors, ListAnnotationAiModelsResponses, ListFilesTasksData, ListFilesTasksErrors, ListFilesTasksResponses, ListLdacaFeaturedCollectionsData, ListLdacaFeaturedCollectionsErrors, ListLdacaFeaturedCollectionsResponses, ListTasksData, ListTasksErrors, ListTasksResponses, ListUsersData, ListUsersErrors, ListUsersResponses, ListWorkspacesData, ListWorkspacesErrors, ListWorkspacesResponses, LogoutData, LogoutErrors, LogoutResponses, MaterializeConcordanceData, MaterializeConcordanceErrors, MaterializeConcordanceResponses, MaterializeQuotationData, MaterializeQuotationErrors, MaterializeQuotationResponses, MoveFileData, MoveFileErrors, MoveFileResponses, PolarsExpressionApplyData, PolarsExpressionApplyErrors, PolarsExpressionApplyResponses, PolarsExpressionPreviewData, PolarsExpressionPreviewErrors, PolarsExpressionPreviewResponses, PreviewSequentialAnalysisData, PreviewSequentialAnalysisErrors, PreviewSequentialAnalysisResponses, PutWorkspaceTabsData, PutWorkspaceTabsErrors, PutWorkspaceTabsResponses, QuotationDetachOptionsData, QuotationDetachOptionsErrors, QuotationDetachOptionsResponses, QuotationTaskRequestData, QuotationTaskRequestErrors, QuotationTaskRequestResponses, QuotationTaskResultData, QuotationTaskResultErrors, QuotationTaskResultResponses, RedoNodeOperationData, RedoNodeOperationErrors, RedoNodeOperationResponses, RenameNodeColumnData, RenameNodeColumnErrors, RenameNodeColumnResponses, RenameWorkspaceData, RenameWorkspaceErrors, RenameWorkspaceResponses, ReorderWorkspaceNodesData, ReorderWorkspaceNodesErrors, ReorderWorkspaceNodesResponses, ReplaceApplyData, ReplaceApplyErrors, ReplaceApplyResponses, ReplacePreviewData, ReplacePreviewErrors, ReplacePreviewResponses, RootData, RootResponses, RunConcordanceData, RunConcordanceErrors, RunConcordanceResponses, RunSequentialAnalysisData, RunSequentialAnalysisErrors, RunSequentialAnalysisResponses, RunTopicModelingData, RunTopicModelingErrors, RunTopicModelingResponses, SaveWorkspaceData, SaveWorkspaceErrors, SaveWorkspaceResponses, SearchLdacaCollectionsData, SearchLdacaCollectionsErrors, SearchLdacaCollectionsResponses, SequentialAnalysisTaskRequestData, SequentialAnalysisTaskRequestErrors, SequentialAnalysisTaskRequestResponses, SequentialAnalysisTaskResultData, SequentialAnalysisTaskResultErrors, SequentialAnalysisTaskResultResponses, SetAnnotationCellData, SetAnnotationCellErrors, SetAnnotationCellResponses, SetAnnotationClassParentData, SetAnnotationClassParentErrors, SetAnnotationClassParentResponses, SetCurrentWorkspaceData, SetCurrentWorkspaceErrors, SetCurrentWorkspaceResponses, SetNodeColorData, SetNodeColorErrors, SetNodeColorResponses, SetNodeDocumentColumnData, SetNodeDocumentColumnErrors, SetNodeDocumentColumnResponses, SetNodeTokenizationPreferenceData, SetNodeTokenizationPreferenceErrors, SetNodeTokenizationPreferenceResponses, SliceNodeData, SliceNodeErrors, SliceNodeResponses, SlicePreviewData, SlicePreviewErrors, SlicePreviewResponses, StartWorkspaceDownloadData, StartWorkspaceDownloadErrors, StartWorkspaceDownloadResponses, StatusData, StatusResponses, StreamTasksData, StreamTasksErrors, StreamTasksResponses, TokenFrequenciesTaskRequestData, TokenFrequenciesTaskRequestErrors, TokenFrequenciesTaskRequestResponses, TokenFrequenciesTaskResultData, TokenFrequenciesTaskResultErrors, TokenFrequenciesTaskResultResponses, TopicModelingDetachOptionsData, TopicModelingDetachOptionsErrors, TopicModelingDetachOptionsResponses, TopicModelingTaskRequestData, TopicModelingTaskRequestErrors, TopicModelingTaskRequestResponses, TopicModelingTaskResultData, TopicModelingTaskResultErrors, TopicModelingTaskResultResponses, UndoNodeOperationData, UndoNodeOperationErrors, UndoNodeOperationResponses, UnifiedFilePreviewData, UnifiedFilePreviewErrors, UnifiedFilePreviewResponses, UnloadWorkspaceData, UnloadWorkspaceErrors, UnloadWorkspaceResponses, UpdateAnnotationClassDescriptionsData, UpdateAnnotationClassDescriptionsErrors, UpdateAnnotationClassDescriptionsResponses, UpdateConfigData, UpdateConfigErrors, UpdateConfigResponses, UpdateNodeNameData, UpdateNodeNameErrors, UpdateNodeNameResponses, UpdatePreferencesData, UpdatePreferencesErrors, UpdatePreferencesResponses, UpdateQuotationTaskResultData, UpdateQuotationTaskResultErrors, UpdateQuotationTaskResultResponses, UpdateSequentialAnalysisTaskResultData, UpdateSequentialAnalysisTaskResultErrors, UpdateSequentialAnalysisTaskResultResponses, UpdateTokenFrequenciesTaskResultData, UpdateTokenFrequenciesTaskResultErrors, UpdateTokenFrequenciesTaskResultResponses, UpdateWorkspaceDescriptionData, UpdateWorkspaceDescriptionErrors, UpdateWorkspaceDescriptionResponses, UploadFileData, UploadFileErrors, UploadFileResponses, UploadWorkspaceZipData, UploadWorkspaceZipErrors, UploadWorkspaceZipResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Root
 *
 * Return API feature/index metadata.
 *
 * Used by:
 * - browser/manual root checks and basic service discovery because callers need the shared
 * shared backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Provides a human-readable entrypoint summary for backend capabilities.
 */
export const root = <ThrowOnError extends boolean = false>(options?: Options<RootData, ThrowOnError>): RequestResult<RootResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RootResponses, unknown, ThrowOnError>({ url: '/api', ...options });

/**
 * Admin Cleanup
 *
 * Trigger cleanup of expired session rows.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - admin maintenance actions because they need this unit's "Trigger cleanup of expired session rows" behavior.
 *
 * Why:
 * - Allows manual session-store maintenance in addition to automatic cleanup.
 *
 * Refactor note:
 * - Add `require_admin` dependency before wider deployment.
 */
export const adminCleanup = <ThrowOnError extends boolean = false>(options?: Options<AdminCleanupData, ThrowOnError>): RequestResult<AdminCleanupResponses, AdminCleanupErrors, ThrowOnError> => (options?.client ?? client).get<AdminCleanupResponses, AdminCleanupErrors, ThrowOnError>({ url: '/api/admin/cleanup', ...options });

/**
 * List Users
 *
 * List users with active-session counts.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - admin dashboard/user management views because they need this unit's "List users with active-session counts" behavior.
 *
 * Why:
 * - Provides operational visibility into user and session activity.
 *
 * Refactor note:
 * - Add `require_admin` dependency before wider deployment to avoid role drift.
 */
export const listUsers = <ThrowOnError extends boolean = false>(options?: Options<ListUsersData, ThrowOnError>): RequestResult<ListUsersResponses, ListUsersErrors, ThrowOnError> => (options?.client ?? client).get<ListUsersResponses, ListUsersErrors, ThrowOnError>({ url: '/api/admin/users', ...options });

/**
 * Get Auth Info
 *
 * Main auth endpoint - tells frontend everything it needs to know.
 *
 * Returns:
 * - In single-user mode: authenticated=True with root user info
 * - In multi-user mode with valid token: authenticated=True with user info
 * - In multi-user mode without token: authenticated=False with available auth methods
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend app bootstrap auth probe because they need this unit's "Main auth endpoint - tells frontend everything it needs to know" behavior.
 *
 * Why:
 * - Provides one canonical auth capability + identity payload per startup.
 */
export const getAuthInfo = <ThrowOnError extends boolean = false>(options?: Options<GetAuthInfoData, ThrowOnError>): RequestResult<GetAuthInfoResponses, GetAuthInfoErrors, ThrowOnError> => (options?.client ?? client).get<GetAuthInfoResponses, GetAuthInfoErrors, ThrowOnError>({ url: '/api/auth/', ...options });

/**
 * Cilogon Callback
 *
 * Handle the CILogon authorization code callback.
 *
 * CILogon redirects here after the user authenticates. We verify the CSRF
 * ``state``, exchange the authorization code for tokens, fetch the user's
 * profile from the userinfo endpoint, provision a local session, and
 * redirect back to the SPA with the access token in the URL query string.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /cilogon/callback route because they need this unit's "Handle the CILogon authorization code callback" behavior.
 */
export const cilogonCallback = <ThrowOnError extends boolean = false>(options?: Options<CilogonCallbackData, ThrowOnError>): RequestResult<CilogonCallbackResponses, CilogonCallbackErrors, ThrowOnError> => (options?.client ?? client).get<CilogonCallbackResponses, CilogonCallbackErrors, ThrowOnError>({ url: '/api/auth/cilogon/callback', ...options });

/**
 * Cilogon Login
 *
 * Redirect the browser to the CILogon authorization endpoint.
 *
 * Generates a random ``state`` for CSRF protection (stored as a short-lived
 * cookie), builds the OIDC authorization URL from the discovery document,
 * and redirects the user to CILogon to authenticate.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /cilogon/login route because they need this unit's "Redirect the browser to the CILogon authorization endpoint" behavior.
 */
export const cilogonLogin = <ThrowOnError extends boolean = false>(options?: Options<CilogonLoginData, ThrowOnError>): RequestResult<CilogonLoginResponses, unknown, ThrowOnError> => (options?.client ?? client).get<CilogonLoginResponses, unknown, ThrowOnError>({ url: '/api/auth/cilogon/login', ...options });

/**
 * Google Auth
 *
 * Authenticate user via Google OAuth and create app session tokens.
 *
 * Used by the frontend Google sign-in flow (JSON body variant). Delegates
 * the verification + provisioning logic to :func:`_verify_and_create_session`
 * and shapes the response into ``GoogleOut``.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /google route because they need this unit's "Authenticate user via Google OAuth and create app session tokens" behavior.
 */
export const googleAuth = <ThrowOnError extends boolean = false>(options: Options<GoogleAuthData, ThrowOnError>): RequestResult<GoogleAuthResponses, GoogleAuthErrors, ThrowOnError> => (options.client ?? client).post<GoogleAuthResponses, GoogleAuthErrors, ThrowOnError>({
    url: '/api/auth/google',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Google Auth Callback
 *
 * Handle the Google Identity Services redirect callback.
 *
 * Google POSTs the ID-token ``credential`` and a CSRF token here after the
 * user authenticates on Google's consent page (``ux_mode: 'redirect'``).
 * We verify the token, create/find the local user, issue a session, and
 * redirect back to the SPA with the access token in the URL fragment.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /google/callback route because they need this unit's "Handle the Google Identity Services redirect callback" behavior.
 */
export const googleAuthCallback = <ThrowOnError extends boolean = false>(options: Options<GoogleAuthCallbackData, ThrowOnError>): RequestResult<GoogleAuthCallbackResponses, GoogleAuthCallbackErrors, ThrowOnError> => (options.client ?? client).post<GoogleAuthCallbackResponses, GoogleAuthCallbackErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/auth/google/callback',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Auth Health
 *
 * Return authentication subsystem readiness metadata.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - health/status probes and diagnostics pages because they need this unit's "Return authentication subsystem readiness metadata" behavior.
 *
 * Why:
 * - Exposes auth mode and endpoint availability without authentication.
 */
export const authHealth = <ThrowOnError extends boolean = false>(options?: Options<AuthHealthData, ThrowOnError>): RequestResult<AuthHealthResponses, unknown, ThrowOnError> => (options?.client ?? client).get<AuthHealthResponses, unknown, ThrowOnError>({ url: '/api/auth/health', ...options });

/**
 * Logout
 *
 * Logout current user session (multi-user) or no-op (single-user).
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend sign-out action because they need this unit's "Logout current user session (multi-user) or no-op (single-user)" behavior.
 *
 * Why:
 * - Keeps logout behavior mode-aware while preserving shared endpoint contract.
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>): RequestResult<LogoutResponses, LogoutErrors, ThrowOnError> => (options?.client ?? client).post<LogoutResponses, LogoutErrors, ThrowOnError>({ url: '/api/auth/logout', ...options });

/**
 * Get Current User Info
 *
 * Return normalized current user profile fields.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend profile/session widgets because they need this unit's "Return normalized current user profile fields" behavior.
 *
 * Why:
 * - Provides stable user response shape independent of DB row types.
 */
export const getCurrentUserInfo = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentUserInfoData, ThrowOnError>): RequestResult<GetCurrentUserInfoResponses, GetCurrentUserInfoErrors, ThrowOnError> => (options?.client ?? client).get<GetCurrentUserInfoResponses, GetCurrentUserInfoErrors, ThrowOnError>({ url: '/api/auth/me', ...options });

/**
 * Auth Status
 *
 * Return minimal authenticated status payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - lightweight frontend auth status checks because they need this unit's "Return minimal authenticated status payload" behavior.
 *
 * Why:
 * - Allows cheap auth verification without full `get_auth_info` metadata.
 */
export const authStatus = <ThrowOnError extends boolean = false>(options?: Options<AuthStatusData, ThrowOnError>): RequestResult<AuthStatusResponses, AuthStatusErrors, ThrowOnError> => (options?.client ?? client).get<AuthStatusResponses, AuthStatusErrors, ThrowOnError>({ url: '/api/auth/status', ...options });

/**
 * Get Config
 *
 * Return currently effective runtime configuration values.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend settings/config panels and OAuth client bootstrap because they need this unit's "Return currently effective runtime configuration values" behavior.
 *
 * Why:
 * - Exposes backend mode, storage root, and Google OAuth client ID so the
 * frontend can initialize the login provider at runtime.
 */
export const getConfig = <ThrowOnError extends boolean = false>(options?: Options<GetConfigData, ThrowOnError>): RequestResult<GetConfigResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetConfigResponses, unknown, ThrowOnError>({ url: '/api/config/', ...options });

/**
 * Update Config
 *
 * Update in-memory runtime configuration values.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend config edit flow because they need this unit's "Update in-memory runtime configuration values" behavior.
 *
 * Why:
 * - Allows runtime overrides without process restart.
 *
 * Refactor note:
 * - Current update is in-memory only; persist-or-reload strategy may be needed
 * for multi-process or restart-stable configuration behavior.
 */
export const updateConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateConfigData, ThrowOnError>): RequestResult<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError> => (options.client ?? client).post<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError>({
    url: '/api/config/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get User Files
 *
 * List user-visible files as a nested tree.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file browser panel.
 *
 * Why:
 * - The frontend consumes the directory structure directly instead of
 * reconstructing it from a flat file listing.
 */
export const getUserFiles = <ThrowOnError extends boolean = false>(options?: Options<GetUserFilesData, ThrowOnError>): RequestResult<GetUserFilesResponses, GetUserFilesErrors, ThrowOnError> => (options?.client ?? client).get<GetUserFilesResponses, GetUserFilesErrors, ThrowOnError>({ url: '/api/files/', ...options });

/**
 * Create Folder
 *
 * Create a folder inside the user's data directory.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /folders route.
 */
export const createFolder = <ThrowOnError extends boolean = false>(options: Options<CreateFolderData, ThrowOnError>): RequestResult<CreateFolderResponses, CreateFolderErrors, ThrowOnError> => (options.client ?? client).post<CreateFolderResponses, CreateFolderErrors, ThrowOnError>({
    url: '/api/files/folders',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import Ldaca Dataset
 *
 * Submit background task to import LDaCA dataset from URL.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend dataset import action.
 *
 * Why:
 * - Runs network/download/import pipeline outside request-response lifecycle.
 */
export const importLdacaDataset = <ThrowOnError extends boolean = false>(options: Options<ImportLdacaDatasetData, ThrowOnError>): RequestResult<ImportLdacaDatasetResponses, ImportLdacaDatasetErrors, ThrowOnError> => (options.client ?? client).post<ImportLdacaDatasetResponses, ImportLdacaDatasetErrors, ThrowOnError>({
    url: '/api/files/import-ldaca',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import Sample Data
 *
 * Import (or re-import) sample data for the current user on demand.
 *
 * Copies bundled datasets (SCL, ADO/twitter) immediately, then downloads any
 * missing or updated remote datasets if SAMPLE_DATA_REMOTE_URL is configured.
 *
 * If request.collection_ids is non-empty, only those collections are
 * downloaded remotely. An empty list means "download all non-bundled
 * collections" (default).
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /import-sample-data route.
 */
export const importSampleData = <ThrowOnError extends boolean = false>(options?: Options<ImportSampleDataData, ThrowOnError>): RequestResult<ImportSampleDataResponses, ImportSampleDataErrors, ThrowOnError> => (options?.client ?? client).post<ImportSampleDataResponses, ImportSampleDataErrors, ThrowOnError>({
    url: '/api/files/import-sample-data',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * List Ldaca Featured Collections
 *
 * Return staff-picked LDaCA collections for the import dialog.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /ldaca/featured route.
 */
export const listLdacaFeaturedCollections = <ThrowOnError extends boolean = false>(options?: Options<ListLdacaFeaturedCollectionsData, ThrowOnError>): RequestResult<ListLdacaFeaturedCollectionsResponses, ListLdacaFeaturedCollectionsErrors, ThrowOnError> => (options?.client ?? client).get<ListLdacaFeaturedCollectionsResponses, ListLdacaFeaturedCollectionsErrors, ThrowOnError>({ url: '/api/files/ldaca/featured', ...options });

/**
 * Search Ldaca Collections
 *
 * Search LDaCA records through the backend ONI proxy.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /ldaca/search route.
 */
export const searchLdacaCollections = <ThrowOnError extends boolean = false>(options: Options<SearchLdacaCollectionsData, ThrowOnError>): RequestResult<SearchLdacaCollectionsResponses, SearchLdacaCollectionsErrors, ThrowOnError> => (options.client ?? client).post<SearchLdacaCollectionsResponses, SearchLdacaCollectionsErrors, ThrowOnError>({
    url: '/api/files/ldaca/search',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Move File
 *
 * Move a file into another directory inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /move route.
 */
export const moveFile = <ThrowOnError extends boolean = false>(options: Options<MoveFileData, ThrowOnError>): RequestResult<MoveFileResponses, MoveFileErrors, ThrowOnError> => (options.client ?? client).post<MoveFileResponses, MoveFileErrors, ThrowOnError>({
    url: '/api/files/move',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unified File Preview
 *
 * Unified file preview endpoint.
 *
 * - Returns supported types based on extension.
 * - Provides preview data (first few rows or page slice).
 * - For Excel files, returns sheet_names and supports selecting sheet via
 * payload.sheet_name.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file preview modal/table.
 *
 * Why:
 * - Provides one format-aware preview API for heterogeneous file types.
 */
export const unifiedFilePreview = <ThrowOnError extends boolean = false>(options: Options<UnifiedFilePreviewData, ThrowOnError>): RequestResult<UnifiedFilePreviewResponses, UnifiedFilePreviewErrors, ThrowOnError> => (options.client ?? client).post<UnifiedFilePreviewResponses, UnifiedFilePreviewErrors, ThrowOnError>({
    url: '/api/files/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Raw File
 *
 * Return raw UTF-8 text content for a file inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /raw route.
 */
export const getRawFile = <ThrowOnError extends boolean = false>(options: Options<GetRawFileData, ThrowOnError>): RequestResult<GetRawFileResponses, GetRawFileErrors, ThrowOnError> => (options.client ?? client).get<GetRawFileResponses, GetRawFileErrors, ThrowOnError>({ url: '/api/files/raw', ...options });

/**
 * Get Sample Data Catalogue
 *
 * Return the sample data catalogue augmented with per-collection status.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sample-data/catalogue route.
 */
export const getSampleDataCatalogue = <ThrowOnError extends boolean = false>(options?: Options<GetSampleDataCatalogueData, ThrowOnError>): RequestResult<GetSampleDataCatalogueResponses, GetSampleDataCatalogueErrors, ThrowOnError> => (options?.client ?? client).get<GetSampleDataCatalogueResponses, GetSampleDataCatalogueErrors, ThrowOnError>({ url: '/api/files/sample-data/catalogue', ...options });

/**
 * Get Sample Data Readme
 *
 * Proxy a README.md from the remote sample data repository.
 *
 * Only .md files are permitted; path traversal is rejected.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sample-data/readme route.
 */
export const getSampleDataReadme = <ThrowOnError extends boolean = false>(options: Options<GetSampleDataReadmeData, ThrowOnError>): RequestResult<GetSampleDataReadmeResponses, GetSampleDataReadmeErrors, ThrowOnError> => (options.client ?? client).get<GetSampleDataReadmeResponses, GetSampleDataReadmeErrors, ThrowOnError>({ url: '/api/files/sample-data/readme', ...options });

/**
 * List Files Tasks
 *
 * List file-import worker tasks exposed via the files API.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend import-task status polling.
 *
 * Why:
 * - Exposes file-import task status via explicit task types.
 */
export const listFilesTasks = <ThrowOnError extends boolean = false>(options?: Options<ListFilesTasksData, ThrowOnError>): RequestResult<ListFilesTasksResponses, ListFilesTasksErrors, ThrowOnError> => (options?.client ?? client).get<ListFilesTasksResponses, ListFilesTasksErrors, ThrowOnError>({ url: '/api/files/tasks', ...options });

/**
 * Clear Files Tasks
 *
 * Clear persisted file-import task records.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend task-list cleanup actions.
 *
 * Why:
 * - Removes completed/failed import task clutter while keeping artifacts.
 */
export const clearFilesTasks = <ThrowOnError extends boolean = false>(options?: Options<ClearFilesTasksData, ThrowOnError>): RequestResult<ClearFilesTasksResponses, ClearFilesTasksErrors, ThrowOnError> => (options?.client ?? client).post<ClearFilesTasksResponses, ClearFilesTasksErrors, ThrowOnError>({ url: '/api/files/tasks/clear', ...options });

/**
 * Upload File
 *
 * Upload file to user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /upload route.
 */
export const uploadFile = <ThrowOnError extends boolean = false>(options: Options<UploadFileData, ThrowOnError>): RequestResult<UploadFileResponses, UploadFileErrors, ThrowOnError> => (options.client ?? client).post<UploadFileResponses, UploadFileErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/api/files/upload',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Delete File
 *
 * Delete user's file.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /{filename:path} route.
 */
export const deleteFile = <ThrowOnError extends boolean = false>(options: Options<DeleteFileData, ThrowOnError>): RequestResult<DeleteFileResponses, DeleteFileErrors, ThrowOnError> => (options.client ?? client).delete<DeleteFileResponses, DeleteFileErrors, ThrowOnError>({ url: '/api/files/{filename}', ...options });

/**
 * Download File
 *
 * Download user's file.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /{filename:path} route.
 */
export const downloadFile = <ThrowOnError extends boolean = false>(options: Options<DownloadFileData, ThrowOnError>): RequestResult<DownloadFileResponses, DownloadFileErrors, ThrowOnError> => (options.client ?? client).get<DownloadFileResponses, DownloadFileErrors, ThrowOnError>({ url: '/api/files/{filename}', ...options });

/**
 * Get File Info
 *
 * Get detailed file information.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /{filename:path}/info route.
 */
export const getFileInfo = <ThrowOnError extends boolean = false>(options: Options<GetFileInfoData, ThrowOnError>): RequestResult<GetFileInfoResponses, GetFileInfoErrors, ThrowOnError> => (options.client ?? client).get<GetFileInfoResponses, GetFileInfoErrors, ThrowOnError>({ url: '/api/files/{filename}/info', ...options });

/**
 * Get Preferences
 *
 * Return get preferences API requests for preference routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET / route because they need this unit's "Return get preferences API requests for preference routes" behavior.
 */
export const getPreferences = <ThrowOnError extends boolean = false>(options?: Options<GetPreferencesData, ThrowOnError>): RequestResult<GetPreferencesResponses, GetPreferencesErrors, ThrowOnError> => (options?.client ?? client).get<GetPreferencesResponses, GetPreferencesErrors, ThrowOnError>({ url: '/api/preferences/', ...options });

/**
 * Update Preferences
 *
 * Update update preferences API requests for preference routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT / route because they need this unit's "Update update preferences API requests for preference routes" behavior.
 */
export const updatePreferences = <ThrowOnError extends boolean = false>(options: Options<UpdatePreferencesData, ThrowOnError>): RequestResult<UpdatePreferencesResponses, UpdatePreferencesErrors, ThrowOnError> => (options.client ?? client).put<UpdatePreferencesResponses, UpdatePreferencesErrors, ThrowOnError>({
    url: '/api/preferences/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Tasks
 *
 * List tasks for current user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET / route because they need this unit's "List tasks for current user" behavior.
 */
export const listTasks = <ThrowOnError extends boolean = false>(options?: Options<ListTasksData, ThrowOnError>): RequestResult<ListTasksResponses, ListTasksErrors, ThrowOnError> => (options?.client ?? client).get<ListTasksResponses, ListTasksErrors, ThrowOnError>({ url: '/api/tasks', ...options });

/**
 * Cancel Task
 *
 * Stop a running task and mark it as cancelled.
 *
 * Unlike ``/tasks/clear``, the task record is kept so the user can see the
 * cancelled state in the task list and explicitly clear it afterwards.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /cancel route because they need this unit's "Stop a running task and mark it as cancelled" behavior.
 */
export const cancelTask = <ThrowOnError extends boolean = false>(options: Options<CancelTaskData, ThrowOnError>): RequestResult<CancelTaskResponses, CancelTaskErrors, ThrowOnError> => (options.client ?? client).post<CancelTaskResponses, CancelTaskErrors, ThrowOnError>({ url: '/api/tasks/cancel', ...options });

/**
 * Clear Tasks
 *
 * Clear a task and all associated caches by task id.
 *
 * If the task is still running it is cancelled first.  Both the worker
 * task manager record and the analysis task manager record (including
 * the current-task-id mapping) are removed.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /clear route because they need this unit's "Clear a task and all associated caches by task id" behavior.
 */
export const clearTasks = <ThrowOnError extends boolean = false>(options: Options<ClearTasksData, ThrowOnError>): RequestResult<ClearTasksResponses, ClearTasksErrors, ThrowOnError> => (options.client ?? client).post<ClearTasksResponses, ClearTasksErrors, ThrowOnError>({ url: '/api/tasks/clear', ...options });

/**
 * Stream Tasks
 *
 * Unified SSE stream for task center.
 *
 * Includes all user tasks from a single per-user task manager channel.
 *
 * - frontend Task Center SSE subscriber
 *
 * Why:
 * - Streams all per-user task updates through one connection.
 *
 * Refactor note:
 * - Nested helper closures inside endpoint are sizeable; extraction to a small
 * streaming service object could improve testability.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /stream route because they need this unit's "Unified SSE stream for task center" behavior.
 */
export const streamTasks = <ThrowOnError extends boolean = false>(options?: Options<StreamTasksData, ThrowOnError>): RequestResult<StreamTasksResponses, StreamTasksErrors, ThrowOnError> => (options?.client ?? client).get<StreamTasksResponses, StreamTasksErrors, ThrowOnError>({ url: '/api/tasks/stream', ...options });

/**
 * List Workspaces
 *
 * List all persisted workspaces visible to the current user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend workspace switcher/landing views because they need this unit's "List all persisted workspaces visible to the current user" behavior.
 *
 * Why:
 * - Provides fast summary metadata without loading full workspace graphs.
 */
export const listWorkspaces = <ThrowOnError extends boolean = false>(options?: Options<ListWorkspacesData, ThrowOnError>): RequestResult<ListWorkspacesResponses, ListWorkspacesErrors, ThrowOnError> => (options?.client ?? client).get<ListWorkspacesResponses, ListWorkspacesErrors, ThrowOnError>({ url: '/api/workspaces/', ...options });

/**
 * Create Workspace
 *
 * Create a workspace and return normalized workspace metadata.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend new-workspace dialog because they need this unit's "Create a workspace and return normalized workspace metadata" behavior.
 *
 * Why:
 * - Centralizes workspace-name validation and initialization metadata.
 */
export const createWorkspace = <ThrowOnError extends boolean = false>(options: Options<CreateWorkspaceData, ThrowOnError>): RequestResult<CreateWorkspaceResponses, CreateWorkspaceErrors, ThrowOnError> => (options.client ?? client).post<CreateWorkspaceResponses, CreateWorkspaceErrors, ThrowOnError>({
    url: '/api/workspaces/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Annotate Ai All
 *
 * AI-classify every row concurrently and write the whole column in one go.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel's "Annotate All" button because filling a
 * whole column one cell at a time is slow; this runs all batches concurrently
 * on the server and persists the result as a single column rewrite.
 *
 * Flow:
 * - Resolve the workspace + source node (404), collect it once, and require an
 * existing *string* annotation column (400) so the write target is valid.
 * - Load the authoritative class list from the class node (404/400).
 * - Reuse any labels already cached in the preview store (rows the user previewed,
 * with their manual overrides), and dispatch batches concurrently via
 * ``annotate_all`` for ONLY the remaining rows — so a full run never re-spends on
 * pages the user already previewed and respects edits made during preview.
 * - Overwrite the annotation column with the merged labels (null where the model
 * chose no class) and restage/persist the node; clear the cache since the
 * predictions now live in the column. Provider errors become a 502 and nothing
 * is written.
 */
export const annotateAiAll = <ThrowOnError extends boolean = false>(options: Options<AnnotateAiAllData, ThrowOnError>): RequestResult<AnnotateAiAllResponses, AnnotateAiAllErrors, ThrowOnError> => (options.client ?? client).post<AnnotateAiAllResponses, AnnotateAiAllErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/annotate-all',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Detach Ai Previewed Rows
 *
 * Copy the already-previewed rows into a new annotated child node.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel's "Detach Previewed Rows" button because
 * AI preview predictions live only in the server-side preview store; detaching
 * turns every page the user has already assessed (not just the current one) into
 * a persisted child table so those labels survive as a real data block without
 * re-running (or persisting) the whole column on the source node.
 *
 * Flow:
 * - Resolve the workspace + source node (404).
 * - Build the row->label map from the node's active preview session (every
 * previewed row across all viewed pages, override winning over the model label);
 * fall back to any explicit ``rows`` the client sent.
 * - When ``dry_run`` is set, return that map's size immediately (``node=None``,
 * nothing created). The panel calls this on mount to gate/label its Detach
 * button from the authoritative server session, so a tab switch (which wipes the
 * panel's local page map) no longer leaves the button wrongly disabled.
 * - Otherwise require a non-blank annotation column (400) and at least one row
 * (400) — this is what fixes "detach only grabbed one page", since the source of
 * truth is now the whole server session rather than the panel's current page.
 * - Collect the source once; validate every ``row_index`` is in range (400).
 * - Filter the source down to those indices (original order preserved), overwrite
 * the annotation column with the aligned labels (blank/whitespace -> null), and
 * stage the subset as a lazy frame.
 * - Add it as a child of the source via ``_create_and_persist_child_node`` (so the
 * lineage shows in the graph) and return the new node metadata + row count.
 */
export const detachAiPreviewedRows = <ThrowOnError extends boolean = false>(options: Options<DetachAiPreviewedRowsData, ThrowOnError>): RequestResult<DetachAiPreviewedRowsResponses, DetachAiPreviewedRowsErrors, ThrowOnError> => (options.client ?? client).post<DetachAiPreviewedRowsResponses, DetachAiPreviewedRowsErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/detach-previewed',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Annotation Ai Models
 *
 * Proxy a provider's model listing so the browser never calls providers.
 *
 * Used by:
 * - Frontend ModelNameCombobox to populate the model dropdown. Authentication is
 * required (per-user gate) even though no workspace is touched, so an
 * unauthenticated caller cannot use the backend as an open model-listing proxy.
 *
 * Flow:
 * - Delegate to ``core.annotation_ai.list_models`` (native SDK per provider).
 * - Translate any provider/SDK failure into a 502 with the provider's message.
 */
export const listAnnotationAiModels = <ThrowOnError extends boolean = false>(options: Options<ListAnnotationAiModelsData, ThrowOnError>): RequestResult<ListAnnotationAiModelsResponses, ListAnnotationAiModelsErrors, ThrowOnError> => (options.client ?? client).post<ListAnnotationAiModelsResponses, ListAnnotationAiModelsErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/models',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Annotate Ai Preview
 *
 * AI-classify one page of texts, caching results in the preview store.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel because previewing must not mutate the
 * workspace; the panel renders the returned labels beside the (unchanged)
 * source column so users can eyeball provider/model/prompt/class quality.
 *
 * Flow:
 * - Resolve the workspace + source node (404) and require the text column (400).
 * - Load the authoritative class list from the class-description node (404/400).
 * - Slice exactly the requested page of the text column — the same
 * ``(page-1)*page_size`` slice ``get_node_data`` uses — so labels line up with
 * the rows the panel displays.
 * - Sync the server-side preview session for this node + config signature, then
 * dispatch a provider batch for ONLY the page rows not already cached (re-viewed
 * pages are cache hits and cost nothing). Store the fresh labels and return the
 * model labels for the whole page in order; provider errors become a 502.
 */
export const annotateAiPreview = <ThrowOnError extends boolean = false>(options: Options<AnnotateAiPreviewData, ThrowOnError>): RequestResult<AnnotateAiPreviewResponses, AnnotateAiPreviewErrors, ThrowOnError> => (options.client ?? client).post<AnnotateAiPreviewResponses, AnnotateAiPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Annotate Ai Preview Clear
 *
 * Discard the node's cached AI preview session.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel when the user clicks "Close preview". The
 * in-memory preview store keeps a node's predictions across tab switches so the
 * panel can rehydrate, but an explicit close means those predictions are no
 * longer wanted; dropping the session frees the memory and resets the
 * detach/annotate-all counts so a later preview starts clean.
 *
 * Flow:
 * - Resolve the current workspace (the store is keyed per user+workspace+node).
 * - Clear the node's session unconditionally. Clearing is idempotent, so a missing
 * session (nothing previewed, or already cleared) is a successful no-op; the node
 * itself is not required to still exist, which is why no 404 is raised here.
 */
export const annotateAiPreviewClear = <ThrowOnError extends boolean = false>(options: Options<AnnotateAiPreviewClearData, ThrowOnError>): RequestResult<AnnotateAiPreviewClearResponses, AnnotateAiPreviewClearErrors, ThrowOnError> => (options.client ?? client).post<AnnotateAiPreviewClearResponses, AnnotateAiPreviewClearErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/preview/clear',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Annotate Ai Preview Override
 *
 * Persist one manual cell edit onto the node's active preview session.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel when the user changes a prediction in the
 * dropdown, so the edit survives a tab switch (it returns via ``preview/state``)
 * and is honoured by detach/annotate-all. A blank label is stored as an explicit
 * null override (the user chose "None"), which still wins over the model label.
 *
 * Flow:
 * - Resolve the workspace + source node (404).
 * - Write the override to the current session; ``ok=False`` when there is no active
 * session (the edit is stale — an override can only follow a preview).
 */
export const annotateAiPreviewOverride = <ThrowOnError extends boolean = false>(options: Options<AnnotateAiPreviewOverrideData, ThrowOnError>): RequestResult<AnnotateAiPreviewOverrideResponses, AnnotateAiPreviewOverrideErrors, ThrowOnError> => (options.client ?? client).put<AnnotateAiPreviewOverrideResponses, AnnotateAiPreviewOverrideErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/preview/override',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Annotate Ai Preview State
 *
 * Return the cached preview rows for a node so the panel can rehydrate.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel on mount because its per-row AI labels and
 * manual overrides are in-memory only; after a tab switch the panel re-reads
 * them here so the preview reappears exactly as the user left it.
 *
 * Flow:
 * - Resolve the workspace + source node (404).
 * - Recompute the config signature and ask the store for its rows; if the stored
 * session belongs to a different config the store returns nothing, so the panel
 * shows an empty preview and re-classifies on demand rather than displaying
 * stale labels.
 */
export const annotateAiPreviewState = <ThrowOnError extends boolean = false>(options: Options<AnnotateAiPreviewStateData, ThrowOnError>): RequestResult<AnnotateAiPreviewStateResponses, AnnotateAiPreviewStateErrors, ThrowOnError> => (options.client ?? client).post<AnnotateAiPreviewStateResponses, AnnotateAiPreviewStateErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/ai/preview/state',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Annotation Class Descriptions
 *
 * Create an empty class-description table for the Annotation view.
 *
 * Used by:
 * - Frontend Annotation view through the generated SDK because users need a
 * one-click way to seed the class/description table required by the view.
 *
 * Flow:
 * - Resolve the active workspace.
 * - Build an empty ``class``/``description`` table with string dtypes.
 * - Persist it to the workspace data folder and add it as a root node.
 * - Save the workspace and return the standard node metadata response.
 */
export const createAnnotationClassDescriptions = <ThrowOnError extends boolean = false>(options?: Options<CreateAnnotationClassDescriptionsData, ThrowOnError>): RequestResult<CreateAnnotationClassDescriptionsResponses, CreateAnnotationClassDescriptionsErrors, ThrowOnError> => (options?.client ?? client).post<CreateAnnotationClassDescriptionsResponses, CreateAnnotationClassDescriptionsErrors, ThrowOnError>({ url: '/api/workspaces/annotation/class-descriptions', ...options });

/**
 * Get Annotation Class Descriptions
 *
 * Return editable class-description rows for one selected node.
 *
 * Used by:
 * - Frontend Annotation view because the class editor needs a compact
 * two-column row payload independent of the full workspace table UI.
 */
export const getAnnotationClassDescriptions = <ThrowOnError extends boolean = false>(options: Options<GetAnnotationClassDescriptionsData, ThrowOnError>): RequestResult<GetAnnotationClassDescriptionsResponses, GetAnnotationClassDescriptionsErrors, ThrowOnError> => (options.client ?? client).get<GetAnnotationClassDescriptionsResponses, GetAnnotationClassDescriptionsErrors, ThrowOnError>({ url: '/api/workspaces/annotation/class-descriptions/{node_id}', ...options });

/**
 * Update Annotation Class Descriptions
 *
 * Persist edited class-description rows back to the selected node.
 *
 * Used by:
 * - Frontend Annotation view on cell blur/add-row because class labels and
 * descriptions should behave like workspace data, not transient UI state.
 *
 * Flow:
 * - Validate the node and selected columns.
 * - Rewrite the selected class/description columns from the submitted rows.
 * - Restage the dataframe as workspace data and save the workspace.
 */
export const updateAnnotationClassDescriptions = <ThrowOnError extends boolean = false>(options: Options<UpdateAnnotationClassDescriptionsData, ThrowOnError>): RequestResult<UpdateAnnotationClassDescriptionsResponses, UpdateAnnotationClassDescriptionsErrors, ThrowOnError> => (options.client ?? client).put<UpdateAnnotationClassDescriptionsResponses, UpdateAnnotationClassDescriptionsErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/class-descriptions/{node_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set Annotation Class Parent
 *
 * Reparent a class-description node under the annotated source node.
 *
 * Used by:
 * - Frontend Annotation view on Start because making the class table a child
 * of the source node keeps the annotation lineage visible in the graph.
 *
 * Flow:
 * - Resolve workspace, source, and class nodes (404 if missing).
 * - Reject self-parenting, then set the class node's only parent to the source
 * and re-tidy its list position.
 * - Persist and return the standard node metadata response.
 */
export const setAnnotationClassParent = <ThrowOnError extends boolean = false>(options: Options<SetAnnotationClassParentData, ThrowOnError>): RequestResult<SetAnnotationClassParentResponses, SetAnnotationClassParentErrors, ThrowOnError> => (options.client ?? client).put<SetAnnotationClassParentResponses, SetAnnotationClassParentErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/class-descriptions/{node_id}/parent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set Annotation Cell
 *
 * Write a single class label into the source node's annotation column.
 *
 * Used by:
 * - Frontend Annotation results table because each per-row class dropdown
 * persists its selection so annotations survive reloads and feed downstream
 * analyses, rather than staying as local component state.
 *
 * Flow:
 * - Resolve the workspace and source node (404 if missing).
 * - Require a non-blank, existing string annotation column (400 otherwise).
 * - Bounds-check the absolute row index against the collected frame (400).
 * - Rewrite just the target cell (blank/whitespace clears it to null), restage
 * the materialized frame, persist the workspace, and return node metadata.
 */
export const setAnnotationCell = <ThrowOnError extends boolean = false>(options: Options<SetAnnotationCellData, ThrowOnError>): RequestResult<SetAnnotationCellResponses, SetAnnotationCellErrors, ThrowOnError> => (options.client ?? client).put<SetAnnotationCellResponses, SetAnnotationCellErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/source/{node_id}/annotation-cell',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Annotation Column
 *
 * Add an empty string annotation column to the selected source node.
 *
 * Used by:
 * - Frontend Annotation view on Start (new-annotation mode) because the source
 * block needs a real, persisted column before the per-row class dropdowns can
 * record annotations and before the view can switch into resume mode.
 *
 * Flow:
 * - Resolve the workspace and source node (404 if missing).
 * - Reject blank names and names that already exist on the node (400).
 * - Append a null-filled string column lazily, then restage the materialized
 * frame so the node stays portable and lazy by default.
 * - Persist the workspace and return the refreshed node metadata.
 */
export const createAnnotationColumn = <ThrowOnError extends boolean = false>(options: Options<CreateAnnotationColumnData, ThrowOnError>): RequestResult<CreateAnnotationColumnResponses, CreateAnnotationColumnErrors, ThrowOnError> => (options.client ?? client).post<CreateAnnotationColumnResponses, CreateAnnotationColumnErrors, ThrowOnError>({
    url: '/api/workspaces/annotation/source/{node_id}/annotation-column',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Run Concordance
 *
 * Run concordance immediately and store task metadata for retrieval.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend run route: `POST /workspaces/{id}/concordance` because they need this unit's "Run concordance immediately and store task metadata for retrieval" behavior.
 *
 * Why:
 * - Keeps API behavior aligned with other analyses by returning task-linked
 * responses while using shared concordance response builders.
 */
export const runConcordance = <ThrowOnError extends boolean = false>(options: Options<RunConcordanceData, ThrowOnError>): RequestResult<RunConcordanceResponses, RunConcordanceErrors, ThrowOnError> => (options.client ?? client).post<RunConcordanceResponses, RunConcordanceErrors, ThrowOnError>({
    url: '/api/workspaces/concordance',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Concordance Task Dispersion Bins
 *
 * Return 100-bucket dispersion histogram for one materialised concordance node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend dispersion summary plot, when a node has been materialised by because they need this unit's "Return 100-bucket dispersion histogram for one materialised concordance node" behavior.
 * "Process All". The frontend re-aggregates these 100 buckets into a
 * smaller number of display bins (4, 5, 10, 20, 25, 50, 100) without
 * another network round-trip.
 *
 * Why:
 * - Server-side pre-binning collapses the response from one row per hit
 * (potentially tens of MB on large blocks) to ~100 rows per matched-text
 * term, regardless of corpus size.
 */
export const concordanceTaskDispersionBins = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskDispersionBinsData, ThrowOnError>): RequestResult<ConcordanceTaskDispersionBinsResponses, ConcordanceTaskDispersionBinsErrors, ThrowOnError> => (options.client ?? client).get<ConcordanceTaskDispersionBinsResponses, ConcordanceTaskDispersionBinsErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/{task_id}/bins', ...options });

/**
 * Concordance Task Request
 *
 * Return stored request payload for a concordance task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /concordance/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a concordance task" behavior.
 */
export const concordanceTaskRequest = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskRequestData, ThrowOnError>): RequestResult<ConcordanceTaskRequestResponses, ConcordanceTaskRequestErrors, ThrowOnError> => (options.client ?? client).get<ConcordanceTaskRequestResponses, ConcordanceTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/{task_id}/request', ...options });

/**
 * Concordance Task Result
 *
 * Read concordance result with optional pagination/sort overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend polling route: `GET /workspaces/{id}/concordance/tasks/{id}/result` because they need this unit's "Read concordance result with optional pagination/sort overrides" behavior.
 *
 * Why:
 * - Hydrates saved concordance state while allowing query-time view changes.
 */
export const concordanceTaskResult = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskResultData, ThrowOnError>): RequestResult<ConcordanceTaskResultResponses, ConcordanceTaskResultErrors, ThrowOnError> => (options.client ?? client).get<ConcordanceTaskResultResponses, ConcordanceTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/concordance/tasks/{task_id}/result', ...options });

/**
 * Concordance Task Result Post
 *
 * Read concordance result using POST body overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend state-sync route: because they need this unit's "Read concordance result using POST body overrides" behavior.
 * `POST /workspaces/{id}/concordance/tasks/{id}/result`
 *
 * Why:
 * - Preserves compatibility with clients that send result preferences in body
 * payloads instead of query parameters.
 */
export const concordanceTaskResultPost = <ThrowOnError extends boolean = false>(options: Options<ConcordanceTaskResultPostData, ThrowOnError>): RequestResult<ConcordanceTaskResultPostResponses, ConcordanceTaskResultPostErrors, ThrowOnError> => (options.client ?? client).post<ConcordanceTaskResultPostResponses, ConcordanceTaskResultPostErrors, ThrowOnError>({
    url: '/api/workspaces/concordance/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Current Workspace
 *
 * Return get current workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /current route because they need this unit's "Return get current workspace API requests for workspace lifecycle routes" behavior.
 */
export const getCurrentWorkspace = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentWorkspaceData, ThrowOnError>): RequestResult<GetCurrentWorkspaceResponses, GetCurrentWorkspaceErrors, ThrowOnError> => (options?.client ?? client).get<GetCurrentWorkspaceResponses, GetCurrentWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/current', ...options });

/**
 * Set Current Workspace
 *
 * Set or clear the current in-memory workspace for the user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend workspace selection flow because they need this unit's "Set or clear the current in-memory workspace for the user" behavior.
 *
 * Why:
 * - Ensures subsequent node/analysis operations target the intended workspace.
 */
export const setCurrentWorkspace = <ThrowOnError extends boolean = false>(options?: Options<SetCurrentWorkspaceData, ThrowOnError>): RequestResult<SetCurrentWorkspaceResponses, SetCurrentWorkspaceErrors, ThrowOnError> => (options?.client ?? client).post<SetCurrentWorkspaceResponses, SetCurrentWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/current', ...options });

/**
 * Delete Workspace
 *
 * Delete delete workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /delete route because they need this unit's "Delete delete workspace API requests for workspace lifecycle routes" behavior.
 */
export const deleteWorkspace = <ThrowOnError extends boolean = false>(options: Options<DeleteWorkspaceData, ThrowOnError>): RequestResult<DeleteWorkspaceResponses, DeleteWorkspaceErrors, ThrowOnError> => (options.client ?? client).delete<DeleteWorkspaceResponses, DeleteWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/delete', ...options });

/**
 * Update Workspace Description
 *
 * Update update workspace description API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /description route because they need this unit's "Update update workspace description API requests for workspace lifecycle routes" behavior.
 */
export const updateWorkspaceDescription = <ThrowOnError extends boolean = false>(options?: Options<UpdateWorkspaceDescriptionData, ThrowOnError>): RequestResult<UpdateWorkspaceDescriptionResponses, UpdateWorkspaceDescriptionErrors, ThrowOnError> => (options?.client ?? client).put<UpdateWorkspaceDescriptionResponses, UpdateWorkspaceDescriptionErrors, ThrowOnError>({ url: '/api/workspaces/description', ...options });

/**
 * Start Workspace Download
 *
 * Start a background task to package the workspace as a ZIP archive.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend Download button in Workspace Manager because they need this unit's "Start a background task to package the workspace as a ZIP archive" behavior.
 *
 * Why:
 * - Moves potentially slow ZIP compression into the Task Center so users can
 * track progress and the UI stays responsive.
 */
export const startWorkspaceDownload = <ThrowOnError extends boolean = false>(options?: Options<StartWorkspaceDownloadData, ThrowOnError>): RequestResult<StartWorkspaceDownloadResponses, StartWorkspaceDownloadErrors, ThrowOnError> => (options?.client ?? client).post<StartWorkspaceDownloadResponses, StartWorkspaceDownloadErrors, ThrowOnError>({ url: '/api/workspaces/download', ...options });

/**
 * Download Workspace Artifact
 *
 * Stream a completed workspace ZIP artifact and delete it after download.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend auto-download on task completion because they need this unit's "Stream a completed workspace ZIP artifact and delete it after download" behavior.
 *
 * Why:
 * - One-time artifact policy: the ZIP is deleted after the first successful
 * download to avoid unbounded disk usage.
 */
export const downloadWorkspaceArtifact = <ThrowOnError extends boolean = false>(options: Options<DownloadWorkspaceArtifactData, ThrowOnError>): RequestResult<DownloadWorkspaceArtifactResponses, DownloadWorkspaceArtifactErrors, ThrowOnError> => (options.client ?? client).get<DownloadWorkspaceArtifactResponses, DownloadWorkspaceArtifactErrors, ThrowOnError>({ url: '/api/workspaces/download/tasks/{task_id}/artifact', ...options });

/**
 * Export Nodes
 *
 * Export one or more workspace nodes as downloadable file(s).
 *
 * If multiple node_ids are provided, a ZIP archive is returned.
 * Supported formats: csv, json, parquet, ipc, ndjson.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /export route because they need this unit's "Export one or more workspace nodes as downloadable file(s)" behavior.
 */
export const exportNodes = <ThrowOnError extends boolean = false>(options: Options<ExportNodesData, ThrowOnError>): RequestResult<ExportNodesResponses, ExportNodesErrors, ThrowOnError> => (options.client ?? client).get<ExportNodesResponses, ExportNodesErrors, ThrowOnError>({ url: '/api/workspaces/export', ...options });

/**
 * Get Workspace Graph
 *
 * Return workspace graph payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend graph canvas initialization and refresh because they need this unit's "Return workspace graph payload" behavior.
 */
export const getWorkspaceGraph = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspaceGraphData, ThrowOnError>): RequestResult<GetWorkspaceGraphResponses, GetWorkspaceGraphErrors, ThrowOnError> => (options?.client ?? client).get<GetWorkspaceGraphResponses, GetWorkspaceGraphErrors, ThrowOnError>({ url: '/api/workspaces/graph', ...options });

/**
 * Get Workspace Info
 *
 * Return get workspace info API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /info route because they need this unit's "Return get workspace info API requests for workspace lifecycle routes" behavior.
 */
export const getWorkspaceInfo = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspaceInfoData, ThrowOnError>): RequestResult<GetWorkspaceInfoResponses, GetWorkspaceInfoErrors, ThrowOnError> => (options?.client ?? client).get<GetWorkspaceInfoResponses, GetWorkspaceInfoErrors, ThrowOnError>({ url: '/api/workspaces/info', ...options });

/**
 * Rename Workspace
 *
 * Update rename workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /name route because they need this unit's "Update rename workspace API requests for workspace lifecycle routes" behavior.
 */
export const renameWorkspace = <ThrowOnError extends boolean = false>(options: Options<RenameWorkspaceData, ThrowOnError>): RequestResult<RenameWorkspaceResponses, RenameWorkspaceErrors, ThrowOnError> => (options.client ?? client).put<RenameWorkspaceResponses, RenameWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/name', ...options });

/**
 * Get Workspace Nodes
 *
 * Return get workspace nodes API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /nodes route because they need this unit's "Return get workspace nodes API requests for workspace lifecycle routes" behavior.
 */
export const getWorkspaceNodes = <ThrowOnError extends boolean = false>(options?: Options<GetWorkspaceNodesData, ThrowOnError>): RequestResult<GetWorkspaceNodesResponses, GetWorkspaceNodesErrors, ThrowOnError> => (options?.client ?? client).get<GetWorkspaceNodesResponses, GetWorkspaceNodesErrors, ThrowOnError>({ url: '/api/workspaces/nodes', ...options });

/**
 * Add Node To Workspace
 *
 * Add a data file as a new node to workspace.
 *
 * Files are eagerly loaded into a Polars DataFrame, persisted as parquet under the workspace's `data/` folder,
 * and then reloaded as a LazyFrame. This separates bulk data from `metadata.json` while keeping
 * lazy processing semantics.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes route because they need this unit's "Add a data file as a new node to workspace" behavior.
 */
export const addNodeToWorkspace = <ThrowOnError extends boolean = false>(options: Options<AddNodeToWorkspaceData, ThrowOnError>): RequestResult<AddNodeToWorkspaceResponses, AddNodeToWorkspaceErrors, ThrowOnError> => (options.client ?? client).post<AddNodeToWorkspaceResponses, AddNodeToWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/nodes', ...options });

/**
 * Concat Nodes
 *
 * Create a new child node by concatenating multiple nodes vertically.
 */
export const concatNodes = <ThrowOnError extends boolean = false>(options: Options<ConcatNodesData, ThrowOnError>): RequestResult<ConcatNodesResponses, ConcatNodesErrors, ThrowOnError> => (options.client ?? client).post<ConcatNodesResponses, ConcatNodesErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/concat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Concat Nodes Preview
 *
 * Preview the result of concatenating multiple nodes vertically.
 */
export const concatNodesPreview = <ThrowOnError extends boolean = false>(options: Options<ConcatNodesPreviewData, ThrowOnError>): RequestResult<ConcatNodesPreviewResponses, ConcatNodesPreviewErrors, ThrowOnError> => (options.client ?? client).post<ConcatNodesPreviewResponses, ConcatNodesPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/concat/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Join Nodes
 *
 * Join two workspace nodes and persist the result as a new child node.
 */
export const joinNodes = <ThrowOnError extends boolean = false>(options: Options<JoinNodesData, ThrowOnError>): RequestResult<JoinNodesResponses, JoinNodesErrors, ThrowOnError> => (options.client ?? client).post<JoinNodesResponses, JoinNodesErrors, ThrowOnError>({ url: '/api/workspaces/nodes/join', ...options });

/**
 * Join Nodes Preview
 *
 * Preview a join between two workspace nodes.
 */
export const joinNodesPreview = <ThrowOnError extends boolean = false>(options: Options<JoinNodesPreviewData, ThrowOnError>): RequestResult<JoinNodesPreviewResponses, JoinNodesPreviewErrors, ThrowOnError> => (options.client ?? client).post<JoinNodesPreviewResponses, JoinNodesPreviewErrors, ThrowOnError>({ url: '/api/workspaces/nodes/join/preview', ...options });

/**
 * Reorder Workspace Nodes
 *
 * Persist a new node order for the current workspace and return the graph.
 *
 * Flow:
 * - Resolve the authenticated user's active workspace id and workspace object.
 * - Apply ``ordered_ids`` through ``Workspace.reorder_nodes`` (unknown ids are
 * ignored and any omitted node keeps its tail position, so a stale client
 * payload can never drop nodes).
 * - Persist the workspace and return the rebuilt graph in the new order.
 *
 * Used by:
 * - Frontend workspace list-view drag-to-reorder gesture because dropping a row
 * commits the full node sequence as the durable source of truth.
 */
export const reorderWorkspaceNodes = <ThrowOnError extends boolean = false>(options: Options<ReorderWorkspaceNodesData, ThrowOnError>): RequestResult<ReorderWorkspaceNodesResponses, ReorderWorkspaceNodesErrors, ThrowOnError> => (options.client ?? client).put<ReorderWorkspaceNodesResponses, ReorderWorkspaceNodesErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/order',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Node
 *
 * Delete a node from the workspace.
 */
export const deleteNode = <ThrowOnError extends boolean = false>(options: Options<DeleteNodeData, ThrowOnError>): RequestResult<DeleteNodeResponses, DeleteNodeErrors, ThrowOnError> => (options.client ?? client).delete<DeleteNodeResponses, DeleteNodeErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}', ...options });

/**
 * Get Node Info
 *
 * Return workspace node metadata.
 */
export const getNodeInfo = <ThrowOnError extends boolean = false>(options: Options<GetNodeInfoData, ThrowOnError>): RequestResult<GetNodeInfoResponses, GetNodeInfoErrors, ThrowOnError> => (options.client ?? client).get<GetNodeInfoResponses, GetNodeInfoErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}', ...options });

/**
 * Cast Node
 *
 * Cast a single column data type in a node using Polars casting methods (in-place operation).
 *
 * Args:
 * workspace_id: The workspace identifier
 * node_id: The node identifier to cast
 * cast_data: Dictionary with casting specifications:
 * - column: str - name of the column to cast
 * - target_type: str - target data type (e.g., "integer", "float", "string", "datetime", "boolean", "categorical")
 * - format: str (optional) - datetime format string for string to datetime conversion
 * Example: {"column": "date_col", "target_type": "datetime", "format": "%Y-%m-%d"}
 *
 * Returns:
 * Dictionary with the updated node information after casting
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/cast route because they need this unit's "Cast a single column data type in a node using Polars casting methods (in-place operation)" behavior.
 */
export const castNode = <ThrowOnError extends boolean = false>(options: Options<CastNodeData, ThrowOnError>): RequestResult<CastNodeResponses, CastNodeErrors, ThrowOnError> => (options.client ?? client).post<CastNodeResponses, CastNodeErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/cast',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clone Node
 *
 * Clone a workspace node (deep-copy its LazyFrame).
 */
export const cloneNode = <ThrowOnError extends boolean = false>(options: Options<CloneNodeData, ThrowOnError>): RequestResult<CloneNodeResponses, CloneNodeErrors, ThrowOnError> => (options.client ?? client).post<CloneNodeResponses, CloneNodeErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/clone', ...options });

/**
 * Set Node Color
 *
 * Set the persistent visualization colour on a workspace node.
 */
export const setNodeColor = <ThrowOnError extends boolean = false>(options: Options<SetNodeColorData, ThrowOnError>): RequestResult<SetNodeColorResponses, SetNodeColorErrors, ThrowOnError> => (options.client ?? client).post<SetNodeColorResponses, SetNodeColorErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/color',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete Node Column
 *
 * Delete a column from a node by delegating to DocWorkspace Node.drop.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /nodes/{node_id}/columns/{column_name} route because they need this unit's "Delete a column from a node by delegating to DocWorkspace Node.drop" behavior.
 */
export const deleteNodeColumn = <ThrowOnError extends boolean = false>(options: Options<DeleteNodeColumnData, ThrowOnError>): RequestResult<DeleteNodeColumnResponses, DeleteNodeColumnErrors, ThrowOnError> => (options.client ?? client).delete<DeleteNodeColumnResponses, DeleteNodeColumnErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}', ...options });

/**
 * Rename Node Column
 *
 * Rename a column by delegating to DocWorkspace Node.rename.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /nodes/{node_id}/columns/{column_name} route because they need this unit's "Rename a column by delegating to DocWorkspace Node.rename" behavior.
 */
export const renameNodeColumn = <ThrowOnError extends boolean = false>(options: Options<RenameNodeColumnData, ThrowOnError>): RequestResult<RenameNodeColumnResponses, RenameNodeColumnErrors, ThrowOnError> => (options.client ?? client).put<RenameNodeColumnResponses, RenameNodeColumnErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/columns/{column_name}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Describe Column
 *
 * Get descriptive statistics for a column using Polars describe.
 */
export const describeColumn = <ThrowOnError extends boolean = false>(options: Options<DescribeColumnData, ThrowOnError>): RequestResult<DescribeColumnResponses, DescribeColumnErrors, ThrowOnError> => (options.client ?? client).get<DescribeColumnResponses, DescribeColumnErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}/describe', ...options });

/**
 * Column Operations
 *
 * Return available no-arg Polars operations for a column, filtered by dtype.
 */
export const columnOperations = <ThrowOnError extends boolean = false>(options: Options<ColumnOperationsData, ThrowOnError>): RequestResult<ColumnOperationsResponses, ColumnOperationsErrors, ThrowOnError> => (options.client ?? client).get<ColumnOperationsResponses, ColumnOperationsErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}/operations', ...options });

/**
 * Get Column Unique Values
 *
 * Return unique values for a column.
 */
export const getColumnUniqueValues = <ThrowOnError extends boolean = false>(options: Options<GetColumnUniqueValuesData, ThrowOnError>): RequestResult<GetColumnUniqueValuesResponses, GetColumnUniqueValuesErrors, ThrowOnError> => (options.client ?? client).get<GetColumnUniqueValuesResponses, GetColumnUniqueValuesErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/columns/{column_name}/unique', ...options });

/**
 * Detach Concordance
 *
 * Submit a background task to create a concordance-detached node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend detach action: because they need this unit's "Submit a background task to create a concordance-detached node" behavior.
 * `POST /workspaces/{id}/nodes/{node_id}/concordance/detach`
 *
 * Why:
 * - Runs potentially expensive row extraction out-of-band and returns task id
 * for progress tracking.
 */
export const detachConcordance = <ThrowOnError extends boolean = false>(options: Options<DetachConcordanceData, ThrowOnError>): RequestResult<DetachConcordanceResponses, DetachConcordanceErrors, ThrowOnError> => (options.client ?? client).post<DetachConcordanceResponses, DetachConcordanceErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/concordance/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Concordance Detach Options
 *
 * Return detachable concordance columns for one node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend concordance detach dialog because they need this unit's "Return detachable concordance columns for one node" behavior.
 *
 * Why:
 * - Keeps mandatory generated concordance columns and optional metadata
 * columns aligned with backend detach behavior.
 */
export const concordanceDetachOptions = <ThrowOnError extends boolean = false>(options: Options<ConcordanceDetachOptionsData, ThrowOnError>): RequestResult<ConcordanceDetachOptionsResponses, ConcordanceDetachOptionsErrors, ThrowOnError> => (options.client ?? client).get<ConcordanceDetachOptionsResponses, ConcordanceDetachOptionsErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/concordance/detach-options', ...options });

/**
 * Detach Concordance Dispersion
 *
 * Submit a per-document aggregated detach (dispersion view).
 *
 * Output shape differs from the per-hit detach: one row per source document,
 * matched-text/L1/R1/freq columns become `List<T>`, plus a `CONC_extraction`
 * string column that joins each hit's character slice with newline + asterisk
 * bullets in document-flow order. Used by the dispersion summary chart so the
 * user can pull a per-document view (optionally limited to bin-filtered hits)
 * into the workspace as a new data block.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/concordance/dispersion-detach route because they need this unit's "Submit a per-document aggregated detach (dispersion view)" behavior.
 */
export const detachConcordanceDispersion = <ThrowOnError extends boolean = false>(options: Options<DetachConcordanceDispersionData, ThrowOnError>): RequestResult<DetachConcordanceDispersionResponses, DetachConcordanceDispersionErrors, ThrowOnError> => (options.client ?? client).post<DetachConcordanceDispersionResponses, DetachConcordanceDispersionErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/concordance/dispersion-detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Materialize Concordance
 *
 * Submit a background task that writes the full flattened occurrence parquet.
 *
 * Unlike detach, this does not add a node to the workspace. On completion the
 * parent concordance analysis task's `materialized_paths` is updated so
 * subsequent pagination and detach reuse the cached parquet.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/concordance/materialize route because they need this unit's "Submit a background task that writes the full flattened occurrence parquet" behavior.
 */
export const materializeConcordance = <ThrowOnError extends boolean = false>(options: Options<MaterializeConcordanceData, ThrowOnError>): RequestResult<MaterializeConcordanceResponses, MaterializeConcordanceErrors, ThrowOnError> => (options.client ?? client).post<MaterializeConcordanceResponses, MaterializeConcordanceErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/concordance/materialize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Node Data
 *
 * Return paginated node data rows.
 */
export const getNodeData = <ThrowOnError extends boolean = false>(options: Options<GetNodeDataData, ThrowOnError>): RequestResult<GetNodeDataResponses, GetNodeDataErrors, ThrowOnError> => (options.client ?? client).get<GetNodeDataResponses, GetNodeDataErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/data', ...options });

/**
 * Set Node Document Column
 *
 * Set or clear the document column on a workspace node.
 */
export const setNodeDocumentColumn = <ThrowOnError extends boolean = false>(options: Options<SetNodeDocumentColumnData, ThrowOnError>): RequestResult<SetNodeDocumentColumnResponses, SetNodeDocumentColumnErrors, ThrowOnError> => (options.client ?? client).put<SetNodeDocumentColumnResponses, SetNodeDocumentColumnErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/document-column',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Polars Expression Apply
 *
 * Apply a Polars expression and persist the result.
 */
export const polarsExpressionApply = <ThrowOnError extends boolean = false>(options: Options<PolarsExpressionApplyData, ThrowOnError>): RequestResult<PolarsExpressionApplyResponses, PolarsExpressionApplyErrors, ThrowOnError> => (options.client ?? client).post<PolarsExpressionApplyResponses, PolarsExpressionApplyErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/expression/apply',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Polars Expression Preview
 *
 * Preview a Polars expression applied to the source node.
 */
export const polarsExpressionPreview = <ThrowOnError extends boolean = false>(options: Options<PolarsExpressionPreviewData, ThrowOnError>): RequestResult<PolarsExpressionPreviewResponses, PolarsExpressionPreviewErrors, ThrowOnError> => (options.client ?? client).post<PolarsExpressionPreviewResponses, PolarsExpressionPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/expression/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Filter Node
 *
 * Create a new child node by filtering the source node's data.
 */
export const filterNode = <ThrowOnError extends boolean = false>(options: Options<FilterNodeData, ThrowOnError>): RequestResult<FilterNodeResponses, FilterNodeErrors, ThrowOnError> => (options.client ?? client).post<FilterNodeResponses, FilterNodeErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/filter',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Filter Preview
 *
 * Preview the result of a filter operation on the source node.
 */
export const filterPreview = <ThrowOnError extends boolean = false>(options: Options<FilterPreviewData, ThrowOnError>): RequestResult<FilterPreviewResponses, FilterPreviewErrors, ThrowOnError> => (options.client ?? client).post<FilterPreviewResponses, FilterPreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/filter/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Node Name
 *
 * Rename a workspace node.
 */
export const updateNodeName = <ThrowOnError extends boolean = false>(options: Options<UpdateNodeNameData, ThrowOnError>): RequestResult<UpdateNodeNameResponses, UpdateNodeNameErrors, ThrowOnError> => (options.client ?? client).put<UpdateNodeNameResponses, UpdateNodeNameErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/name', ...options });

/**
 * Get Node Query Plan
 *
 * Return the Polars query plan for a workspace node.
 */
export const getNodeQueryPlan = <ThrowOnError extends boolean = false>(options: Options<GetNodeQueryPlanData, ThrowOnError>): RequestResult<GetNodeQueryPlanResponses, GetNodeQueryPlanErrors, ThrowOnError> => (options.client ?? client).get<GetNodeQueryPlanResponses, GetNodeQueryPlanErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/query-plan', ...options });

/**
 * Get Quotation
 *
 * Run quotation extraction on selected node and store latest task payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend quotation run/search action because they need this unit's "Run quotation extraction on selected node and store latest task payload" behavior.
 *
 * Why:
 * - Produces immediate result payload and persists it as current quotation task.
 */
export const getQuotation = <ThrowOnError extends boolean = false>(options: Options<GetQuotationData, ThrowOnError>): RequestResult<GetQuotationResponses, GetQuotationErrors, ThrowOnError> => (options.client ?? client).post<GetQuotationResponses, GetQuotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/quotation',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Detach Quotation
 *
 * Submit background task to detach quotations into a new workspace node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend quotation detach action because they need this unit's "Submit background task to detach quotations into a new workspace node" behavior.
 *
 * Why:
 * - Offloads potentially expensive extraction/materialization to worker tasks.
 */
export const detachQuotation = <ThrowOnError extends boolean = false>(options: Options<DetachQuotationData, ThrowOnError>): RequestResult<DetachQuotationResponses, DetachQuotationErrors, ThrowOnError> => (options.client ?? client).post<DetachQuotationResponses, DetachQuotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/quotation/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Quotation Detach Options
 *
 * Return detachable quotation columns for one node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend quotation detach dialog because they need this unit's "Return detachable quotation columns for one node" behavior.
 *
 * Why:
 * - Keeps mandatory generated quotation columns and optional source columns
 * aligned with backend detach behavior.
 */
export const quotationDetachOptions = <ThrowOnError extends boolean = false>(options: Options<QuotationDetachOptionsData, ThrowOnError>): RequestResult<QuotationDetachOptionsResponses, QuotationDetachOptionsErrors, ThrowOnError> => (options.client ?? client).get<QuotationDetachOptionsResponses, QuotationDetachOptionsErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/quotation/detach-options', ...options });

/**
 * Materialize Quotation
 *
 * Submit a background task that writes the full flattened quotation parquet.
 *
 * Unlike detach, this does not add a node to the workspace. On completion the
 * parent quotation analysis task's `materialized_path` is updated so subsequent
 * pagination and detach reuse the cached parquet.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/quotation/materialize route because they need this unit's "Submit a background task that writes the full flattened quotation parquet" behavior.
 */
export const materializeQuotation = <ThrowOnError extends boolean = false>(options: Options<MaterializeQuotationData, ThrowOnError>): RequestResult<MaterializeQuotationResponses, MaterializeQuotationErrors, ThrowOnError> => (options.client ?? client).post<MaterializeQuotationResponses, MaterializeQuotationErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/quotation/materialize',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redo Node Operation
 *
 * Redo the latest undone in-memory execution plan change for a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/redo route because they need this unit's "Redo the latest undone in-memory execution plan change for a node" behavior.
 */
export const redoNodeOperation = <ThrowOnError extends boolean = false>(options: Options<RedoNodeOperationData, ThrowOnError>): RequestResult<RedoNodeOperationResponses, RedoNodeOperationErrors, ThrowOnError> => (options.client ?? client).post<RedoNodeOperationResponses, RedoNodeOperationErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/redo', ...options });

/**
 * Replace Apply
 *
 * Apply a regex replace operation and persist the updated node.
 */
export const replaceApply = <ThrowOnError extends boolean = false>(options: Options<ReplaceApplyData, ThrowOnError>): RequestResult<ReplaceApplyResponses, ReplaceApplyErrors, ThrowOnError> => (options.client ?? client).post<ReplaceApplyResponses, ReplaceApplyErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/replace',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Replace Preview
 *
 * Preview a regex replace operation on the source node.
 */
export const replacePreview = <ThrowOnError extends boolean = false>(options: Options<ReplacePreviewData, ThrowOnError>): RequestResult<ReplacePreviewResponses, ReplacePreviewErrors, ThrowOnError> => (options.client ?? client).post<ReplacePreviewResponses, ReplacePreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/replace/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Run Sequential Analysis
 *
 * Run sequential analysis for one node and persist/update task payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend sequential-analysis run action because they need this unit's "Run sequential analysis for one node and persist/update task payload" behavior.
 *
 * Why:
 * - Produces aggregated time-series counts and stores them under an explicit task id.
 */
export const runSequentialAnalysis = <ThrowOnError extends boolean = false>(options: Options<RunSequentialAnalysisData, ThrowOnError>): RequestResult<RunSequentialAnalysisResponses, RunSequentialAnalysisErrors, ThrowOnError> => (options.client ?? client).post<RunSequentialAnalysisResponses, RunSequentialAnalysisErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/sequential-analysis',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Preview Sequential Analysis
 *
 * Run the aggregation server-side but skip task registration and
 * conflict checks.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend preview/count checks because they need row-count feedback without registering
 * or replacing the live analysis task.
 * - callers that pass ``include_data=true`` because they need the same aggregated rows the
 * regular endpoint produces without mutating task state.
 *
 * Why a separate endpoint rather than a flag on the existing one:
 * the existing endpoint is task-aware (rejects conflicting requests,
 * inherits chart_type, writes the result into the task store). All
 * of that machinery is wrong for a pure preview query.
 */
export const previewSequentialAnalysis = <ThrowOnError extends boolean = false>(options: Options<PreviewSequentialAnalysisData, ThrowOnError>): RequestResult<PreviewSequentialAnalysisResponses, PreviewSequentialAnalysisErrors, ThrowOnError> => (options.client ?? client).post<PreviewSequentialAnalysisResponses, PreviewSequentialAnalysisErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/sequential-analysis/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Node Shape
 *
 * Return the row x column shape of a workspace node.
 */
export const getNodeShape = <ThrowOnError extends boolean = false>(options: Options<GetNodeShapeData, ThrowOnError>): RequestResult<GetNodeShapeResponses, GetNodeShapeErrors, ThrowOnError> => (options.client ?? client).get<GetNodeShapeResponses, GetNodeShapeErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/shape', ...options });

/**
 * Slice Node
 *
 * Create a new child node by slicing/sampling/shuffling the source.
 */
export const sliceNode = <ThrowOnError extends boolean = false>(options: Options<SliceNodeData, ThrowOnError>): RequestResult<SliceNodeResponses, SliceNodeErrors, ThrowOnError> => (options.client ?? client).post<SliceNodeResponses, SliceNodeErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/slice',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Slice Preview
 *
 * Preview the result of a slice/sample/shuffle on the source node.
 */
export const slicePreview = <ThrowOnError extends boolean = false>(options: Options<SlicePreviewData, ThrowOnError>): RequestResult<SlicePreviewResponses, SlicePreviewErrors, ThrowOnError> => (options.client ?? client).post<SlicePreviewResponses, SlicePreviewErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/slice/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set Node Tokenization Preference
 *
 * Set or clear a tokenization preference on a workspace node.
 */
export const setNodeTokenizationPreference = <ThrowOnError extends boolean = false>(options: Options<SetNodeTokenizationPreferenceData, ThrowOnError>): RequestResult<SetNodeTokenizationPreferenceResponses, SetNodeTokenizationPreferenceErrors, ThrowOnError> => (options.client ?? client).put<SetNodeTokenizationPreferenceResponses, SetNodeTokenizationPreferenceErrors, ThrowOnError>({
    url: '/api/workspaces/nodes/{node_id}/tokenization-preference',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Undo Node Operation
 *
 * Undo the latest in-memory execution plan change for a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/undo route because they need this unit's "Undo the latest in-memory execution plan change for a node" behavior.
 */
export const undoNodeOperation = <ThrowOnError extends boolean = false>(options: Options<UndoNodeOperationData, ThrowOnError>): RequestResult<UndoNodeOperationResponses, UndoNodeOperationErrors, ThrowOnError> => (options.client ?? client).post<UndoNodeOperationResponses, UndoNodeOperationErrors, ThrowOnError>({ url: '/api/workspaces/nodes/{node_id}/undo', ...options });

/**
 * Quotation Task Request
 *
 * Return stored request payload for a quotation task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /quotation/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a quotation task" behavior.
 */
export const quotationTaskRequest = <ThrowOnError extends boolean = false>(options: Options<QuotationTaskRequestData, ThrowOnError>): RequestResult<QuotationTaskRequestResponses, QuotationTaskRequestErrors, ThrowOnError> => (options.client ?? client).get<QuotationTaskRequestResponses, QuotationTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/quotation/tasks/{task_id}/request', ...options });

/**
 * Quotation Task Result
 *
 * Return stored quotation result, optionally recomputed for new page params.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend polling route for quotation result panels because they need this unit's "Return stored quotation result, optionally recomputed for new page params" behavior.
 *
 * Why:
 * - Supports cheap preference-only reads and on-demand page recomputation.
 */
export const quotationTaskResult = <ThrowOnError extends boolean = false>(options: Options<QuotationTaskResultData, ThrowOnError>): RequestResult<QuotationTaskResultResponses, QuotationTaskResultErrors, ThrowOnError> => (options.client ?? client).get<QuotationTaskResultResponses, QuotationTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/quotation/tasks/{task_id}/result', ...options });

/**
 * Update Quotation Task Result
 *
 * Persist quotation display preferences and optional page overrides.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend preference updates for context length/sort/page controls because they need this unit's "Persist quotation display preferences and optional page overrides" behavior.
 *
 * Why:
 * - Lets UI tune quotation presentation without rerunning analysis creation.
 *
 * Refactor note:
 * - Shares substantial logic with `quotation_task_result`; both could delegate
 * to a single internal read/update orchestrator.
 */
export const updateQuotationTaskResult = <ThrowOnError extends boolean = false>(options: Options<UpdateQuotationTaskResultData, ThrowOnError>): RequestResult<UpdateQuotationTaskResultResponses, UpdateQuotationTaskResultErrors, ThrowOnError> => (options.client ?? client).post<UpdateQuotationTaskResultResponses, UpdateQuotationTaskResultErrors, ThrowOnError>({
    url: '/api/workspaces/quotation/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Save Workspace
 *
 * Handle save workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /save route because they need this unit's "Handle save workspace API requests for workspace lifecycle routes" behavior.
 */
export const saveWorkspace = <ThrowOnError extends boolean = false>(options?: Options<SaveWorkspaceData, ThrowOnError>): RequestResult<SaveWorkspaceResponses, SaveWorkspaceErrors, ThrowOnError> => (options?.client ?? client).post<SaveWorkspaceResponses, SaveWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/save', ...options });

/**
 * Detach Sequential Analysis Task
 *
 * Create a filtered child node from selected sequential-analysis periods.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /sequential-analysis/tasks/{task_id}/detach route because they need this unit's "Create a filtered child node from selected sequential-analysis periods" behavior.
 */
export const detachSequentialAnalysisTask = <ThrowOnError extends boolean = false>(options: Options<DetachSequentialAnalysisTaskData, ThrowOnError>): RequestResult<DetachSequentialAnalysisTaskResponses, DetachSequentialAnalysisTaskErrors, ThrowOnError> => (options.client ?? client).post<DetachSequentialAnalysisTaskResponses, DetachSequentialAnalysisTaskErrors, ThrowOnError>({
    url: '/api/workspaces/sequential-analysis/tasks/{task_id}/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Sequential Analysis Task Request
 *
 * Return stored request payload for a sequential-analysis task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sequential-analysis/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a sequential-analysis task" behavior.
 */
export const sequentialAnalysisTaskRequest = <ThrowOnError extends boolean = false>(options: Options<SequentialAnalysisTaskRequestData, ThrowOnError>): RequestResult<SequentialAnalysisTaskRequestResponses, SequentialAnalysisTaskRequestErrors, ThrowOnError> => (options.client ?? client).get<SequentialAnalysisTaskRequestResponses, SequentialAnalysisTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/sequential-analysis/tasks/{task_id}/request', ...options });

/**
 * Sequential Analysis Task Result
 *
 * Return stored result payload for a sequential-analysis task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sequential-analysis/tasks/{task_id}/result route because they need this unit's "Return stored result payload for a sequential-analysis task" behavior.
 */
export const sequentialAnalysisTaskResult = <ThrowOnError extends boolean = false>(options: Options<SequentialAnalysisTaskResultData, ThrowOnError>): RequestResult<SequentialAnalysisTaskResultResponses, SequentialAnalysisTaskResultErrors, ThrowOnError> => (options.client ?? client).get<SequentialAnalysisTaskResultResponses, SequentialAnalysisTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/sequential-analysis/tasks/{task_id}/result', ...options });

/**
 * Update Sequential Analysis Task Result
 *
 * Persist display-only sequential analysis options on a saved task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend chart-type preference updates because they need this unit's "Persist display-only sequential analysis options on a saved task" behavior.
 *
 * Why:
 * - Avoids recomputation when only chart presentation changes.
 *
 * Refactor note:
 * - Mirrors preference update behavior in other analyses; a shared
 * task-preferences helper could reduce duplication.
 */
export const updateSequentialAnalysisTaskResult = <ThrowOnError extends boolean = false>(options: Options<UpdateSequentialAnalysisTaskResultData, ThrowOnError>): RequestResult<UpdateSequentialAnalysisTaskResultResponses, UpdateSequentialAnalysisTaskResultErrors, ThrowOnError> => (options.client ?? client).post<UpdateSequentialAnalysisTaskResultResponses, UpdateSequentialAnalysisTaskResultErrors, ThrowOnError>({
    url: '/api/workspaces/sequential-analysis/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clear Token Frequencies
 *
 * Clear Token Frequency analysis state for a workspace.
 *
 * Legacy broad clear endpoint: removes all token-frequency task records for
 * the active workspace. Tabbed clients should normally clear by explicit
 * task_id through `/api/tasks/clear` instead.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /token-frequencies route because they need this unit's "Clear Token Frequency analysis state for a workspace" behavior.
 */
export const clearTokenFrequencies = <ThrowOnError extends boolean = false>(options?: Options<ClearTokenFrequenciesData, ThrowOnError>): RequestResult<ClearTokenFrequenciesResponses, ClearTokenFrequenciesErrors, ThrowOnError> => (options?.client ?? client).delete<ClearTokenFrequenciesResponses, ClearTokenFrequenciesErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies', ...options });

/**
 * Calculate token frequencies for selected nodes
 *
 * Calculate and compare token frequencies across one or two nodes using polars-text
 */
export const calculateTokenFrequencies = <ThrowOnError extends boolean = false>(options: Options<CalculateTokenFrequenciesData, ThrowOnError>): RequestResult<CalculateTokenFrequenciesResponses, CalculateTokenFrequenciesErrors, ThrowOnError> => (options.client ?? client).post<CalculateTokenFrequenciesResponses, CalculateTokenFrequenciesErrors, ThrowOnError>({
    url: '/api/workspaces/token-frequencies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Token Frequencies Task Request
 *
 * Return stored request payload for a token-frequencies task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /token-frequencies/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a token-frequencies task" behavior.
 */
export const tokenFrequenciesTaskRequest = <ThrowOnError extends boolean = false>(options: Options<TokenFrequenciesTaskRequestData, ThrowOnError>): RequestResult<TokenFrequenciesTaskRequestResponses, TokenFrequenciesTaskRequestErrors, ThrowOnError> => (options.client ?? client).get<TokenFrequenciesTaskRequestResponses, TokenFrequenciesTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies/tasks/{task_id}/request', ...options });

/**
 * Token Frequencies Task Result
 *
 * Return normalized token-frequency result payload for one task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /token-frequencies/tasks/{task_id}/result route because they need this unit's "Return normalized token-frequency result payload for one task" behavior.
 */
export const tokenFrequenciesTaskResult = <ThrowOnError extends boolean = false>(options: Options<TokenFrequenciesTaskResultData, ThrowOnError>): RequestResult<TokenFrequenciesTaskResultResponses, TokenFrequenciesTaskResultErrors, ThrowOnError> => (options.client ?? client).get<TokenFrequenciesTaskResultResponses, TokenFrequenciesTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/token-frequencies/tasks/{task_id}/result', ...options });

/**
 * Update Token Frequencies Task Result
 *
 * Persist token-frequency preference overrides on an existing task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /token-frequencies/tasks/{task_id}/result route because they need this unit's "Persist token-frequency preference overrides on an existing task" behavior.
 */
export const updateTokenFrequenciesTaskResult = <ThrowOnError extends boolean = false>(options: Options<UpdateTokenFrequenciesTaskResultData, ThrowOnError>): RequestResult<UpdateTokenFrequenciesTaskResultResponses, UpdateTokenFrequenciesTaskResultErrors, ThrowOnError> => (options.client ?? client).post<UpdateTokenFrequenciesTaskResultResponses, UpdateTokenFrequenciesTaskResultErrors, ThrowOnError>({
    url: '/api/workspaces/token-frequencies/tasks/{task_id}/result',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Tokenizer Models
 *
 * Return available tokenizer model records.
 */
export const getTokenizerModels = <ThrowOnError extends boolean = false>(options?: Options<GetTokenizerModelsData, ThrowOnError>): RequestResult<GetTokenizerModelsResponses, GetTokenizerModelsErrors, ThrowOnError> => (options?.client ?? client).get<GetTokenizerModelsResponses, GetTokenizerModelsErrors, ThrowOnError>({ url: '/api/workspaces/tokenizer-models', ...options });

/**
 * Clear Topic Modeling Results
 *
 * Clear stored topic-modeling task state for a workspace.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend clear action: `DELETE /workspaces/{id}/topic-modeling` because they need this unit's "Clear stored topic-modeling task state for a workspace" behavior.
 *
 * Why:
 * - Removes explicit topic-modeling task records for broad legacy clear actions.
 */
export const clearTopicModelingResults = <ThrowOnError extends boolean = false>(options?: Options<ClearTopicModelingResultsData, ThrowOnError>): RequestResult<ClearTopicModelingResultsResponses, ClearTopicModelingResultsErrors, ThrowOnError> => (options?.client ?? client).delete<ClearTopicModelingResultsResponses, ClearTopicModelingResultsErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling', ...options });

/**
 * Run Topic Modeling
 *
 * Submit topic-modeling analysis as a worker-backed background task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend run route: `POST /workspaces/{id}/topic-modeling` because they need this unit's "Submit topic-modeling analysis as a worker-backed background task" behavior.
 *
 * Why:
 * - Offloads heavy modeling work to worker processes and returns `task_id`
 * for progress/result polling.
 */
export const runTopicModeling = <ThrowOnError extends boolean = false>(options: Options<RunTopicModelingData, ThrowOnError>): RequestResult<RunTopicModelingResponses, RunTopicModelingErrors, ThrowOnError> => (options.client ?? client).post<RunTopicModelingResponses, RunTopicModelingErrors, ThrowOnError>({
    url: '/api/workspaces/topic-modeling',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Detach Topic Modeling
 *
 * Create detached nodes from artifact-backed topic-modeling outputs.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend detach route: because they need this unit's "Create detached nodes from artifact-backed topic-modeling outputs" behavior.
 * `POST /workspaces/{id}/topic-modeling/tasks/{task_id}/detach`
 *
 * Why:
 * - Materializes user-selected columns and topic labels as reusable workspace
 * nodes without rerunning the model.
 */
export const detachTopicModeling = <ThrowOnError extends boolean = false>(options: Options<DetachTopicModelingData, ThrowOnError>): RequestResult<DetachTopicModelingResponses, DetachTopicModelingErrors, ThrowOnError> => (options.client ?? client).post<DetachTopicModelingResponses, DetachTopicModelingErrors, ThrowOnError>({
    url: '/api/workspaces/topic-modeling/tasks/{task_id}/detach',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Topic Modeling Detach Options
 *
 * List detachable node/column options for a completed topic task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend detach-options route: because they need this unit's "List detachable node/column options for a completed topic task" behavior.
 * `GET /workspaces/{id}/topic-modeling/tasks/{task_id}/detach-options`
 *
 * Why:
 * - Exposes artifact-backed node metadata so users can choose output columns safely.
 */
export const topicModelingDetachOptions = <ThrowOnError extends boolean = false>(options: Options<TopicModelingDetachOptionsData, ThrowOnError>): RequestResult<TopicModelingDetachOptionsResponses, TopicModelingDetachOptionsErrors, ThrowOnError> => (options.client ?? client).get<TopicModelingDetachOptionsResponses, TopicModelingDetachOptionsErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/{task_id}/detach-options', ...options });

/**
 * Topic Modeling Task Request
 *
 * Return stored request payload for a topic-modeling task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /topic-modeling/tasks/{task_id}/request route because they need this unit's "Return stored request payload for a topic-modeling task" behavior.
 */
export const topicModelingTaskRequest = <ThrowOnError extends boolean = false>(options: Options<TopicModelingTaskRequestData, ThrowOnError>): RequestResult<TopicModelingTaskRequestResponses, TopicModelingTaskRequestErrors, ThrowOnError> => (options.client ?? client).get<TopicModelingTaskRequestResponses, TopicModelingTaskRequestErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/{task_id}/request', ...options });

/**
 * Topic Modeling Task Result
 *
 * Return current status or final payload for a topic-modeling task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend polling route: because they need this unit's "Return current status or final payload for a topic-modeling task" behavior.
 * `GET /workspaces/{id}/topic-modeling/tasks/{task_id}/result`
 *
 * Why:
 * - Normalizes task lifecycle states into one response contract for UI polling.
 */
export const topicModelingTaskResult = <ThrowOnError extends boolean = false>(options: Options<TopicModelingTaskResultData, ThrowOnError>): RequestResult<TopicModelingTaskResultResponses, TopicModelingTaskResultErrors, ThrowOnError> => (options.client ?? client).get<TopicModelingTaskResultResponses, TopicModelingTaskResultErrors, ThrowOnError>({ url: '/api/workspaces/topic-modeling/tasks/{task_id}/result', ...options });

/**
 * Unload Workspace
 *
 * Handle unload workspace API requests for workspace lifecycle routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /unload route because they need this unit's "Handle unload workspace API requests for workspace lifecycle routes" behavior.
 */
export const unloadWorkspace = <ThrowOnError extends boolean = false>(options?: Options<UnloadWorkspaceData, ThrowOnError>): RequestResult<UnloadWorkspaceResponses, UnloadWorkspaceErrors, ThrowOnError> => (options?.client ?? client).post<UnloadWorkspaceResponses, UnloadWorkspaceErrors, ThrowOnError>({ url: '/api/workspaces/unload', ...options });

/**
 * Upload Workspace Zip
 *
 * Upload a workspace ZIP archive and import it into user workspace storage.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /upload route because they need this unit's "Upload a workspace ZIP archive and import it into user workspace storage" behavior.
 */
export const uploadWorkspaceZip = <ThrowOnError extends boolean = false>(options: Options<UploadWorkspaceZipData, ThrowOnError>): RequestResult<UploadWorkspaceZipResponses, UploadWorkspaceZipErrors, ThrowOnError> => (options.client ?? client).post<UploadWorkspaceZipResponses, UploadWorkspaceZipErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/api/workspaces/upload',
    ...options,
    headers: {
        'Content-Type': null,
        ...options.headers
    }
});

/**
 * Get Workspace Tabs
 *
 * Return the persisted analysis-tab state for a workspace.
 *
 * Used by the frontend tab store on view entry to restore tabs (and their
 * task ids) after a reload. Returns the empty default when no sidecar exists.
 */
export const getWorkspaceTabs = <ThrowOnError extends boolean = false>(options: Options<GetWorkspaceTabsData, ThrowOnError>): RequestResult<GetWorkspaceTabsResponses, GetWorkspaceTabsErrors, ThrowOnError> => (options.client ?? client).get<GetWorkspaceTabsResponses, GetWorkspaceTabsErrors, ThrowOnError>({ url: '/api/workspaces/{workspace_id}/tabs', ...options });

/**
 * Put Workspace Tabs
 *
 * Replace the persisted analysis-tab state for a workspace.
 *
 * Used by the frontend tab store whenever tabs are created, closed, renamed,
 * reordered, activated, or wired to a new task id. Full-replacement semantics
 * write the whole tabs.json sidecar on every change.
 */
export const putWorkspaceTabs = <ThrowOnError extends boolean = false>(options: Options<PutWorkspaceTabsData, ThrowOnError>): RequestResult<PutWorkspaceTabsResponses, PutWorkspaceTabsErrors, ThrowOnError> => (options.client ?? client).put<PutWorkspaceTabsResponses, PutWorkspaceTabsErrors, ThrowOnError>({
    url: '/api/workspaces/{workspace_id}/tabs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Health Check
 *
 * Return lightweight health status for liveness probes.
 *
 * Used by:
 * - deployment health checks and uptime monitors because callers need the shared shared
 * backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Gives a fast no-auth probe endpoint for runtime readiness checks.
 */
export const healthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthCheckData, ThrowOnError>): RequestResult<HealthCheckResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HealthCheckResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * Status
 *
 * Return detailed component/module status information.
 *
 * Used by:
 * - diagnostics pages and manual troubleshooting because callers need the shared shared
 * backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Exposes richer operational metadata than ``/health``.
 */
export const status = <ThrowOnError extends boolean = false>(options?: Options<StatusData, ThrowOnError>): RequestResult<StatusResponses, unknown, ThrowOnError> => (options?.client ?? client).get<StatusResponses, unknown, ThrowOnError>({ url: '/status', ...options });
