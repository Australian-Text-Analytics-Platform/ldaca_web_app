// This file is auto-generated by @hey-api/openapi-ts

import { type InfiniteData, infiniteQueryOptions, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { addNodeToWorkspace, adminCleanup, analysisTaskDetachOptions, analysisTaskDispersionBins, analysisTaskPreferences, analysisTaskRequest, analysisTaskResult, analysisTaskResultQuery, annotateAiAll, annotateAiPreview, annotateAiPreviewClear, annotateAiPreviewOverride, annotateAiPreviewState, authHealth, authStatus, calculateTokenFrequencies, cancelTask, castNode, cilogonCallback, cilogonLogin, clearFilesTasks, clearTask, clearTopicModelingResults, cloneNode, columnOperations, concatNodes, concatNodesPreview, createAnalysisTaskDetachment, createAnalysisTaskDispersionDetachment, createAnalysisTaskMaterialization, createAnnotationClassDescriptions, createAnnotationColumn, createFolder, createWorkspace, deleteFile, deleteNode, deleteNodeColumn, deleteWorkspaceById, describeColumn, detachAiPreviewedRows, downloadFile, downloadWorkspaceArtifact, exportNodes, filterNode, filterPreview, getAnnotationClassDescriptions, getAuthInfo, getColumnUniqueValues, getCurrentUserInfo, getFileInfo, getMyCurrentWorkspace, getNodeDataByWorkspaceId, getNodeQueryPlan, getNodeShape, getPreferences, getQuotation, getRawFile, getRuntimeConfig, getSampleDataCatalogue, getSampleDataReadme, getTokenizerModels, getUserFiles, getWorkspaceById, getWorkspaceGraphById, getWorkspaceNodesInfoById, getWorkspaceTabs, googleAuth, googleAuthCallback, healthCheck, importLdacaDataset, importSampleData, joinNodes, joinNodesPreview, listAnnotationAiModels, listFilesTasks, listLdacaFeaturedCollections, listTasks, listUsers, listWorkspaces, logout, moveFile, type Options, polarsExpressionApply, polarsExpressionPreview, previewSequentialAnalysis, putWorkspaceTabs, redoNodeOperation, renameNodeColumn, reorderWorkspaceNodesById, replaceApply, replacePreview, root, runConcordance, runSequentialAnalysis, runTopicModeling, saveWorkspaceById, searchLdacaCollections, setAnnotationCell, setAnnotationClassParent, setMyCurrentWorkspace, setNodeColor, setNodeDocumentColumn, setNodeTokenizationPreference, sliceNode, slicePreview, startWorkspaceDownload, status, undoNodeOperation, unifiedFilePreview, unloadWorkspace, updateAdminConfig, updateAnnotationClassDescriptions, updateNodeName, updatePreferences, updateWorkspaceById, uploadFile, uploadWorkspaceZip } from '../sdk.gen';
import type { AddNodeToWorkspaceData, AddNodeToWorkspaceError, AddNodeToWorkspaceResponse, AdminCleanupData, AdminCleanupError, AdminCleanupResponse2, AnalysisTaskDetachOptionsData, AnalysisTaskDetachOptionsError, AnalysisTaskDetachOptionsResponse, AnalysisTaskDispersionBinsData, AnalysisTaskDispersionBinsError, AnalysisTaskDispersionBinsResponse, AnalysisTaskPreferencesData, AnalysisTaskPreferencesError, AnalysisTaskRequestData, AnalysisTaskRequestError, AnalysisTaskResultData, AnalysisTaskResultError, AnalysisTaskResultQueryData, AnalysisTaskResultQueryError, AnnotateAiAllData, AnnotateAiAllError, AnnotateAiAllResponse, AnnotateAiPreviewClearData, AnnotateAiPreviewClearError, AnnotateAiPreviewClearResponse, AnnotateAiPreviewData, AnnotateAiPreviewError, AnnotateAiPreviewOverrideData, AnnotateAiPreviewOverrideError, AnnotateAiPreviewOverrideResponse, AnnotateAiPreviewResponse, AnnotateAiPreviewStateData, AnnotateAiPreviewStateError, AnnotateAiPreviewStateResponse, AuthHealthData, AuthHealthError, AuthHealthResponse2, AuthStatusData, AuthStatusError, AuthStatusResponse2, CalculateTokenFrequenciesData, CalculateTokenFrequenciesError, CalculateTokenFrequenciesResponse, CancelTaskData, CancelTaskError, CancelTaskResponse, CastNodeData, CastNodeError, CastNodeResponse2, CilogonCallbackData, CilogonCallbackError, CilogonLoginData, CilogonLoginError, ClearFilesTasksData, ClearFilesTasksError, ClearFilesTasksResponse, ClearTaskData, ClearTaskError, ClearTaskResponse, ClearTopicModelingResultsData, ClearTopicModelingResultsError, ClearTopicModelingResultsResponse, CloneNodeData, CloneNodeError, CloneNodeResponse, ColumnOperationsData, ColumnOperationsError, ColumnOperationsResponse2, ConcatNodesData, ConcatNodesError, ConcatNodesPreviewData, ConcatNodesPreviewError, ConcatNodesPreviewResponse, ConcatNodesResponse, CreateAnalysisTaskDetachmentData, CreateAnalysisTaskDetachmentError, CreateAnalysisTaskDetachmentResponse, CreateAnalysisTaskDispersionDetachmentData, CreateAnalysisTaskDispersionDetachmentError, CreateAnalysisTaskDispersionDetachmentResponse, CreateAnalysisTaskMaterializationData, CreateAnalysisTaskMaterializationError, CreateAnalysisTaskMaterializationResponse, CreateAnnotationClassDescriptionsData, CreateAnnotationClassDescriptionsError, CreateAnnotationClassDescriptionsResponse, CreateAnnotationColumnData, CreateAnnotationColumnError, CreateAnnotationColumnResponse, CreateFolderData, CreateFolderError, CreateFolderResponse2, CreateWorkspaceData, CreateWorkspaceError, CreateWorkspaceResponse, DeleteFileData, DeleteFileError, DeleteFileResponse, DeleteNodeColumnData, DeleteNodeColumnError, DeleteNodeColumnResponse, DeleteNodeData, DeleteNodeError, DeleteNodeResponse, DeleteWorkspaceByIdData, DeleteWorkspaceByIdError, DeleteWorkspaceByIdResponse, DescribeColumnData, DescribeColumnError, DescribeColumnResponse, DetachAiPreviewedRowsData, DetachAiPreviewedRowsError, DetachAiPreviewedRowsResponse, DownloadFileData, DownloadFileError, DownloadFileResponse, DownloadWorkspaceArtifactData, DownloadWorkspaceArtifactError, DownloadWorkspaceArtifactResponse, ExportNodesData, ExportNodesError, ExportNodesResponse, FilterNodeData, FilterNodeError, FilterNodeResponse, FilterPreviewData, FilterPreviewError, FilterPreviewResponse2, GetAnnotationClassDescriptionsData, GetAnnotationClassDescriptionsError, GetAnnotationClassDescriptionsResponse, GetAuthInfoData, GetAuthInfoError, GetAuthInfoResponse, GetColumnUniqueValuesData, GetColumnUniqueValuesError, GetColumnUniqueValuesResponse, GetCurrentUserInfoData, GetCurrentUserInfoError, GetCurrentUserInfoResponse, GetFileInfoData, GetFileInfoError, GetFileInfoResponse, GetMyCurrentWorkspaceData, GetMyCurrentWorkspaceError, GetMyCurrentWorkspaceResponse, GetNodeDataByWorkspaceIdData, GetNodeDataByWorkspaceIdError, GetNodeDataByWorkspaceIdResponse, GetNodeQueryPlanData, GetNodeQueryPlanError, GetNodeQueryPlanResponse, GetNodeShapeData, GetNodeShapeError, GetNodeShapeResponse, GetPreferencesData, GetPreferencesError, GetPreferencesResponse, GetQuotationData, GetQuotationError, GetQuotationResponse, GetRawFileData, GetRawFileError, GetRawFileResponse, GetRuntimeConfigData, GetRuntimeConfigError, GetRuntimeConfigResponse, GetSampleDataCatalogueData, GetSampleDataCatalogueError, GetSampleDataCatalogueResponse, GetSampleDataReadmeData, GetSampleDataReadmeError, GetSampleDataReadmeResponse, GetTokenizerModelsData, GetTokenizerModelsError, GetTokenizerModelsResponse, GetUserFilesData, GetUserFilesError, GetUserFilesResponse, GetWorkspaceByIdData, GetWorkspaceByIdError, GetWorkspaceByIdResponse, GetWorkspaceGraphByIdData, GetWorkspaceGraphByIdError, GetWorkspaceGraphByIdResponse, GetWorkspaceNodesInfoByIdData, GetWorkspaceNodesInfoByIdError, GetWorkspaceNodesInfoByIdResponse, GetWorkspaceTabsData, GetWorkspaceTabsError, GetWorkspaceTabsResponse, GoogleAuthCallbackData, GoogleAuthCallbackError, GoogleAuthData, GoogleAuthError, GoogleAuthResponse, HealthCheckData, HealthCheckError, ImportLdacaDatasetData, ImportLdacaDatasetError, ImportLdacaDatasetResponse, ImportSampleDataData, ImportSampleDataError, ImportSampleDataResponse2, JoinNodesData, JoinNodesError, JoinNodesPreviewData, JoinNodesPreviewError, JoinNodesPreviewResponse, JoinNodesResponse, ListAnnotationAiModelsData, ListAnnotationAiModelsError, ListAnnotationAiModelsResponse, ListFilesTasksData, ListFilesTasksError, ListFilesTasksResponse, ListLdacaFeaturedCollectionsData, ListLdacaFeaturedCollectionsError, ListLdacaFeaturedCollectionsResponse, ListTasksData, ListTasksError, ListTasksResponse, ListUsersData, ListUsersError, ListUsersResponse, ListWorkspacesData, ListWorkspacesError, ListWorkspacesResponse, LogoutData, LogoutError, LogoutResponse, MoveFileData, MoveFileError, MoveFileResponse, PolarsExpressionApplyData, PolarsExpressionApplyError, PolarsExpressionApplyResponse2, PolarsExpressionPreviewData, PolarsExpressionPreviewError, PolarsExpressionPreviewResponse, PreviewSequentialAnalysisData, PreviewSequentialAnalysisError, PreviewSequentialAnalysisResponse, PutWorkspaceTabsData, PutWorkspaceTabsError, PutWorkspaceTabsResponse, RedoNodeOperationData, RedoNodeOperationError, RedoNodeOperationResponse, RenameNodeColumnData, RenameNodeColumnError, RenameNodeColumnResponse, ReorderWorkspaceNodesByIdData, ReorderWorkspaceNodesByIdError, ReorderWorkspaceNodesByIdResponse, ReplaceApplyData, ReplaceApplyError, ReplaceApplyResponse2, ReplacePreviewData, ReplacePreviewError, ReplacePreviewResponse, RootData, RootError, RootResponse2, RunConcordanceData, RunConcordanceError, RunConcordanceResponse, RunSequentialAnalysisData, RunSequentialAnalysisError, RunSequentialAnalysisResponse, RunTopicModelingData, RunTopicModelingError, RunTopicModelingResponse, SaveWorkspaceByIdData, SaveWorkspaceByIdError, SaveWorkspaceByIdResponse, SearchLdacaCollectionsData, SearchLdacaCollectionsError, SearchLdacaCollectionsResponse, SetAnnotationCellData, SetAnnotationCellError, SetAnnotationCellResponse, SetAnnotationClassParentData, SetAnnotationClassParentError, SetAnnotationClassParentResponse, SetMyCurrentWorkspaceData, SetMyCurrentWorkspaceError, SetMyCurrentWorkspaceResponse, SetNodeColorData, SetNodeColorError, SetNodeColorResponse, SetNodeDocumentColumnData, SetNodeDocumentColumnError, SetNodeDocumentColumnResponse, SetNodeTokenizationPreferenceData, SetNodeTokenizationPreferenceError, SetNodeTokenizationPreferenceResponse, SliceNodeData, SliceNodeError, SliceNodeResponse, SlicePreviewData, SlicePreviewError, SlicePreviewResponse, StartWorkspaceDownloadData, StartWorkspaceDownloadError, StartWorkspaceDownloadResponse, StatusData, StatusError, UndoNodeOperationData, UndoNodeOperationError, UndoNodeOperationResponse, UnifiedFilePreviewData, UnifiedFilePreviewError, UnifiedFilePreviewResponse, UnloadWorkspaceData, UnloadWorkspaceError, UnloadWorkspaceResponse, UpdateAdminConfigData, UpdateAdminConfigError, UpdateAdminConfigResponse, UpdateAnnotationClassDescriptionsData, UpdateAnnotationClassDescriptionsError, UpdateAnnotationClassDescriptionsResponse, UpdateNodeNameData, UpdateNodeNameError, UpdateNodeNameResponse, UpdatePreferencesData, UpdatePreferencesError, UpdatePreferencesResponse, UpdateWorkspaceByIdData, UpdateWorkspaceByIdError, UpdateWorkspaceByIdResponse, UploadFileData, UploadFileError, UploadFileResponse, UploadWorkspaceZipData, UploadWorkspaceZipError, UploadWorkspaceZipResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const rootQueryKey = (options?: Options<RootData>) => createQueryKey('root', options);

/**
 * Root
 *
 * Return API feature/index metadata.
 *
 * Used by:
 * - browser/manual root checks and basic service discovery because callers need the shared
 * shared backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Provides a human-readable entrypoint summary for backend capabilities.
 */
export const rootOptions = (options?: Options<RootData>) => queryOptions<RootResponse2, RootError, RootResponse2, ReturnType<typeof rootQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await root({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: rootQueryKey(options)
});

export const adminCleanupQueryKey = (options?: Options<AdminCleanupData>) => createQueryKey('adminCleanup', options, false, ['admin']);

/**
 * Admin Cleanup
 *
 * Trigger cleanup of expired session rows.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - admin maintenance actions because they need this unit's "Trigger cleanup of expired session rows" behavior.
 *
 * Why:
 * - Allows manual session-store maintenance in addition to automatic cleanup.
 *
 * Refactor note:
 * - Add `require_admin` dependency before wider deployment.
 */
export const adminCleanupOptions = (options?: Options<AdminCleanupData>) => queryOptions<AdminCleanupResponse2, AdminCleanupError, AdminCleanupResponse2, ReturnType<typeof adminCleanupQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await adminCleanup({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: adminCleanupQueryKey(options)
});

/**
 * Update Admin Config
 *
 * Update process-local runtime configuration.
 *
 * Used by:
 * - frontend working-directory settings because changing the data root affects
 * file/workspace state and must stay behind the admin boundary.
 *
 * Flow:
 * - Authenticate and authorize the caller through the shared admin allowlist.
 * - Store the requested data root in the process environment.
 * - Reload the settings singleton so existing import sites observe the new
 * process-local value, then return the effective admin config snapshot.
 */
export const updateAdminConfigMutation = (options?: Partial<Options<UpdateAdminConfigData>>): UseMutationOptions<UpdateAdminConfigResponse, UpdateAdminConfigError, Options<UpdateAdminConfigData>> => {
    const mutationOptions: UseMutationOptions<UpdateAdminConfigResponse, UpdateAdminConfigError, Options<UpdateAdminConfigData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await updateAdminConfig({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const listUsersQueryKey = (options?: Options<ListUsersData>) => createQueryKey('listUsers', options, false, ['admin']);

/**
 * List Users
 *
 * List users with active-session counts.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - admin dashboard/user management views because they need this unit's "List users with active-session counts" behavior.
 *
 * Why:
 * - Provides operational visibility into user and session activity.
 *
 * Refactor note:
 * - Add `require_admin` dependency before wider deployment to avoid role drift.
 */
export const listUsersOptions = (options?: Options<ListUsersData>) => queryOptions<ListUsersResponse, ListUsersError, ListUsersResponse, ReturnType<typeof listUsersQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listUsers({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listUsersQueryKey(options)
});

export const getAuthInfoQueryKey = (options?: Options<GetAuthInfoData>) => createQueryKey('getAuthInfo', options, false, ['authentication']);

/**
 * Get Auth Info
 *
 * Main auth endpoint - tells frontend everything it needs to know.
 *
 * Returns:
 * - In single-user mode: authenticated=True with root user info
 * - In multi-user mode with valid token: authenticated=True with user info
 * - In multi-user mode without token: authenticated=False with available auth methods
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend app bootstrap auth probe because they need this unit's "Main auth endpoint - tells frontend everything it needs to know" behavior.
 *
 * Why:
 * - Provides one canonical auth capability + identity payload per startup.
 */
export const getAuthInfoOptions = (options?: Options<GetAuthInfoData>) => queryOptions<GetAuthInfoResponse, GetAuthInfoError, GetAuthInfoResponse, ReturnType<typeof getAuthInfoQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getAuthInfo({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getAuthInfoQueryKey(options)
});

export const cilogonCallbackQueryKey = (options?: Options<CilogonCallbackData>) => createQueryKey('cilogonCallback', options, false, ['authentication']);

/**
 * Cilogon Callback
 *
 * Handle the CILogon authorization code callback.
 *
 * CILogon redirects here after the user authenticates. We verify the CSRF
 * ``state``, exchange the authorization code for tokens, fetch the user's
 * profile from the userinfo endpoint, provision a local session, and
 * redirect back to the SPA with the access token in the URL query string.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /cilogon/callback route because they need this unit's "Handle the CILogon authorization code callback" behavior.
 */
export const cilogonCallbackOptions = (options?: Options<CilogonCallbackData>) => queryOptions<unknown, CilogonCallbackError, unknown, ReturnType<typeof cilogonCallbackQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cilogonCallback({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cilogonCallbackQueryKey(options)
});

export const cilogonLoginQueryKey = (options?: Options<CilogonLoginData>) => createQueryKey('cilogonLogin', options, false, ['authentication']);

/**
 * Cilogon Login
 *
 * Redirect the browser to the CILogon authorization endpoint.
 *
 * Generates a random ``state`` for CSRF protection (stored as a short-lived
 * cookie), builds the OIDC authorization URL from the discovery document,
 * and redirects the user to CILogon to authenticate.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /cilogon/login route because they need this unit's "Redirect the browser to the CILogon authorization endpoint" behavior.
 */
export const cilogonLoginOptions = (options?: Options<CilogonLoginData>) => queryOptions<unknown, CilogonLoginError, unknown, ReturnType<typeof cilogonLoginQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await cilogonLogin({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: cilogonLoginQueryKey(options)
});

/**
 * Google Auth
 *
 * Authenticate user via Google OAuth and create app session tokens.
 *
 * Used by the frontend Google sign-in flow (JSON body variant). Delegates
 * the verification + provisioning logic to :func:`_verify_and_create_session`
 * and shapes the response into ``GoogleOut``.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /google route because they need this unit's "Authenticate user via Google OAuth and create app session tokens" behavior.
 */
export const googleAuthMutation = (options?: Partial<Options<GoogleAuthData>>): UseMutationOptions<GoogleAuthResponse, GoogleAuthError, Options<GoogleAuthData>> => {
    const mutationOptions: UseMutationOptions<GoogleAuthResponse, GoogleAuthError, Options<GoogleAuthData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleAuth({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Google Auth Callback
 *
 * Handle the Google Identity Services redirect callback.
 *
 * Google POSTs the ID-token ``credential`` and a CSRF token here after the
 * user authenticates on Google's consent page (``ux_mode: 'redirect'``).
 * We verify the token, create/find the local user, issue a session, and
 * redirect back to the SPA with the access token in the URL fragment.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /google/callback route because they need this unit's "Handle the Google Identity Services redirect callback" behavior.
 */
export const googleAuthCallbackMutation = (options?: Partial<Options<GoogleAuthCallbackData>>): UseMutationOptions<unknown, GoogleAuthCallbackError, Options<GoogleAuthCallbackData>> => {
    const mutationOptions: UseMutationOptions<unknown, GoogleAuthCallbackError, Options<GoogleAuthCallbackData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleAuthCallback({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const authHealthQueryKey = (options?: Options<AuthHealthData>) => createQueryKey('authHealth', options, false, ['authentication']);

/**
 * Auth Health
 *
 * Return authentication subsystem readiness metadata.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - health/status probes and diagnostics pages because they need this unit's "Return authentication subsystem readiness metadata" behavior.
 *
 * Why:
 * - Exposes auth mode and endpoint availability without authentication.
 */
export const authHealthOptions = (options?: Options<AuthHealthData>) => queryOptions<AuthHealthResponse2, AuthHealthError, AuthHealthResponse2, ReturnType<typeof authHealthQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await authHealth({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: authHealthQueryKey(options)
});

/**
 * Logout
 *
 * Logout current user session (multi-user) or no-op (single-user).
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend sign-out action because they need this unit's "Logout current user session (multi-user) or no-op (single-user)" behavior.
 *
 * Why:
 * - Keeps logout behavior mode-aware while preserving shared endpoint contract.
 */
export const logoutMutation = (options?: Partial<Options<LogoutData>>): UseMutationOptions<LogoutResponse, LogoutError, Options<LogoutData>> => {
    const mutationOptions: UseMutationOptions<LogoutResponse, LogoutError, Options<LogoutData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await logout({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getCurrentUserInfoQueryKey = (options?: Options<GetCurrentUserInfoData>) => createQueryKey('getCurrentUserInfo', options, false, ['authentication']);

/**
 * Get Current User Info
 *
 * Return normalized current user profile fields.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend profile/session widgets because they need this unit's "Return normalized current user profile fields" behavior.
 *
 * Why:
 * - Provides stable user response shape independent of DB row types.
 */
export const getCurrentUserInfoOptions = (options?: Options<GetCurrentUserInfoData>) => queryOptions<GetCurrentUserInfoResponse, GetCurrentUserInfoError, GetCurrentUserInfoResponse, ReturnType<typeof getCurrentUserInfoQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getCurrentUserInfo({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getCurrentUserInfoQueryKey(options)
});

export const authStatusQueryKey = (options?: Options<AuthStatusData>) => createQueryKey('authStatus', options, false, ['authentication']);

/**
 * Auth Status
 *
 * Return minimal authenticated status payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - lightweight frontend auth status checks because they need this unit's "Return minimal authenticated status payload" behavior.
 *
 * Why:
 * - Allows cheap auth verification without full `get_auth_info` metadata.
 */
export const authStatusOptions = (options?: Options<AuthStatusData>) => queryOptions<AuthStatusResponse2, AuthStatusError, AuthStatusResponse2, ReturnType<typeof authStatusQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await authStatus({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: authStatusQueryKey(options)
});

/**
 * Delete File
 *
 * Delete a file or directory inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file browser delete actions because path-bearing file operations
 * use the same query-parameter contract as raw-content reads.
 */
export const deleteFileMutation = (options?: Partial<Options<DeleteFileData>>): UseMutationOptions<DeleteFileResponse, DeleteFileError, Options<DeleteFileData>> => {
    const mutationOptions: UseMutationOptions<DeleteFileResponse, DeleteFileError, Options<DeleteFileData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteFile({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getUserFilesQueryKey = (options?: Options<GetUserFilesData>) => createQueryKey('getUserFiles', options, false, ['files']);

/**
 * Get User Files
 *
 * List user-visible files as a nested tree.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file browser panel.
 *
 * Why:
 * - The frontend consumes the directory structure directly instead of
 * reconstructing it from a flat file listing.
 */
export const getUserFilesOptions = (options?: Options<GetUserFilesData>) => queryOptions<GetUserFilesResponse, GetUserFilesError, GetUserFilesResponse, ReturnType<typeof getUserFilesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getUserFiles({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getUserFilesQueryKey(options)
});

export const downloadFileQueryKey = (options: Options<DownloadFileData>) => createQueryKey('downloadFile', options, false, ['files']);

/**
 * Download File
 *
 * Download user's file.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file download actions because path-bearing file reads should use
 * one query-parameter contract instead of catch-all routes.
 */
export const downloadFileOptions = (options: Options<DownloadFileData>) => queryOptions<DownloadFileResponse, DownloadFileError, DownloadFileResponse, ReturnType<typeof downloadFileQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await downloadFile({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: downloadFileQueryKey(options)
});

/**
 * Create Folder
 *
 * Create a folder inside the user's data directory.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /folders route.
 */
export const createFolderMutation = (options?: Partial<Options<CreateFolderData>>): UseMutationOptions<CreateFolderResponse2, CreateFolderError, Options<CreateFolderData>> => {
    const mutationOptions: UseMutationOptions<CreateFolderResponse2, CreateFolderError, Options<CreateFolderData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createFolder({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Import Ldaca Dataset
 *
 * Submit background task to import LDaCA dataset from URL.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend dataset import action.
 *
 * Why:
 * - Runs network/download/import pipeline outside request-response lifecycle.
 */
export const importLdacaDatasetMutation = (options?: Partial<Options<ImportLdacaDatasetData>>): UseMutationOptions<ImportLdacaDatasetResponse, ImportLdacaDatasetError, Options<ImportLdacaDatasetData>> => {
    const mutationOptions: UseMutationOptions<ImportLdacaDatasetResponse, ImportLdacaDatasetError, Options<ImportLdacaDatasetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await importLdacaDataset({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Import Sample Data
 *
 * Import (or re-import) sample data for the current user on demand.
 *
 * Copies bundled datasets (SCL, ADO/twitter) immediately, then downloads any
 * missing or updated remote datasets if SAMPLE_DATA_REMOTE_URL is configured.
 *
 * If request.collection_ids is non-empty, only those collections are
 * downloaded remotely. An empty list means "download all non-bundled
 * collections" (default).
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /import-sample-data route.
 */
export const importSampleDataMutation = (options?: Partial<Options<ImportSampleDataData>>): UseMutationOptions<ImportSampleDataResponse2, ImportSampleDataError, Options<ImportSampleDataData>> => {
    const mutationOptions: UseMutationOptions<ImportSampleDataResponse2, ImportSampleDataError, Options<ImportSampleDataData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await importSampleData({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getFileInfoQueryKey = (options: Options<GetFileInfoData>) => createQueryKey('getFileInfo', options, false, ['files']);

/**
 * Get File Info
 *
 * Get detailed file information.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file detail consumers because path-bearing file reads should use
 * one query-parameter contract instead of catch-all routes.
 */
export const getFileInfoOptions = (options: Options<GetFileInfoData>) => queryOptions<GetFileInfoResponse, GetFileInfoError, GetFileInfoResponse, ReturnType<typeof getFileInfoQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getFileInfo({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getFileInfoQueryKey(options)
});

export const listLdacaFeaturedCollectionsQueryKey = (options?: Options<ListLdacaFeaturedCollectionsData>) => createQueryKey('listLdacaFeaturedCollections', options, false, ['files']);

/**
 * List Ldaca Featured Collections
 *
 * Return staff-picked LDaCA collections for the import dialog.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /ldaca/featured route.
 */
export const listLdacaFeaturedCollectionsOptions = (options?: Options<ListLdacaFeaturedCollectionsData>) => queryOptions<ListLdacaFeaturedCollectionsResponse, ListLdacaFeaturedCollectionsError, ListLdacaFeaturedCollectionsResponse, ReturnType<typeof listLdacaFeaturedCollectionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listLdacaFeaturedCollections({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listLdacaFeaturedCollectionsQueryKey(options)
});

/**
 * Search Ldaca Collections
 *
 * Search LDaCA records through the backend ONI proxy.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /ldaca/search route.
 */
export const searchLdacaCollectionsMutation = (options?: Partial<Options<SearchLdacaCollectionsData>>): UseMutationOptions<SearchLdacaCollectionsResponse, SearchLdacaCollectionsError, Options<SearchLdacaCollectionsData>> => {
    const mutationOptions: UseMutationOptions<SearchLdacaCollectionsResponse, SearchLdacaCollectionsError, Options<SearchLdacaCollectionsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await searchLdacaCollections({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Move File
 *
 * Move a file into another directory inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /move route.
 */
export const moveFileMutation = (options?: Partial<Options<MoveFileData>>): UseMutationOptions<MoveFileResponse, MoveFileError, Options<MoveFileData>> => {
    const mutationOptions: UseMutationOptions<MoveFileResponse, MoveFileError, Options<MoveFileData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await moveFile({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Unified File Preview
 *
 * Unified file preview endpoint.
 *
 * - Returns supported types based on extension.
 * - Provides preview data (first few rows or page slice).
 * - For Excel files, returns sheet_names and supports selecting sheet via
 * payload.sheet_name.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend file preview modal/table.
 *
 * Why:
 * - Provides one format-aware preview API for heterogeneous file types.
 */
export const unifiedFilePreviewMutation = (options?: Partial<Options<UnifiedFilePreviewData>>): UseMutationOptions<UnifiedFilePreviewResponse, UnifiedFilePreviewError, Options<UnifiedFilePreviewData>> => {
    const mutationOptions: UseMutationOptions<UnifiedFilePreviewResponse, UnifiedFilePreviewError, Options<UnifiedFilePreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await unifiedFilePreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getRawFileQueryKey = (options: Options<GetRawFileData>) => createQueryKey('getRawFile', options, false, ['files']);

/**
 * Get Raw File
 *
 * Return raw UTF-8 text content for a file inside the user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /raw route.
 */
export const getRawFileOptions = (options: Options<GetRawFileData>) => queryOptions<GetRawFileResponse, GetRawFileError, GetRawFileResponse, ReturnType<typeof getRawFileQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRawFile({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRawFileQueryKey(options)
});

export const getSampleDataCatalogueQueryKey = (options?: Options<GetSampleDataCatalogueData>) => createQueryKey('getSampleDataCatalogue', options, false, ['files']);

/**
 * Get Sample Data Catalogue
 *
 * Return the sample data catalogue augmented with per-collection status.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sample-data/catalogue route.
 */
export const getSampleDataCatalogueOptions = (options?: Options<GetSampleDataCatalogueData>) => queryOptions<GetSampleDataCatalogueResponse, GetSampleDataCatalogueError, GetSampleDataCatalogueResponse, ReturnType<typeof getSampleDataCatalogueQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getSampleDataCatalogue({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getSampleDataCatalogueQueryKey(options)
});

export const getSampleDataReadmeQueryKey = (options: Options<GetSampleDataReadmeData>) => createQueryKey('getSampleDataReadme', options, false, ['files']);

/**
 * Get Sample Data Readme
 *
 * Proxy a README.md from the remote sample data repository.
 *
 * Only .md files are permitted; path traversal is rejected.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /sample-data/readme route.
 */
export const getSampleDataReadmeOptions = (options: Options<GetSampleDataReadmeData>) => queryOptions<GetSampleDataReadmeResponse, GetSampleDataReadmeError, GetSampleDataReadmeResponse, ReturnType<typeof getSampleDataReadmeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getSampleDataReadme({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getSampleDataReadmeQueryKey(options)
});

export const listFilesTasksQueryKey = (options?: Options<ListFilesTasksData>) => createQueryKey('listFilesTasks', options, false, ['files']);

/**
 * List Files Tasks
 *
 * List file-import worker tasks exposed via the files API.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend import-task status polling.
 *
 * Why:
 * - Exposes file-import task status via explicit task types.
 */
export const listFilesTasksOptions = (options?: Options<ListFilesTasksData>) => queryOptions<ListFilesTasksResponse, ListFilesTasksError, ListFilesTasksResponse, ReturnType<typeof listFilesTasksQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listFilesTasks({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listFilesTasksQueryKey(options)
});

/**
 * Clear Files Tasks
 *
 * Clear persisted file-import task records.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend task-list cleanup actions.
 *
 * Why:
 * - Removes completed/failed import task clutter while keeping artifacts.
 */
export const clearFilesTasksMutation = (options?: Partial<Options<ClearFilesTasksData>>): UseMutationOptions<ClearFilesTasksResponse, ClearFilesTasksError, Options<ClearFilesTasksData>> => {
    const mutationOptions: UseMutationOptions<ClearFilesTasksResponse, ClearFilesTasksError, Options<ClearFilesTasksData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await clearFilesTasks({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Upload File
 *
 * Upload file to user's data folder.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the
 * owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /upload route.
 */
export const uploadFileMutation = (options?: Partial<Options<UploadFileData>>): UseMutationOptions<UploadFileResponse, UploadFileError, Options<UploadFileData>> => {
    const mutationOptions: UseMutationOptions<UploadFileResponse, UploadFileError, Options<UploadFileData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await uploadFile({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getPreferencesQueryKey = (options?: Options<GetPreferencesData>) => createQueryKey('getPreferences', options, false, ['preferences']);

/**
 * Get Preferences
 *
 * Return get preferences API requests for preference routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET / route because they need this unit's "Return get preferences API requests for preference routes" behavior.
 */
export const getPreferencesOptions = (options?: Options<GetPreferencesData>) => queryOptions<GetPreferencesResponse, GetPreferencesError, GetPreferencesResponse, ReturnType<typeof getPreferencesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getPreferences({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getPreferencesQueryKey(options)
});

/**
 * Update Preferences
 *
 * Update update preferences API requests for preference routes.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT / route because they need this unit's "Update update preferences API requests for preference routes" behavior.
 */
export const updatePreferencesMutation = (options?: Partial<Options<UpdatePreferencesData>>): UseMutationOptions<UpdatePreferencesResponse, UpdatePreferencesError, Options<UpdatePreferencesData>> => {
    const mutationOptions: UseMutationOptions<UpdatePreferencesResponse, UpdatePreferencesError, Options<UpdatePreferencesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await updatePreferences({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getRuntimeConfigQueryKey = (options?: Options<GetRuntimeConfigData>) => createQueryKey('getRuntimeConfig', options, false, ['configuration']);

/**
 * Get Runtime Config
 *
 * Return public frontend bootstrap configuration.
 *
 * Used by:
 * - frontend auth bootstrap and generated API clients because the app needs
 * auth mode and Google client metadata before user authentication.
 *
 * Flow:
 * - Read the current process settings.
 * - Return only public runtime fields; mutable and filesystem settings stay
 * behind admin routes.
 */
export const getRuntimeConfigOptions = (options?: Options<GetRuntimeConfigData>) => queryOptions<GetRuntimeConfigResponse, GetRuntimeConfigError, GetRuntimeConfigResponse, ReturnType<typeof getRuntimeConfigQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRuntimeConfig({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRuntimeConfigQueryKey(options)
});

export const listTasksQueryKey = (options?: Options<ListTasksData>) => createQueryKey('listTasks', options, false, ['task_streaming']);

/**
 * List Tasks
 *
 * List tasks for current user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET / route because they need this unit's "List tasks for current user" behavior.
 */
export const listTasksOptions = (options?: Options<ListTasksData>) => queryOptions<ListTasksResponse, ListTasksError, ListTasksResponse, ReturnType<typeof listTasksQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listTasks({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listTasksQueryKey(options)
});

/**
 * Clear Task
 *
 * Clear a task and all associated caches by task id.
 *
 * If the task is still running it is cancelled first.  Both the worker
 * task manager record and the analysis task manager record (including
 * the current-task-id mapping) are removed.
 *
 * Flow:
 * - Resolve authentication and task id from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /{task_id} route
 * because they need this unit's "Clear a task and all associated caches by task id" behavior.
 */
export const clearTaskMutation = (options?: Partial<Options<ClearTaskData>>): UseMutationOptions<ClearTaskResponse, ClearTaskError, Options<ClearTaskData>> => {
    const mutationOptions: UseMutationOptions<ClearTaskResponse, ClearTaskError, Options<ClearTaskData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await clearTask({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Cancel Task
 *
 * Stop a running task and mark it as cancelled.
 *
 * Unlike ``DELETE /tasks/{task_id}``, the task record is kept so the user can
 * see the cancelled state in the task list and explicitly clear it afterwards.
 *
 * Flow:
 * - Resolve authentication and task id from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /{task_id}/cancel route
 * because they need this unit's "Stop a running task and mark it as cancelled" behavior.
 */
export const cancelTaskMutation = (options?: Partial<Options<CancelTaskData>>): UseMutationOptions<CancelTaskResponse, CancelTaskError, Options<CancelTaskData>> => {
    const mutationOptions: UseMutationOptions<CancelTaskResponse, CancelTaskError, Options<CancelTaskData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await cancelTask({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getMyCurrentWorkspaceQueryKey = (options?: Options<GetMyCurrentWorkspaceData>) => createQueryKey('getMyCurrentWorkspace', options, false, ['users']);

/**
 * Get My Current Workspace
 *
 * Return the authenticated user's selected workspace id.
 *
 * Used by:
 * - frontend startup hydration because the browser needs to restore the
 * server-side workspace selection after authentication.
 *
 * Flow: resolve the user id and read the workspace manager's selected
 * workspace pointer without loading workspace data.
 */
export const getMyCurrentWorkspaceOptions = (options?: Options<GetMyCurrentWorkspaceData>) => queryOptions<GetMyCurrentWorkspaceResponse, GetMyCurrentWorkspaceError, GetMyCurrentWorkspaceResponse, ReturnType<typeof getMyCurrentWorkspaceQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getMyCurrentWorkspace({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getMyCurrentWorkspaceQueryKey(options)
});

/**
 * Set My Current Workspace
 *
 * Set or clear the authenticated user's selected workspace id.
 *
 * Used by:
 * - frontend workspace switcher and data-folder reset flows because current
 * workspace is a user selection preference, not an implicit API target.
 *
 * Flow: submit the requested id or ``None`` to the workspace manager, surface
 * missing workspaces as 404, and return the selected id in the response.
 */
export const setMyCurrentWorkspaceMutation = (options?: Partial<Options<SetMyCurrentWorkspaceData>>): UseMutationOptions<SetMyCurrentWorkspaceResponse, SetMyCurrentWorkspaceError, Options<SetMyCurrentWorkspaceData>> => {
    const mutationOptions: UseMutationOptions<SetMyCurrentWorkspaceResponse, SetMyCurrentWorkspaceError, Options<SetMyCurrentWorkspaceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setMyCurrentWorkspace({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const listWorkspacesQueryKey = (options?: Options<ListWorkspacesData>) => createQueryKey('listWorkspaces', options, false, ['lifecycle']);

/**
 * List Workspaces
 *
 * List all persisted workspaces visible to the current user.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend workspace switcher/landing views because they need this unit's "List all persisted workspaces visible to the current user" behavior.
 *
 * Why:
 * - Provides fast summary metadata without loading full workspace graphs.
 */
export const listWorkspacesOptions = (options?: Options<ListWorkspacesData>) => queryOptions<ListWorkspacesResponse, ListWorkspacesError, ListWorkspacesResponse, ReturnType<typeof listWorkspacesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listWorkspaces({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listWorkspacesQueryKey(options)
});

/**
 * Create Workspace
 *
 * Create a workspace and return normalized workspace metadata.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend new-workspace dialog because they need this unit's "Create a workspace and return normalized workspace metadata" behavior.
 *
 * Why:
 * - Centralizes workspace-name validation and initialization metadata.
 */
export const createWorkspaceMutation = (options?: Partial<Options<CreateWorkspaceData>>): UseMutationOptions<CreateWorkspaceResponse, CreateWorkspaceError, Options<CreateWorkspaceData>> => {
    const mutationOptions: UseMutationOptions<CreateWorkspaceResponse, CreateWorkspaceError, Options<CreateWorkspaceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createWorkspace({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getTokenizerModelsQueryKey = (options?: Options<GetTokenizerModelsData>) => createQueryKey('getTokenizerModels', options, false, ['nodes']);

/**
 * Get Tokenizer Models
 *
 * Return available tokenizer model records.
 */
export const getTokenizerModelsOptions = (options?: Options<GetTokenizerModelsData>) => queryOptions<GetTokenizerModelsResponse, GetTokenizerModelsError, GetTokenizerModelsResponse, ReturnType<typeof getTokenizerModelsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTokenizerModels({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTokenizerModelsQueryKey(options)
});

/**
 * Upload Workspace Zip
 *
 * Upload a workspace ZIP archive and import it into user workspace storage.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /upload route because they need this unit's "Upload a workspace ZIP archive and import it into user workspace storage" behavior.
 */
export const uploadWorkspaceZipMutation = (options?: Partial<Options<UploadWorkspaceZipData>>): UseMutationOptions<UploadWorkspaceZipResponse, UploadWorkspaceZipError, Options<UploadWorkspaceZipData>> => {
    const mutationOptions: UseMutationOptions<UploadWorkspaceZipResponse, UploadWorkspaceZipError, Options<UploadWorkspaceZipData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await uploadWorkspaceZip({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Workspace By Id
 *
 * Delete the explicit workspace id in the path.
 *
 * Used by:
 * - frontend workspace manager delete actions and test cleanup because deletion
 * should not depend on the backend-selected current workspace.
 *
 * Flow: let the UUID path converter reject static route names, delegate
 * deletion to the manager, and return the standard workspace action
 * response used by lifecycle mutations.
 */
export const deleteWorkspaceByIdMutation = (options?: Partial<Options<DeleteWorkspaceByIdData>>): UseMutationOptions<DeleteWorkspaceByIdResponse, DeleteWorkspaceByIdError, Options<DeleteWorkspaceByIdData>> => {
    const mutationOptions: UseMutationOptions<DeleteWorkspaceByIdResponse, DeleteWorkspaceByIdError, Options<DeleteWorkspaceByIdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteWorkspaceById({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getWorkspaceByIdQueryKey = (options: Options<GetWorkspaceByIdData>) => createQueryKey('getWorkspaceById', options, false, ['lifecycle']);

/**
 * Get Workspace By Id
 *
 * Return workspace metadata for the explicit workspace id in the path.
 *
 * Used by:
 * - frontend workspace-detail reads because callers already know the selected
 * workspace id and should not depend on hidden current-workspace state.
 *
 * Flow: resolve the authenticated user, load the requested workspace id, and
 * serialize the standard ``WorkspaceInfo`` payload for that explicit
 * workspace.
 */
export const getWorkspaceByIdOptions = (options: Options<GetWorkspaceByIdData>) => queryOptions<GetWorkspaceByIdResponse, GetWorkspaceByIdError, GetWorkspaceByIdResponse, ReturnType<typeof getWorkspaceByIdQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getWorkspaceById({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getWorkspaceByIdQueryKey(options)
});

/**
 * Update Workspace By Id
 *
 * Update metadata for the explicit workspace id in the path.
 *
 * Used by:
 * - frontend workspace manager and header rename flows because workspace
 * mutations should name their target resource directly.
 *
 * Flow: load the requested workspace, validate any submitted name, apply only
 * supplied metadata fields, persist through the shared workspace saver,
 * and return refreshed workspace metadata.
 */
export const updateWorkspaceByIdMutation = (options?: Partial<Options<UpdateWorkspaceByIdData>>): UseMutationOptions<UpdateWorkspaceByIdResponse, UpdateWorkspaceByIdError, Options<UpdateWorkspaceByIdData>> => {
    const mutationOptions: UseMutationOptions<UpdateWorkspaceByIdResponse, UpdateWorkspaceByIdError, Options<UpdateWorkspaceByIdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await updateWorkspaceById({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const analysisTaskDetachOptionsQueryKey = (options: Options<AnalysisTaskDetachOptionsData>) => createQueryKey('analysisTaskDetachOptions', options, false, ['analysis-tasks']);

/**
 * Analysis Task Detach Options
 *
 * Return detach option metadata for an analysis task.
 *
 * Used by:
 * - topic-modeling, concordance, and quotation detach dialogs because
 * generated artifact options should stay under the task resource that
 * produced the result.
 *
 * Flow: validate task ownership, dispatch to analyses that expose task-level
 * detach options, require node/column query data for node-scoped
 * analyses, and reject unsupported task types.
 */
export const analysisTaskDetachOptionsOptions = (options: Options<AnalysisTaskDetachOptionsData>) => queryOptions<AnalysisTaskDetachOptionsResponse, AnalysisTaskDetachOptionsError, AnalysisTaskDetachOptionsResponse, ReturnType<typeof analysisTaskDetachOptionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await analysisTaskDetachOptions({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: analysisTaskDetachOptionsQueryKey(options)
});

/**
 * Create Analysis Task Detachment
 *
 * Create detached workspace nodes from an analysis task.
 *
 * Used by:
 * - sequential-analysis, topic-modeling, concordance, and quotation panels
 * because detaching generated analysis output is a task lifecycle
 * operation.
 *
 * Flow: validate task ownership, parse the body with the owning analysis
 * request model, and delegate node creation to the analysis helper.
 */
export const createAnalysisTaskDetachmentMutation = (options?: Partial<Options<CreateAnalysisTaskDetachmentData>>): UseMutationOptions<CreateAnalysisTaskDetachmentResponse, CreateAnalysisTaskDetachmentError, Options<CreateAnalysisTaskDetachmentData>> => {
    const mutationOptions: UseMutationOptions<CreateAnalysisTaskDetachmentResponse, CreateAnalysisTaskDetachmentError, Options<CreateAnalysisTaskDetachmentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createAnalysisTaskDetachment({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const analysisTaskDispersionBinsQueryKey = (options: Options<AnalysisTaskDispersionBinsData>) => createQueryKey('analysisTaskDispersionBins', options, false, ['analysis-tasks']);

/**
 * Analysis Task Dispersion Bins
 *
 * Return materialized concordance dispersion bins for one node.
 *
 * Used by:
 * - concordance dispersion charts because whole-corpus materialized bins are
 * a read of the parent concordance task, not a separate analysis namespace.
 *
 * Flow: validate task ownership, reject non-concordance tasks, then delegate
 * bin loading to the concordance artifact helper.
 */
export const analysisTaskDispersionBinsOptions = (options: Options<AnalysisTaskDispersionBinsData>) => queryOptions<AnalysisTaskDispersionBinsResponse, AnalysisTaskDispersionBinsError, AnalysisTaskDispersionBinsResponse, ReturnType<typeof analysisTaskDispersionBinsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await analysisTaskDispersionBins({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: analysisTaskDispersionBinsQueryKey(options)
});

/**
 * Create Analysis Task Dispersion Detachment
 *
 * Create a dispersion-detached workspace node from a concordance task.
 *
 * Used by:
 * - concordance dispersion views because selected-bin detaches are a
 * concordance-specific child action of the parent analysis task.
 *
 * Flow: validate task ownership, reject non-concordance tasks, then submit
 * the existing dispersion-detach worker with the URL task id as the
 * parent task.
 */
export const createAnalysisTaskDispersionDetachmentMutation = (options?: Partial<Options<CreateAnalysisTaskDispersionDetachmentData>>): UseMutationOptions<CreateAnalysisTaskDispersionDetachmentResponse, CreateAnalysisTaskDispersionDetachmentError, Options<CreateAnalysisTaskDispersionDetachmentData>> => {
    const mutationOptions: UseMutationOptions<CreateAnalysisTaskDispersionDetachmentResponse, CreateAnalysisTaskDispersionDetachmentError, Options<CreateAnalysisTaskDispersionDetachmentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createAnalysisTaskDispersionDetachment({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Create Analysis Task Materialization
 *
 * Create or refresh a materialized artifact for an analysis task.
 *
 * Used by:
 * - concordance and quotation result views because processing all matches is
 * a task child action that should update the parent task's cached artifact
 * path.
 *
 * Flow: validate task ownership, parse the body with the owning analysis
 * request model, and submit the existing materialize worker with the URL
 * task id as the parent task.
 */
export const createAnalysisTaskMaterializationMutation = (options?: Partial<Options<CreateAnalysisTaskMaterializationData>>): UseMutationOptions<CreateAnalysisTaskMaterializationResponse, CreateAnalysisTaskMaterializationError, Options<CreateAnalysisTaskMaterializationData>> => {
    const mutationOptions: UseMutationOptions<CreateAnalysisTaskMaterializationResponse, CreateAnalysisTaskMaterializationError, Options<CreateAnalysisTaskMaterializationData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createAnalysisTaskMaterialization({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Analysis Task Preferences
 *
 * Persist display preferences for an existing analysis task.
 *
 * Used by:
 * - quotation, sequential-analysis, and token-frequency panels because
 * presentation settings should not be overloaded onto result reads.
 *
 * Flow: validate task ownership, choose the preference model based on the
 * persisted request type, then delegate to the owning helper that mutates
 * the task record.
 */
export const analysisTaskPreferencesMutation = (options?: Partial<Options<AnalysisTaskPreferencesData>>): UseMutationOptions<unknown, AnalysisTaskPreferencesError, Options<AnalysisTaskPreferencesData>> => {
    const mutationOptions: UseMutationOptions<unknown, AnalysisTaskPreferencesError, Options<AnalysisTaskPreferencesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await analysisTaskPreferences({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const analysisTaskRequestQueryKey = (options: Options<AnalysisTaskRequestData>) => createQueryKey('analysisTaskRequest', options, false, ['analysis-tasks']);

/**
 * Analysis Task Request
 *
 * Return the stored request payload for one analysis task.
 *
 * Used by:
 * - frontend analysis hydration and re-run comparison because every analysis
 * tab can recover its last submitted parameters through one task resource.
 *
 * Flow: validate the task belongs to the workspace path, then serialize the
 * persisted Pydantic request object.
 */
export const analysisTaskRequestOptions = (options: Options<AnalysisTaskRequestData>) => queryOptions<unknown, AnalysisTaskRequestError, unknown, ReturnType<typeof analysisTaskRequestQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await analysisTaskRequest({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: analysisTaskRequestQueryKey(options)
});

export const analysisTaskResultQueryKey = (options: Options<AnalysisTaskResultData>) => createQueryKey('analysisTaskResult', options, false, ['analysis-tasks']);

/**
 * Analysis Task Result
 *
 * Return the current or completed result payload for one analysis task.
 *
 * Used by:
 * - frontend polling and hydration because task status/result reads should
 * not require callers to know the analysis namespace.
 *
 * Flow: validate task ownership, dispatch by persisted request type, and
 * delegate result shaping to the original analysis helper.
 */
export const analysisTaskResultOptions = (options: Options<AnalysisTaskResultData>) => queryOptions<unknown, AnalysisTaskResultError, unknown, ReturnType<typeof analysisTaskResultQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await analysisTaskResult({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: analysisTaskResultQueryKey(options)
});

const createInfiniteParams = <K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>>(queryKey: QueryKey<Options>, page: K) => {
    const params = { ...queryKey[0] };
    if (page.body) {
        params.body = {
            ...queryKey[0].body as any,
            ...page.body as any
        };
    }
    if (page.headers) {
        params.headers = {
            ...queryKey[0].headers,
            ...page.headers
        };
    }
    if (page.path) {
        params.path = {
            ...queryKey[0].path as any,
            ...page.path as any
        };
    }
    if (page.query) {
        params.query = {
            ...queryKey[0].query as any,
            ...page.query as any
        };
    }
    return params as unknown as typeof page;
};

export const analysisTaskResultInfiniteQueryKey = (options: Options<AnalysisTaskResultData>): QueryKey<Options<AnalysisTaskResultData>> => createQueryKey('analysisTaskResult', options, true);

/**
 * Analysis Task Result
 *
 * Return the current or completed result payload for one analysis task.
 *
 * Used by:
 * - frontend polling and hydration because task status/result reads should
 * not require callers to know the analysis namespace.
 *
 * Flow: validate task ownership, dispatch by persisted request type, and
 * delegate result shaping to the original analysis helper.
 */
export const analysisTaskResultInfiniteOptions = (options: Options<AnalysisTaskResultData>) => infiniteQueryOptions<unknown, AnalysisTaskResultError, InfiniteData<unknown>, QueryKey<Options<AnalysisTaskResultData>>, number | null | Pick<QueryKey<Options<AnalysisTaskResultData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<AnalysisTaskResultData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await analysisTaskResult({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: analysisTaskResultInfiniteQueryKey(options)
});

/**
 * Analysis Task Result Query
 *
 * Run a body-based result query for a completed analysis task.
 *
 * Used by:
 * - concordance result controls because per-node pagination and sorting
 * overrides are richer than simple URL query params.
 *
 * Flow: validate task ownership, dispatch body queries to analyses that
 * support query-time result shaping, and reject unsupported task types
 * instead of overloading their preference-update endpoints.
 */
export const analysisTaskResultQueryMutation = (options?: Partial<Options<AnalysisTaskResultQueryData>>): UseMutationOptions<unknown, AnalysisTaskResultQueryError, Options<AnalysisTaskResultQueryData>> => {
    const mutationOptions: UseMutationOptions<unknown, AnalysisTaskResultQueryError, Options<AnalysisTaskResultQueryData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await analysisTaskResultQuery({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Annotate Ai Preview
 *
 * AI-classify one page of texts, caching results in a preview session.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel because previewing must not mutate the
 * workspace; the panel renders the returned labels beside the (unchanged)
 * source column so users can eyeball provider/model/prompt/class quality.
 *
 * Flow:
 * - Resolve the workspace + source node (404) and require the text column (400).
 * - Load the authoritative class list from the class-description node (404/400).
 * - Slice exactly the requested page of the text column — the same
 * ``(page-1)*page_size`` slice ``get_node_data`` uses — so labels line up with
 * the rows the panel displays.
 * - Sync the server-side preview session for this node + config signature, then
 * dispatch a provider batch for ONLY the page rows not already cached (re-viewed
 * pages are cache hits and cost nothing). Store the fresh labels and return the
 * model labels for the whole page in order; provider errors become a 502.
 */
export const annotateAiPreviewMutation = (options?: Partial<Options<AnnotateAiPreviewData>>): UseMutationOptions<AnnotateAiPreviewResponse, AnnotateAiPreviewError, Options<AnnotateAiPreviewData>> => {
    const mutationOptions: UseMutationOptions<AnnotateAiPreviewResponse, AnnotateAiPreviewError, Options<AnnotateAiPreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await annotateAiPreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Annotate Ai Preview Clear
 *
 * Discard the node's cached AI preview session.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel when the user clicks "Close preview". The
 * in-memory preview store keeps a node's predictions across tab switches so the
 * panel can rehydrate, but an explicit close means those predictions are no
 * longer wanted; dropping the session frees the memory and resets the
 * detach/annotate-all counts so a later preview starts clean.
 *
 * Flow:
 * - Resolve the path workspace (the store is keyed per user+workspace+node).
 * - Clear the node's session unconditionally. Clearing is idempotent, so a missing
 * session (nothing previewed, or already cleared) is a successful no-op; the node
 * itself is not required to still exist, which is why no 404 is raised here.
 */
export const annotateAiPreviewClearMutation = (options?: Partial<Options<AnnotateAiPreviewClearData>>): UseMutationOptions<AnnotateAiPreviewClearResponse, AnnotateAiPreviewClearError, Options<AnnotateAiPreviewClearData>> => {
    const mutationOptions: UseMutationOptions<AnnotateAiPreviewClearResponse, AnnotateAiPreviewClearError, Options<AnnotateAiPreviewClearData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await annotateAiPreviewClear({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const annotateAiPreviewStateQueryKey = (options: Options<AnnotateAiPreviewStateData>) => createQueryKey('annotateAiPreviewState', options, false, ['annotation']);

/**
 * Annotate Ai Preview State
 *
 * Return the cached preview rows for a node so the panel can rehydrate.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel on mount because its per-row AI labels and
 * manual overrides are in-memory only; after a tab switch the panel re-reads
 * them here so the preview reappears exactly as the user left it.
 *
 * Flow:
 * - Resolve the workspace + source node (404).
 * - Recompute the config signature and ask the store for its rows; if the stored
 * session belongs to a different config the store returns nothing, so the panel
 * shows an empty preview and re-classifies on demand rather than displaying
 * stale labels.
 */
export const annotateAiPreviewStateOptions = (options: Options<AnnotateAiPreviewStateData>) => queryOptions<AnnotateAiPreviewStateResponse, AnnotateAiPreviewStateError, AnnotateAiPreviewStateResponse, ReturnType<typeof annotateAiPreviewStateQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await annotateAiPreviewState({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: annotateAiPreviewStateQueryKey(options)
});

/**
 * Annotate Ai All
 *
 * AI-classify every row concurrently and write the whole column in one go.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel's "Annotate All" button because filling a
 * whole column one cell at a time is slow; this runs all batches concurrently
 * on the server and persists the result as a single column rewrite.
 *
 * Flow:
 * - Resolve the workspace + source node (404), collect it once, and require an
 * existing *string* annotation column (400) so the write target is valid.
 * - Load the authoritative class list from the class node (404/400).
 * - Reuse any labels already cached in the preview store (rows the user previewed,
 * with their manual overrides), and dispatch batches concurrently via
 * ``annotate_all`` for ONLY the remaining rows — so a full run never re-spends on
 * pages the user already previewed and respects edits made during preview.
 * - Overwrite the annotation column with the merged labels (null where the model
 * chose no class) and restage/persist the node; clear the cache since the
 * predictions now live in the column. Provider errors become a 502 and nothing
 * is written.
 */
export const annotateAiAllMutation = (options?: Partial<Options<AnnotateAiAllData>>): UseMutationOptions<AnnotateAiAllResponse, AnnotateAiAllError, Options<AnnotateAiAllData>> => {
    const mutationOptions: UseMutationOptions<AnnotateAiAllResponse, AnnotateAiAllError, Options<AnnotateAiAllData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await annotateAiAll({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Detach Ai Previewed Rows
 *
 * Copy the already-previewed rows into a new annotated child node.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel's "Detach Previewed Rows" button because
 * AI preview predictions live only in the server-side preview store; detaching
 * turns every page the user has already assessed (not just the current one) into
 * a persisted child table so those labels survive as a real data block without
 * re-running (or persisting) the whole column on the source node.
 *
 * Flow:
 * - Resolve the workspace + source node (404).
 * - Build the row->label map from the node's active preview session: every
 * previewed row across all viewed pages, with overrides winning over model labels.
 * - When ``dry_run`` is set, return that map's size immediately (``node=None``,
 * nothing created). The panel calls this on mount to gate/label its Detach
 * button from the authoritative server session, so a tab switch (which wipes the
 * panel's local page map) no longer leaves the button wrongly disabled.
 * - Otherwise require a non-blank annotation column (400) and at least one row
 * (400) — this is what fixes "detach only grabbed one page", since the source of
 * truth is now the whole server session rather than the panel's current page.
 * - Collect the source once; validate every stored row index is in range (400).
 * - Filter the source down to those indices (original order preserved), overwrite
 * the annotation column with the aligned labels (blank/whitespace -> null), and
 * stage the subset as a lazy frame.
 * - Add it as a child of the source via ``_create_and_persist_child_node`` (so the
 * lineage shows in the graph) and return the new node metadata + row count.
 */
export const detachAiPreviewedRowsMutation = (options?: Partial<Options<DetachAiPreviewedRowsData>>): UseMutationOptions<DetachAiPreviewedRowsResponse, DetachAiPreviewedRowsError, Options<DetachAiPreviewedRowsData>> => {
    const mutationOptions: UseMutationOptions<DetachAiPreviewedRowsResponse, DetachAiPreviewedRowsError, Options<DetachAiPreviewedRowsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await detachAiPreviewedRows({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Annotate Ai Preview Override
 *
 * Persist one manual cell edit onto the node's active preview session.
 *
 * Used by:
 * - Frontend AnnotationAiPreviewPanel when the user changes a prediction in the
 * dropdown, so the edit survives a tab switch (it returns via the session
 * resource)
 * and is honoured by detach/annotate-all. A blank label is stored as an explicit
 * null override (the user chose "None"), which still wins over the model label.
 *
 * Flow:
 * - Resolve the workspace + source node (404).
 * - Write the override to the current session; ``ok=False`` when there is no active
 * session (the edit is stale — an override can only follow a preview).
 */
export const annotateAiPreviewOverrideMutation = (options?: Partial<Options<AnnotateAiPreviewOverrideData>>): UseMutationOptions<AnnotateAiPreviewOverrideResponse, AnnotateAiPreviewOverrideError, Options<AnnotateAiPreviewOverrideData>> => {
    const mutationOptions: UseMutationOptions<AnnotateAiPreviewOverrideResponse, AnnotateAiPreviewOverrideError, Options<AnnotateAiPreviewOverrideData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await annotateAiPreviewOverride({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * List Annotation Ai Models
 *
 * Proxy a provider's model listing so the browser never calls providers.
 *
 * Used by:
 * - Frontend ModelNameCombobox to populate the model dropdown. Authentication is
 * required (per-user gate) even though no workspace is touched, so an
 * unauthenticated caller cannot use the backend as an open model-listing proxy.
 *
 * Flow:
 * - Validate the workspace path because the route is workspace-scoped.
 * - Delegate to ``core.annotation_ai.list_models`` (native SDK per provider).
 * - Translate any provider/SDK failure into a 502 with the provider's message.
 */
export const listAnnotationAiModelsMutation = (options?: Partial<Options<ListAnnotationAiModelsData>>): UseMutationOptions<ListAnnotationAiModelsResponse, ListAnnotationAiModelsError, Options<ListAnnotationAiModelsData>> => {
    const mutationOptions: UseMutationOptions<ListAnnotationAiModelsResponse, ListAnnotationAiModelsError, Options<ListAnnotationAiModelsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await listAnnotationAiModels({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Create Annotation Class Descriptions
 *
 * Create an empty class-description table for the Annotation view.
 *
 * Used by:
 * - Frontend Annotation view through the generated SDK because users need a
 * one-click way to seed the class/description table required by the view.
 *
 * Flow:
 * - Resolve the active workspace.
 * - Build an empty ``class``/``description`` table with string dtypes.
 * - Persist it to the workspace data folder and add it as a root node.
 * - Save the workspace and return the standard node metadata response.
 */
export const createAnnotationClassDescriptionsMutation = (options?: Partial<Options<CreateAnnotationClassDescriptionsData>>): UseMutationOptions<CreateAnnotationClassDescriptionsResponse, CreateAnnotationClassDescriptionsError, Options<CreateAnnotationClassDescriptionsData>> => {
    const mutationOptions: UseMutationOptions<CreateAnnotationClassDescriptionsResponse, CreateAnnotationClassDescriptionsError, Options<CreateAnnotationClassDescriptionsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createAnnotationClassDescriptions({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getAnnotationClassDescriptionsQueryKey = (options: Options<GetAnnotationClassDescriptionsData>) => createQueryKey('getAnnotationClassDescriptions', options, false, ['annotation']);

/**
 * Get Annotation Class Descriptions
 *
 * Return editable class-description rows for one selected node.
 *
 * Used by:
 * - Frontend Annotation view because the class editor needs a compact
 * two-column row payload independent of the full workspace table UI.
 */
export const getAnnotationClassDescriptionsOptions = (options: Options<GetAnnotationClassDescriptionsData>) => queryOptions<GetAnnotationClassDescriptionsResponse, GetAnnotationClassDescriptionsError, GetAnnotationClassDescriptionsResponse, ReturnType<typeof getAnnotationClassDescriptionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getAnnotationClassDescriptions({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getAnnotationClassDescriptionsQueryKey(options)
});

/**
 * Update Annotation Class Descriptions
 *
 * Persist edited class-description rows back to the selected node.
 *
 * Used by:
 * - Frontend Annotation view on cell blur/add-row because class labels and
 * descriptions should behave like workspace data, not transient UI state.
 *
 * Flow:
 * - Validate the node and selected columns.
 * - Rewrite the selected class/description columns from the submitted rows.
 * - Restage the dataframe as workspace data and save the workspace.
 */
export const updateAnnotationClassDescriptionsMutation = (options?: Partial<Options<UpdateAnnotationClassDescriptionsData>>): UseMutationOptions<UpdateAnnotationClassDescriptionsResponse, UpdateAnnotationClassDescriptionsError, Options<UpdateAnnotationClassDescriptionsData>> => {
    const mutationOptions: UseMutationOptions<UpdateAnnotationClassDescriptionsResponse, UpdateAnnotationClassDescriptionsError, Options<UpdateAnnotationClassDescriptionsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await updateAnnotationClassDescriptions({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Set Annotation Class Parent
 *
 * Reparent a class-description node under the annotated source node.
 *
 * Used by:
 * - Frontend Annotation view on Start because making the class table a child
 * of the source node keeps the annotation lineage visible in the graph.
 *
 * Flow:
 * - Resolve workspace, source, and class nodes (404 if missing).
 * - Reject self-parenting, then set the class node's only parent to the source
 * and re-tidy its list position.
 * - Persist and return the standard node metadata response.
 */
export const setAnnotationClassParentMutation = (options?: Partial<Options<SetAnnotationClassParentData>>): UseMutationOptions<SetAnnotationClassParentResponse, SetAnnotationClassParentError, Options<SetAnnotationClassParentData>> => {
    const mutationOptions: UseMutationOptions<SetAnnotationClassParentResponse, SetAnnotationClassParentError, Options<SetAnnotationClassParentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setAnnotationClassParent({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Set Annotation Cell
 *
 * Write a single class label into the source node's annotation column.
 *
 * Used by:
 * - Frontend Annotation results table because each per-row class dropdown
 * persists its selection so annotations survive reloads and feed downstream
 * analyses, rather than staying as local component state.
 *
 * Flow:
 * - Resolve the workspace and source node (404 if missing).
 * - Require a non-blank, existing string annotation column (400 otherwise).
 * - Bounds-check the absolute row index against the collected frame (400).
 * - Rewrite just the target cell (blank/whitespace clears it to null), restage
 * the materialized frame, persist the workspace, and return node metadata.
 */
export const setAnnotationCellMutation = (options?: Partial<Options<SetAnnotationCellData>>): UseMutationOptions<SetAnnotationCellResponse, SetAnnotationCellError, Options<SetAnnotationCellData>> => {
    const mutationOptions: UseMutationOptions<SetAnnotationCellResponse, SetAnnotationCellError, Options<SetAnnotationCellData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setAnnotationCell({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Create Annotation Column
 *
 * Add an empty string annotation column to the selected source node.
 *
 * Used by:
 * - Frontend Annotation view on Start (new-annotation mode) because the source
 * block needs a real, persisted column before the per-row class dropdowns can
 * record annotations and before the view can switch into resume mode.
 *
 * Flow:
 * - Resolve the workspace and source node (404 if missing).
 * - Reject blank names and names that already exist on the node (400).
 * - Append a null-filled string column lazily, then restage the materialized
 * frame so the node stays portable and lazy by default.
 * - Persist the workspace and return the refreshed node metadata.
 */
export const createAnnotationColumnMutation = (options?: Partial<Options<CreateAnnotationColumnData>>): UseMutationOptions<CreateAnnotationColumnResponse, CreateAnnotationColumnError, Options<CreateAnnotationColumnData>> => {
    const mutationOptions: UseMutationOptions<CreateAnnotationColumnResponse, CreateAnnotationColumnError, Options<CreateAnnotationColumnData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await createAnnotationColumn({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Run Concordance
 *
 * Run concordance immediately and store task metadata for retrieval.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend run route: `POST /workspaces/{id}/concordance` because they need this unit's "Run concordance immediately and store task metadata for retrieval" behavior.
 *
 * Why:
 * - Keeps API behavior aligned with other analyses by returning task-linked
 * responses while using shared concordance response builders.
 */
export const runConcordanceMutation = (options?: Partial<Options<RunConcordanceData>>): UseMutationOptions<RunConcordanceResponse, RunConcordanceError, Options<RunConcordanceData>> => {
    const mutationOptions: UseMutationOptions<RunConcordanceResponse, RunConcordanceError, Options<RunConcordanceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await runConcordance({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Start Workspace Download
 *
 * Start a background task to package the explicit workspace as a ZIP.
 *
 * Used by:
 * - frontend Download button in Workspace Manager because each row can start
 * a download for its own workspace, independent of active selection.
 *
 * Why:
 * - Moves potentially slow ZIP compression into the Task Center so users can
 * track progress and the UI stays responsive.
 *
 * Flow: resolve the target workspace directory from the path id, persist the
 * latest in-memory state only when that workspace is already active,
 * submit the packaging task with explicit workspace metadata, and return
 * the task id to the caller.
 */
export const startWorkspaceDownloadMutation = (options?: Partial<Options<StartWorkspaceDownloadData>>): UseMutationOptions<StartWorkspaceDownloadResponse, StartWorkspaceDownloadError, Options<StartWorkspaceDownloadData>> => {
    const mutationOptions: UseMutationOptions<StartWorkspaceDownloadResponse, StartWorkspaceDownloadError, Options<StartWorkspaceDownloadData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await startWorkspaceDownload({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const downloadWorkspaceArtifactQueryKey = (options: Options<DownloadWorkspaceArtifactData>) => createQueryKey('downloadWorkspaceArtifact', options, false, ['lifecycle']);

/**
 * Download Workspace Artifact
 *
 * Stream a completed workspace ZIP artifact and delete it after download.
 *
 * Used by:
 * - frontend auto-download on task completion because the artifact task must
 * still be checked against the workspace row that started the download.
 *
 * Why:
 * - One-time artifact policy: the ZIP is deleted after the first successful
 * download to avoid unbounded disk usage.
 *
 * Flow: resolve the path workspace id, fetch the task, reject task/workspace
 * mismatches, stream a successful ZIP artifact, and remove it afterwards.
 */
export const downloadWorkspaceArtifactOptions = (options: Options<DownloadWorkspaceArtifactData>) => queryOptions<DownloadWorkspaceArtifactResponse, DownloadWorkspaceArtifactError, DownloadWorkspaceArtifactResponse, ReturnType<typeof downloadWorkspaceArtifactQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await downloadWorkspaceArtifact({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: downloadWorkspaceArtifactQueryKey(options)
});

export const exportNodesQueryKey = (options: Options<ExportNodesData>) => createQueryKey('exportNodes', options, false, ['workspace']);

/**
 * Export Nodes
 *
 * Export one or more workspace nodes as downloadable file(s).
 *
 * If multiple node_ids are provided, a ZIP archive is returned.
 * Supported formats: csv, json, parquet, ipc, ndjson.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI GET /export route because they need this unit's "Export one or more workspace nodes as downloadable file(s)" behavior.
 */
export const exportNodesOptions = (options: Options<ExportNodesData>) => queryOptions<ExportNodesResponse, ExportNodesError, ExportNodesResponse, ReturnType<typeof exportNodesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await exportNodes({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: exportNodesQueryKey(options)
});

export const getWorkspaceGraphByIdQueryKey = (options: Options<GetWorkspaceGraphByIdData>) => createQueryKey('getWorkspaceGraphById', options, false, ['lifecycle']);

/**
 * Get Workspace Graph By Id
 *
 * Return graph topology for the explicit workspace id in the path.
 *
 * Used by:
 * - frontend graph and sidebar queries because cache keys are already scoped by
 * workspace id and should fetch the same id over the API boundary.
 *
 * Flow: load the requested workspace and serialize its lightweight graph
 * topology without duplicating full node metadata.
 */
export const getWorkspaceGraphByIdOptions = (options: Options<GetWorkspaceGraphByIdData>) => queryOptions<GetWorkspaceGraphByIdResponse, GetWorkspaceGraphByIdError, GetWorkspaceGraphByIdResponse, ReturnType<typeof getWorkspaceGraphByIdQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getWorkspaceGraphById({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getWorkspaceGraphByIdQueryKey(options)
});

/**
 * Add Node To Workspace
 *
 * Add a data file as a new node to workspace.
 *
 * Files are eagerly loaded into a Polars DataFrame, persisted as parquet under the workspace's `data/` folder,
 * and then reloaded as a LazyFrame. This separates bulk data from `metadata.json` while keeping
 * lazy processing semantics.
 *
 * Flow:
 * - Resolve authentication and request body from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes route because they need this unit's "Add a data file as a new node to workspace" behavior.
 */
export const addNodeToWorkspaceMutation = (options?: Partial<Options<AddNodeToWorkspaceData>>): UseMutationOptions<AddNodeToWorkspaceResponse, AddNodeToWorkspaceError, Options<AddNodeToWorkspaceData>> => {
    const mutationOptions: UseMutationOptions<AddNodeToWorkspaceResponse, AddNodeToWorkspaceError, Options<AddNodeToWorkspaceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await addNodeToWorkspace({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Concat Nodes
 *
 * Create a new child node by concatenating multiple nodes vertically.
 */
export const concatNodesMutation = (options?: Partial<Options<ConcatNodesData>>): UseMutationOptions<ConcatNodesResponse, ConcatNodesError, Options<ConcatNodesData>> => {
    const mutationOptions: UseMutationOptions<ConcatNodesResponse, ConcatNodesError, Options<ConcatNodesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await concatNodes({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Concat Nodes Preview
 *
 * Preview the result of concatenating multiple nodes vertically.
 */
export const concatNodesPreviewMutation = (options?: Partial<Options<ConcatNodesPreviewData>>): UseMutationOptions<ConcatNodesPreviewResponse, ConcatNodesPreviewError, Options<ConcatNodesPreviewData>> => {
    const mutationOptions: UseMutationOptions<ConcatNodesPreviewResponse, ConcatNodesPreviewError, Options<ConcatNodesPreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await concatNodesPreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Join Nodes
 *
 * Join two workspace nodes and persist the result as a new child node.
 */
export const joinNodesMutation = (options?: Partial<Options<JoinNodesData>>): UseMutationOptions<JoinNodesResponse, JoinNodesError, Options<JoinNodesData>> => {
    const mutationOptions: UseMutationOptions<JoinNodesResponse, JoinNodesError, Options<JoinNodesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await joinNodes({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Join Nodes Preview
 *
 * Preview a join between two workspace nodes.
 */
export const joinNodesPreviewMutation = (options?: Partial<Options<JoinNodesPreviewData>>): UseMutationOptions<JoinNodesPreviewResponse, JoinNodesPreviewError, Options<JoinNodesPreviewData>> => {
    const mutationOptions: UseMutationOptions<JoinNodesPreviewResponse, JoinNodesPreviewError, Options<JoinNodesPreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await joinNodesPreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Reorder Workspace Nodes By Id
 *
 * Persist node order for the explicit workspace id in the path.
 *
 * Used by:
 * - frontend list-view drag-to-reorder because graph order mutations are
 * workspace-scoped and should not rely on hidden current-workspace state.
 *
 * Flow: load the requested workspace, apply the submitted order, persist, and
 * return the rebuilt graph in the new order.
 */
export const reorderWorkspaceNodesByIdMutation = (options?: Partial<Options<ReorderWorkspaceNodesByIdData>>): UseMutationOptions<ReorderWorkspaceNodesByIdResponse, ReorderWorkspaceNodesByIdError, Options<ReorderWorkspaceNodesByIdData>> => {
    const mutationOptions: UseMutationOptions<ReorderWorkspaceNodesByIdResponse, ReorderWorkspaceNodesByIdError, Options<ReorderWorkspaceNodesByIdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await reorderWorkspaceNodesById({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Node
 *
 * Delete a node from the workspace.
 */
export const deleteNodeMutation = (options?: Partial<Options<DeleteNodeData>>): UseMutationOptions<DeleteNodeResponse, DeleteNodeError, Options<DeleteNodeData>> => {
    const mutationOptions: UseMutationOptions<DeleteNodeResponse, DeleteNodeError, Options<DeleteNodeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteNode({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Cast Node
 *
 * Cast a single node column in place.
 *
 * Used by:
 * - frontend preprocessing/data-table cast actions because the HTTP route
 * owns workspace resolution and persistence while ``core.node_casting`` owns
 * the Polars expression workflow.
 *
 * Flow:
 * - Resolve the workspace and target node from the path ids.
 * - Delegate cast validation and lazy-plan construction to
 * ``cast_lazyframe_column``.
 * - Persist the mutated workspace and return the existing cast response shape.
 */
export const castNodeMutation = (options?: Partial<Options<CastNodeData>>): UseMutationOptions<CastNodeResponse2, CastNodeError, Options<CastNodeData>> => {
    const mutationOptions: UseMutationOptions<CastNodeResponse2, CastNodeError, Options<CastNodeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await castNode({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Clone Node
 *
 * Clone a workspace node (deep-copy its LazyFrame).
 */
export const cloneNodeMutation = (options?: Partial<Options<CloneNodeData>>): UseMutationOptions<CloneNodeResponse, CloneNodeError, Options<CloneNodeData>> => {
    const mutationOptions: UseMutationOptions<CloneNodeResponse, CloneNodeError, Options<CloneNodeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await cloneNode({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Set Node Color
 *
 * Set the persistent visualization colour on a workspace node.
 */
export const setNodeColorMutation = (options?: Partial<Options<SetNodeColorData>>): UseMutationOptions<SetNodeColorResponse, SetNodeColorError, Options<SetNodeColorData>> => {
    const mutationOptions: UseMutationOptions<SetNodeColorResponse, SetNodeColorError, Options<SetNodeColorData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setNodeColor({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete Node Column
 *
 * Delete a column from a node by delegating to DocWorkspace Node.drop.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI DELETE /nodes/{node_id}/columns/{column_name} route because they need this unit's "Delete a column from a node by delegating to DocWorkspace Node.drop" behavior.
 */
export const deleteNodeColumnMutation = (options?: Partial<Options<DeleteNodeColumnData>>): UseMutationOptions<DeleteNodeColumnResponse, DeleteNodeColumnError, Options<DeleteNodeColumnData>> => {
    const mutationOptions: UseMutationOptions<DeleteNodeColumnResponse, DeleteNodeColumnError, Options<DeleteNodeColumnData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteNodeColumn({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Rename Node Column
 *
 * Rename a column by delegating to DocWorkspace Node.rename.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI PUT /nodes/{node_id}/columns/{column_name} route because they need this unit's "Rename a column by delegating to DocWorkspace Node.rename" behavior.
 */
export const renameNodeColumnMutation = (options?: Partial<Options<RenameNodeColumnData>>): UseMutationOptions<RenameNodeColumnResponse, RenameNodeColumnError, Options<RenameNodeColumnData>> => {
    const mutationOptions: UseMutationOptions<RenameNodeColumnResponse, RenameNodeColumnError, Options<RenameNodeColumnData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await renameNodeColumn({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const describeColumnQueryKey = (options: Options<DescribeColumnData>) => createQueryKey('describeColumn', options, false, ['nodes']);

/**
 * Describe Column
 *
 * Get descriptive statistics for a column using Polars describe.
 */
export const describeColumnOptions = (options: Options<DescribeColumnData>) => queryOptions<DescribeColumnResponse, DescribeColumnError, DescribeColumnResponse, ReturnType<typeof describeColumnQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await describeColumn({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: describeColumnQueryKey(options)
});

export const columnOperationsQueryKey = (options: Options<ColumnOperationsData>) => createQueryKey('columnOperations', options, false, ['nodes']);

/**
 * Column Operations
 *
 * Return available no-arg Polars operations for a column, filtered by dtype.
 */
export const columnOperationsOptions = (options: Options<ColumnOperationsData>) => queryOptions<ColumnOperationsResponse2, ColumnOperationsError, ColumnOperationsResponse2, ReturnType<typeof columnOperationsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await columnOperations({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: columnOperationsQueryKey(options)
});

export const getColumnUniqueValuesQueryKey = (options: Options<GetColumnUniqueValuesData>) => createQueryKey('getColumnUniqueValues', options, false, ['nodes']);

/**
 * Get Column Unique Values
 *
 * Return unique values for a column.
 */
export const getColumnUniqueValuesOptions = (options: Options<GetColumnUniqueValuesData>) => queryOptions<GetColumnUniqueValuesResponse, GetColumnUniqueValuesError, GetColumnUniqueValuesResponse, ReturnType<typeof getColumnUniqueValuesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getColumnUniqueValues({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getColumnUniqueValuesQueryKey(options)
});

export const getNodeDataByWorkspaceIdQueryKey = (options: Options<GetNodeDataByWorkspaceIdData>) => createQueryKey('getNodeDataByWorkspaceId', options, false, ['nodes']);

/**
 * Get Node Data By Workspace Id
 *
 * Return paginated node rows from an explicit workspace.
 *
 * Used by:
 * - frontend table, annotation preview, and sampling reads because callers
 * already have workspace id in selection state and cache keys.
 *
 * Flow: let the UUID path converter reject static route names, load the path
 * workspace id, then reuse the shared node-data response builder.
 */
export const getNodeDataByWorkspaceIdOptions = (options: Options<GetNodeDataByWorkspaceIdData>) => queryOptions<GetNodeDataByWorkspaceIdResponse, GetNodeDataByWorkspaceIdError, GetNodeDataByWorkspaceIdResponse, ReturnType<typeof getNodeDataByWorkspaceIdQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getNodeDataByWorkspaceId({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getNodeDataByWorkspaceIdQueryKey(options)
});

export const getNodeDataByWorkspaceIdInfiniteQueryKey = (options: Options<GetNodeDataByWorkspaceIdData>): QueryKey<Options<GetNodeDataByWorkspaceIdData>> => createQueryKey('getNodeDataByWorkspaceId', options, true);

/**
 * Get Node Data By Workspace Id
 *
 * Return paginated node rows from an explicit workspace.
 *
 * Used by:
 * - frontend table, annotation preview, and sampling reads because callers
 * already have workspace id in selection state and cache keys.
 *
 * Flow: let the UUID path converter reject static route names, load the path
 * workspace id, then reuse the shared node-data response builder.
 */
export const getNodeDataByWorkspaceIdInfiniteOptions = (options: Options<GetNodeDataByWorkspaceIdData>) => infiniteQueryOptions<GetNodeDataByWorkspaceIdResponse, GetNodeDataByWorkspaceIdError, InfiniteData<GetNodeDataByWorkspaceIdResponse>, QueryKey<Options<GetNodeDataByWorkspaceIdData>>, number | Pick<QueryKey<Options<GetNodeDataByWorkspaceIdData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<GetNodeDataByWorkspaceIdData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                page: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await getNodeDataByWorkspaceId({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getNodeDataByWorkspaceIdInfiniteQueryKey(options)
});

/**
 * Set Node Document Column
 *
 * Set or clear the document column on a workspace node.
 */
export const setNodeDocumentColumnMutation = (options?: Partial<Options<SetNodeDocumentColumnData>>): UseMutationOptions<SetNodeDocumentColumnResponse, SetNodeDocumentColumnError, Options<SetNodeDocumentColumnData>> => {
    const mutationOptions: UseMutationOptions<SetNodeDocumentColumnResponse, SetNodeDocumentColumnError, Options<SetNodeDocumentColumnData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setNodeDocumentColumn({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Polars Expression Apply
 *
 * Apply a Polars expression and persist the result.
 */
export const polarsExpressionApplyMutation = (options?: Partial<Options<PolarsExpressionApplyData>>): UseMutationOptions<PolarsExpressionApplyResponse2, PolarsExpressionApplyError, Options<PolarsExpressionApplyData>> => {
    const mutationOptions: UseMutationOptions<PolarsExpressionApplyResponse2, PolarsExpressionApplyError, Options<PolarsExpressionApplyData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await polarsExpressionApply({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Polars Expression Preview
 *
 * Preview a Polars expression applied to the source node.
 */
export const polarsExpressionPreviewMutation = (options?: Partial<Options<PolarsExpressionPreviewData>>): UseMutationOptions<PolarsExpressionPreviewResponse, PolarsExpressionPreviewError, Options<PolarsExpressionPreviewData>> => {
    const mutationOptions: UseMutationOptions<PolarsExpressionPreviewResponse, PolarsExpressionPreviewError, Options<PolarsExpressionPreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await polarsExpressionPreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Filter Node
 *
 * Create a new child node by filtering the source node's data.
 */
export const filterNodeMutation = (options?: Partial<Options<FilterNodeData>>): UseMutationOptions<FilterNodeResponse, FilterNodeError, Options<FilterNodeData>> => {
    const mutationOptions: UseMutationOptions<FilterNodeResponse, FilterNodeError, Options<FilterNodeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await filterNode({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Filter Preview
 *
 * Preview the result of a filter operation on the source node.
 */
export const filterPreviewMutation = (options?: Partial<Options<FilterPreviewData>>): UseMutationOptions<FilterPreviewResponse2, FilterPreviewError, Options<FilterPreviewData>> => {
    const mutationOptions: UseMutationOptions<FilterPreviewResponse2, FilterPreviewError, Options<FilterPreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await filterPreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Update Node Name
 *
 * Rename a workspace node.
 */
export const updateNodeNameMutation = (options?: Partial<Options<UpdateNodeNameData>>): UseMutationOptions<UpdateNodeNameResponse, UpdateNodeNameError, Options<UpdateNodeNameData>> => {
    const mutationOptions: UseMutationOptions<UpdateNodeNameResponse, UpdateNodeNameError, Options<UpdateNodeNameData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await updateNodeName({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getNodeQueryPlanQueryKey = (options: Options<GetNodeQueryPlanData>) => createQueryKey('getNodeQueryPlan', options, false, ['nodes']);

/**
 * Get Node Query Plan
 *
 * Return the Polars query plan for a workspace node.
 */
export const getNodeQueryPlanOptions = (options: Options<GetNodeQueryPlanData>) => queryOptions<GetNodeQueryPlanResponse, GetNodeQueryPlanError, GetNodeQueryPlanResponse, ReturnType<typeof getNodeQueryPlanQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getNodeQueryPlan({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getNodeQueryPlanQueryKey(options)
});

/**
 * Get Quotation
 *
 * Submit quotation extraction for selected node and store task payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend quotation run/search action because they need this unit's "Run quotation extraction on selected node and store latest task payload" behavior.
 *
 * Why:
 * - Keeps the initial request responsive by moving extraction/page collection
 * into a worker task.
 */
export const getQuotationMutation = (options?: Partial<Options<GetQuotationData>>): UseMutationOptions<GetQuotationResponse, GetQuotationError, Options<GetQuotationData>> => {
    const mutationOptions: UseMutationOptions<GetQuotationResponse, GetQuotationError, Options<GetQuotationData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await getQuotation({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Redo Node Operation
 *
 * Redo the latest undone in-memory execution plan change for a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/redo route because they need this unit's "Redo the latest undone in-memory execution plan change for a node" behavior.
 */
export const redoNodeOperationMutation = (options?: Partial<Options<RedoNodeOperationData>>): UseMutationOptions<RedoNodeOperationResponse, RedoNodeOperationError, Options<RedoNodeOperationData>> => {
    const mutationOptions: UseMutationOptions<RedoNodeOperationResponse, RedoNodeOperationError, Options<RedoNodeOperationData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await redoNodeOperation({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Replace Apply
 *
 * Apply a regex replace operation and persist the updated node.
 */
export const replaceApplyMutation = (options?: Partial<Options<ReplaceApplyData>>): UseMutationOptions<ReplaceApplyResponse2, ReplaceApplyError, Options<ReplaceApplyData>> => {
    const mutationOptions: UseMutationOptions<ReplaceApplyResponse2, ReplaceApplyError, Options<ReplaceApplyData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await replaceApply({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Replace Preview
 *
 * Preview a regex replace operation on the source node.
 */
export const replacePreviewMutation = (options?: Partial<Options<ReplacePreviewData>>): UseMutationOptions<ReplacePreviewResponse, ReplacePreviewError, Options<ReplacePreviewData>> => {
    const mutationOptions: UseMutationOptions<ReplacePreviewResponse, ReplacePreviewError, Options<ReplacePreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await replacePreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Run Sequential Analysis
 *
 * Run sequential analysis for one node and persist/update task payload.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend sequential-analysis run action because they need this unit's "Run sequential analysis for one node and persist/update task payload" behavior.
 *
 * Why:
 * - Produces aggregated time-series counts and stores them under an explicit task id.
 */
export const runSequentialAnalysisMutation = (options?: Partial<Options<RunSequentialAnalysisData>>): UseMutationOptions<RunSequentialAnalysisResponse, RunSequentialAnalysisError, Options<RunSequentialAnalysisData>> => {
    const mutationOptions: UseMutationOptions<RunSequentialAnalysisResponse, RunSequentialAnalysisError, Options<RunSequentialAnalysisData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await runSequentialAnalysis({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Preview Sequential Analysis
 *
 * Run the aggregation server-side but skip task registration and
 * conflict checks.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - frontend preview/count checks because they need row-count feedback without registering
 * or replacing the live analysis task.
 * - callers that pass ``include_data=true`` because they need the same aggregated rows the
 * regular endpoint produces without mutating task state.
 *
 * Why a separate endpoint rather than a flag on the existing one:
 * the existing endpoint is task-aware (rejects conflicting requests,
 * inherits chart_type, writes the result into the task store). All
 * of that machinery is wrong for a pure preview query.
 */
export const previewSequentialAnalysisMutation = (options?: Partial<Options<PreviewSequentialAnalysisData>>): UseMutationOptions<PreviewSequentialAnalysisResponse, PreviewSequentialAnalysisError, Options<PreviewSequentialAnalysisData>> => {
    const mutationOptions: UseMutationOptions<PreviewSequentialAnalysisResponse, PreviewSequentialAnalysisError, Options<PreviewSequentialAnalysisData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await previewSequentialAnalysis({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getNodeShapeQueryKey = (options: Options<GetNodeShapeData>) => createQueryKey('getNodeShape', options, false, ['nodes']);

/**
 * Get Node Shape
 *
 * Return the row x column shape of a workspace node.
 */
export const getNodeShapeOptions = (options: Options<GetNodeShapeData>) => queryOptions<GetNodeShapeResponse, GetNodeShapeError, GetNodeShapeResponse, ReturnType<typeof getNodeShapeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getNodeShape({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getNodeShapeQueryKey(options)
});

/**
 * Slice Node
 *
 * Create a new child node by slicing/sampling/shuffling the source.
 */
export const sliceNodeMutation = (options?: Partial<Options<SliceNodeData>>): UseMutationOptions<SliceNodeResponse, SliceNodeError, Options<SliceNodeData>> => {
    const mutationOptions: UseMutationOptions<SliceNodeResponse, SliceNodeError, Options<SliceNodeData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await sliceNode({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Slice Preview
 *
 * Preview the result of a slice/sample/shuffle on the source node.
 */
export const slicePreviewMutation = (options?: Partial<Options<SlicePreviewData>>): UseMutationOptions<SlicePreviewResponse, SlicePreviewError, Options<SlicePreviewData>> => {
    const mutationOptions: UseMutationOptions<SlicePreviewResponse, SlicePreviewError, Options<SlicePreviewData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await slicePreview({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Set Node Tokenization Preference
 *
 * Set or clear a tokenization preference on a workspace node.
 */
export const setNodeTokenizationPreferenceMutation = (options?: Partial<Options<SetNodeTokenizationPreferenceData>>): UseMutationOptions<SetNodeTokenizationPreferenceResponse, SetNodeTokenizationPreferenceError, Options<SetNodeTokenizationPreferenceData>> => {
    const mutationOptions: UseMutationOptions<SetNodeTokenizationPreferenceResponse, SetNodeTokenizationPreferenceError, Options<SetNodeTokenizationPreferenceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setNodeTokenizationPreference({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Undo Node Operation
 *
 * Undo the latest in-memory execution plan change for a node.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend and API clients through the FastAPI POST /nodes/{node_id}/undo route because they need this unit's "Undo the latest in-memory execution plan change for a node" behavior.
 */
export const undoNodeOperationMutation = (options?: Partial<Options<UndoNodeOperationData>>): UseMutationOptions<UndoNodeOperationResponse, UndoNodeOperationError, Options<UndoNodeOperationData>> => {
    const mutationOptions: UseMutationOptions<UndoNodeOperationResponse, UndoNodeOperationError, Options<UndoNodeOperationData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await undoNodeOperation({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Get Workspace Nodes Info By Id
 *
 * Return node metadata for requested nodes in an explicit workspace.
 *
 * Used by:
 * - generated frontend clients and API callers whose cache keys already carry
 * workspace id and should send the same id across the HTTP boundary.
 *
 * Flow: accept a body-based batch-get request, load the path workspace id,
 * then reuse the collection node-info response builder.
 */
export const getWorkspaceNodesInfoByIdMutation = (options?: Partial<Options<GetWorkspaceNodesInfoByIdData>>): UseMutationOptions<GetWorkspaceNodesInfoByIdResponse, GetWorkspaceNodesInfoByIdError, Options<GetWorkspaceNodesInfoByIdData>> => {
    const mutationOptions: UseMutationOptions<GetWorkspaceNodesInfoByIdResponse, GetWorkspaceNodesInfoByIdError, Options<GetWorkspaceNodesInfoByIdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await getWorkspaceNodesInfoById({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Save Workspace By Id
 *
 * Persist the explicit workspace id in the path.
 *
 * Used by:
 * - frontend workspace manager save actions because the target workspace is
 * already known by selection state.
 *
 * Flow: load the requested workspace id, persist it through the shared saver,
 * and return the standard workspace action response.
 */
export const saveWorkspaceByIdMutation = (options?: Partial<Options<SaveWorkspaceByIdData>>): UseMutationOptions<SaveWorkspaceByIdResponse, SaveWorkspaceByIdError, Options<SaveWorkspaceByIdData>> => {
    const mutationOptions: UseMutationOptions<SaveWorkspaceByIdResponse, SaveWorkspaceByIdError, Options<SaveWorkspaceByIdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await saveWorkspaceById({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getWorkspaceTabsQueryKey = (options: Options<GetWorkspaceTabsData>) => createQueryKey('getWorkspaceTabs', options, false, ['workspace_tabs']);

/**
 * Get Workspace Tabs
 *
 * Return the persisted analysis-tab state for a workspace.
 *
 * Used by the frontend tab store on view entry to restore tabs (and their
 * task ids) after a reload. Returns the empty default when no sidecar exists.
 */
export const getWorkspaceTabsOptions = (options: Options<GetWorkspaceTabsData>) => queryOptions<GetWorkspaceTabsResponse, GetWorkspaceTabsError, GetWorkspaceTabsResponse, ReturnType<typeof getWorkspaceTabsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getWorkspaceTabs({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getWorkspaceTabsQueryKey(options)
});

/**
 * Put Workspace Tabs
 *
 * Replace the persisted analysis-tab state for a workspace.
 *
 * Used by the frontend tab store whenever tabs are created, closed, renamed,
 * reordered, activated, or wired to a new task id. Full-replacement semantics
 * write the whole tabs.json sidecar on every change.
 */
export const putWorkspaceTabsMutation = (options?: Partial<Options<PutWorkspaceTabsData>>): UseMutationOptions<PutWorkspaceTabsResponse, PutWorkspaceTabsError, Options<PutWorkspaceTabsData>> => {
    const mutationOptions: UseMutationOptions<PutWorkspaceTabsResponse, PutWorkspaceTabsError, Options<PutWorkspaceTabsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await putWorkspaceTabs({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Calculate token frequencies for selected nodes
 *
 * Calculate and compare token frequencies across one or two nodes using polars-text
 */
export const calculateTokenFrequenciesMutation = (options?: Partial<Options<CalculateTokenFrequenciesData>>): UseMutationOptions<CalculateTokenFrequenciesResponse, CalculateTokenFrequenciesError, Options<CalculateTokenFrequenciesData>> => {
    const mutationOptions: UseMutationOptions<CalculateTokenFrequenciesResponse, CalculateTokenFrequenciesError, Options<CalculateTokenFrequenciesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await calculateTokenFrequencies({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Clear Topic Modeling Results
 *
 * Clear stored topic-modeling task state for a workspace.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend clear action: `DELETE /workspaces/{id}/topic-modeling` because they need this unit's "Clear stored topic-modeling task state for a workspace" behavior.
 *
 * Why:
 * - Removes explicit topic-modeling task records for the requested workspace.
 */
export const clearTopicModelingResultsMutation = (options?: Partial<Options<ClearTopicModelingResultsData>>): UseMutationOptions<ClearTopicModelingResultsResponse, ClearTopicModelingResultsError, Options<ClearTopicModelingResultsData>> => {
    const mutationOptions: UseMutationOptions<ClearTopicModelingResultsResponse, ClearTopicModelingResultsError, Options<ClearTopicModelingResultsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await clearTopicModelingResults({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Run Topic Modeling
 *
 * Submit topic-modeling analysis as a worker-backed background task.
 *
 * Flow:
 * - Resolve authentication and request parameters from FastAPI dependencies.
 * - Delegate validation, manager calls, artifacts, or state changes to the owning helper.
 * - Shape the response payload or raise the HTTP error the client should see.
 *
 * Used by:
 * - Frontend run route: `POST /workspaces/{id}/topic-modeling` because they need this unit's "Submit topic-modeling analysis as a worker-backed background task" behavior.
 *
 * Why:
 * - Offloads heavy modeling work to worker processes and returns `task_id`
 * for progress/result polling.
 */
export const runTopicModelingMutation = (options?: Partial<Options<RunTopicModelingData>>): UseMutationOptions<RunTopicModelingResponse, RunTopicModelingError, Options<RunTopicModelingData>> => {
    const mutationOptions: UseMutationOptions<RunTopicModelingResponse, RunTopicModelingError, Options<RunTopicModelingData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await runTopicModeling({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Unload Workspace
 *
 * Unload the explicit workspace id in the path.
 *
 * Used by:
 * - workspace manager clients that want to unload the active workspace while
 * keeping the target visible in the URL.
 *
 * Flow: resolve the authenticated user, pass the path workspace id to the
 * manager unload operation, and return the standard action response or
 * a not-found error when that workspace does not exist.
 */
export const unloadWorkspaceMutation = (options?: Partial<Options<UnloadWorkspaceData>>): UseMutationOptions<UnloadWorkspaceResponse, UnloadWorkspaceError, Options<UnloadWorkspaceData>> => {
    const mutationOptions: UseMutationOptions<UnloadWorkspaceResponse, UnloadWorkspaceError, Options<UnloadWorkspaceData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await unloadWorkspace({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const healthCheckQueryKey = (options?: Options<HealthCheckData>) => createQueryKey('healthCheck', options);

/**
 * Health Check
 *
 * Return lightweight health status for liveness probes.
 *
 * Used by:
 * - deployment health checks and uptime monitors because callers need the shared shared
 * backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Gives a fast no-auth probe endpoint for runtime readiness checks.
 */
export const healthCheckOptions = (options?: Options<HealthCheckData>) => queryOptions<unknown, HealthCheckError, unknown, ReturnType<typeof healthCheckQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await healthCheck({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: healthCheckQueryKey(options)
});

export const statusQueryKey = (options?: Options<StatusData>) => createQueryKey('status', options);

/**
 * Status
 *
 * Return detailed component/module status information.
 *
 * Used by:
 * - diagnostics pages and manual troubleshooting because callers need the shared shared
 * backend behavior rule in one place instead of duplicating it.
 * Why:
 * - Exposes richer operational metadata than ``/health``.
 */
export const statusOptions = (options?: Options<StatusData>) => queryOptions<unknown, StatusError, unknown, ReturnType<typeof statusQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await status({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: statusQueryKey(options)
});
